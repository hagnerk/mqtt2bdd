# Story 4.2: Prepare the Repository for Public Release

## Status

Ready for Review

## Story

**As a** maintainer,
**I want** the repository licensed, audited, and published on GitHub,
**so that** anyone can read the code and a release workflow has somewhere to publish to.

## Acceptance Criteria

> **ACs 1–10 are copied verbatim from Epic 4 in `docs/prd.md:726-749`.** None is reworded.
>
> **AC11 is an SM-homed carry-forward finding** (DOC-001, open since gate `3.5`), not part of the
> original epic text. Story 4.1's PO acceptance explicitly asked the SM to decide which of Stories
> 4.2 and 4.4 adopts it "rather than letting it carry forward a fourth time". The reasoning for
> homing it here rather than on 4.4 is in Dev Notes → Carry-Forward Findings.
>
> **ACs 6, 7, 8 and 10 cannot be completed by the development agent.** They require creating a
> public GitHub repository under the maintainer's account and pushing to it — an irreversible,
> outward-facing publication that is the maintainer's decision to make, not an agent's, and one the
> maintainer has explicitly deferred. They are **not dropped and not silently reinterpreted**: the
> story delivers everything they depend on, states precisely what remains, and carries an exact,
> runnable handover procedure in Dev Notes → Deferred: The Publication Runbook. The story's status
> and its QA gate both record the deferral openly rather than claiming a completion that did not
> happen.

1. `LICENSE` exists at the repository root, containing the unmodified MIT license text with
   copyright line `Copyright (c) 2026 Kevin Hagner`
2. `README.md` carries an MIT license badge next to the existing Go version badge, linking to
   the `LICENSE` file — this closes Story 3.5 AC13, which left the license badge conditional
   ("if applicable") because no license existed at the time
3. `README.md` gains a short **License** section at the end of the document, stating the license
   and pointing to the `LICENSE` file
4. Full git history audited for secrets before the first push: no real credential, private key,
   or non-template environment file is present in any commit. The audit is recorded in the
   story's Dev Agent Record, including the command used, so it is reproducible
5. `.gitignore` verified to still cover `.env`, `.env.prod`, `*.env`, `bin/` and `__debug_bin*`,
   and `git status` is clean before the push
6. A public GitHub repository exists at `github.com/hagnerk/mqtt2bdd`, matching exactly the
   `go.mod` module declaration set in Story 4.1 — any divergence and
   `go install github.com/hagnerk/mqtt2bdd/...` cannot resolve. Story 4.1 must be merged first;
   pushing an unrenamed module publishes a path that cannot be fixed retroactively for anyone
   who has already fetched it
7. Remote `origin` points to that repository and the full `main` branch history is pushed
8. The GitHub repository has a one-line description and topics set (`go`, `mqtt`, `postgresql`,
   `home-automation`, `iot`, `docker`) for discoverability
9. `README.md`'s Quick Start replaces the `<repository-url>` placeholder in the `git clone`
   command with the real repository URL
10. The pushed repository renders correctly on GitHub: README diagrams and tables display as
    intended, and every relative link in the documentation resolves
11. **[Carry-forward, DOC-001]** `README.md`'s Architecture-section diagram annotation is reworded
    so it no longer reads as if `main()` spawns the Paho callback goroutine. The annotation
    currently says main "starts the two goroutines above plus a third (Health Check, below)", while
    the prose two sentences later correctly states that the Paho callback goroutine is *not* one of
    the application's own goroutines — the two statements are in tension inside the same section.
    Open since gate `3.5`, deferred three times ("next time `README.md` is touched"); this story
    touches `README.md` three times over

## Tasks / Subtasks

- [x] Task 1 — Write `LICENSE` (AC: 1)
  - [x] Create `LICENSE` at the repository root with the **unmodified** MIT license text — the
        canonical OSI/SPDX wording, not a paraphrase and not a variant. A modified MIT text is no
        longer MIT, and GitHub's licence detector (which is what puts the "MIT" chip on the
        repository page) matches against the canonical wording; a reworded copy silently degrades to
        "Other".
  - [x] The copyright line reads exactly `Copyright (c) 2026 Kevin Hagner`. One line, one year, no
        range, no email address — the commit author email (`kevin.hagner@spyzone.fr`) is already
        public in the history and does not need restating in the licence.
  - [x] No file extension, no `.md`, no `.txt`: GitHub detects `LICENSE` (and `LICENSE.md`,
        `LICENSE.txt`) but the extensionless form is the convention this project's own Project
        Structure tree will list in Story 4.4 AC8.

- [x] Task 2 — Add the MIT badge next to the Go badge (AC: 2)
  - [x] `README.md:3` currently carries exactly one badge:
        `![Go Version](https://img.shields.io/badge/Go-1.23%2B-00ADD8?logo=go&logoColor=white)`.
        Add a second, immediately after it on the same line, separated by a single space so the two
        render side by side rather than stacking.
  - [x] The badge must **link** to the `LICENSE` file, per AC2's wording ("linking to the `LICENSE`
        file") — so it is a linked image, `[![License: MIT](…)](LICENSE)`, not a bare `![…](…)`.
        Use a **relative** link (`LICENSE`, not an absolute `https://github.com/…` URL): it resolves
        both on GitHub and in a local Markdown preview, and it does not need updating if the
        repository ever moves. This also keeps AC10's "every relative link resolves" honest.
  - [x] Match the existing badge's shields.io style so the pair looks deliberate rather than
        accreted: the Go badge uses the default flat style with an explicit hex colour; use
        shields.io's conventional MIT colour (`yellow`) unless that clashes visibly with the Go
        badge's `00ADD8`.
  - [x] **This closes Story 3.5 AC13**, which required a licence badge "if applicable" and was
        satisfied vacuously because no licence existed. Record that closure in Completion Notes — a
        conditional AC that becomes applicable later is exactly the kind of thing this project's own
        "FIND-017 failure mode" (a finding documented as resolved without an owning story closing
        it) exists to catch.

- [x] Task 3 — Add the License section at the end of `README.md` (AC: 3)
  - [x] `README.md` currently ends with the `### Docker Compose` subsection of `## Learning
        Resources` (the file's last line is a Docker Compose documentation link). Append a new
        top-level `## License` section **after** it, as the document's final section.
  - [x] Keep it short — AC3 says "a short License section". Name the licence, point at the `LICENSE`
        file with a relative link, and stop. Do not restate the licence text, do not add a
        contributor-licence-agreement paragraph, and do not add a "Contributing" section: neither is
        asked for by any AC in this epic, and inventing project governance is out of scope.

- [x] Task 4 — Audit the full git history for secrets, and record it reproducibly (AC: 4)
  - [x] This task is the gate on everything irreversible in this story. Run it **before** any
        publication step, and record every command **verbatim** in the Dev Agent Record — AC4's own
        wording requires the audit be reproducible by a reader, not merely asserted.
  - [x] Re-run all four audit commands yourself rather than accepting this draft's results. The
        draft ran them on 2026-07-29 and found the history clean; the point of re-running is that
        the history has grown by four commits since (Story 4.1's own), and an audit that is not
        re-run is not an audit.

    ```bash
    # 1. Has any env-shaped or key-shaped file EVER been added, in any commit?
    git log --all --diff-filter=A --name-only --format="" | sort -u \
      | grep -E '(^|/)\.env|\.pem$|\.key$|id_rsa|\.p12$|\.pfx$'

    # 2. Does any blob in the entire object database contain a private key?
    git rev-list --all --objects \
      | git cat-file --batch-check='%(objecttype) %(objectname) %(rest)' \
      | awk '$1=="blob"{print $2}' | git cat-file --batch \
      | grep -c "BEGIN .*PRIVATE KEY"

    # 3. Any assignment-shaped credential with a real (non-placeholder) value, in any commit?
    git grep -I -n -E "(PASSWORD|SECRET|TOKEN|API_KEY|ACCESS_KEY)=[^\"'[:space:]<\$]{6,}" \
      $(git rev-list --all) | sed 's/^[0-9a-f]*://' | sort -u

    # 4. Who authored the history that is about to become public?
    git log --all --format="%an <%ae>" | sort -u
    ```

  - [x] The expected results, and what each one means, are in Dev Notes → History Audit. Command 3
        **is expected to return matches** — around a dozen — and every one must be individually
        confirmed to be a development fixture (`dev_password`, `test_password`,
        `prod_test_password`) rather than a real credential. A "clean" result here is not zero
        matches; it is zero *unexplained* matches. Do not report this audit as clean without having
        read each line.
  - [x] Command 4 will report `Kevin Hagner <kevin.hagner@spyzone.fr>` as the sole author. This email
        becomes permanently public on publication and cannot be retracted from an already-fetched
        clone. The epic already records this as considered and accepted ("a professional address,
        accepted", `docs/prd.md:667`) — re-state the confirmation in Completion Notes rather than
        rediscovering it, but do state it: it is the one irreversible personal-data consequence of
        this story.
  - [x] **If any command returns something this draft does not predict, HALT** and report rather than
        judging it yourself. A secret in git history survives file deletion and requires history
        rewriting to remove — which, before the first push, is cheap, and after it, is not. This is
        the one place in the whole epic where stopping is unambiguously better than proceeding.

- [x] Task 5 — Verify `.gitignore` coverage and a clean tree (AC: 5)
  - [x] Confirm by reading `.gitignore` that all five patterns AC5 names are present: `.env`,
        `.env.prod`, `*.env`, `bin/`, `__debug_bin*`. All five are there today (`.gitignore:5,6,7`,
        `:10`, `:16`) — this is a confirm, not an edit.
  - [x] Confirm the patterns actually *work*, rather than merely existing, by asking git directly
        about the files that would be catastrophic to publish:

    ```bash
    git check-ignore -v dev/.env .env.prod bin/ __debug_bin262058197
    git ls-files | grep -E '(^|/)\.env' # must return ONLY the two .example templates
    ```

    `dev/.env` exists on disk today and holds `POSTGRES_PASSWORD=dev_password`; `__debug_bin262058197`
    exists at the repository root. Both must be reported ignored by `git check-ignore`, and neither
    may appear in `git ls-files`.
  - [x] Note the deliberate subtlety in `.gitignore`'s own comment (`.gitignore:1-4`): the patterns
        are `.env.prod` and `*.env` rather than `.env.*`, precisely so the committed template
        `.env.prod.example` stays tracked. Confirm both templates (`.env.prod.example`,
        `dev/.env.example`) are still tracked — an over-broad ignore that silently untracked them
        would be a regression this story would otherwise ship.
  - [x] `git status --short` clean at the end of the story's own work.

- [x] Task 6 — Replace the `<repository-url>` placeholder in Quick Start (AC: 9)
  - [x] `README.md:165` reads `git clone <repository-url>`. Replace the placeholder with
        `https://github.com/hagnerk/mqtt2bdd.git`.
  - [x] Use the **HTTPS** URL, not the SSH form (`git@github.com:hagnerk/mqtt2bdd.git`). Quick Start
        is the first thing a reader runs; HTTPS clones work for anyone, SSH works only for someone
        with a key registered on GitHub. The maintainer's own `gh` configuration uses SSH for *push*
        (`gh auth status` reports `Git operations protocol: ssh`), which is a separate concern
        handled in the publication runbook, not in the README's reader-facing instruction.
  - [x] This is a *forward* reference: it names a repository that does not exist yet at the time this
        story's code changes are made. That is correct and intended — the URL is what the repository
        *will* be, fixed by `go.mod`'s module declaration (Story 4.1) and by AC6. Record in
        Completion Notes that the URL is unverifiable until publication, rather than claiming it was
        checked.

- [x] Task 7 — DOC-001: reword the Architecture diagram annotation (AC: 11)
  - [x] The annotation inside the fenced diagram block (`README.md:72-74`) currently reads:
        "Main goroutine: initializes config/logger/mqtt/database, **starts the two goroutines above
        plus a third** (Health Check, below), then blocks on SIGTERM/SIGINT and coordinates the
        drain-then-exit shutdown sequence." The "two goroutines above" are the MQTT Client box
        (labelled "Paho callback goroutine") and the DB Writer box — but `main()` starts only the
        latter. The Paho callback goroutine is the library's, spawned by the Paho client, not by
        `main()`.
  - [x] Reword so the annotation agrees with the prose three lines below it, which is already
        correct ("plus a fourth concurrent path that is not one of the application's own goroutines:
        the Paho MQTT library's own callback goroutine"). Gate `3.5` suggested: "starts the DB Writer
        goroutine below plus a third (Health Check); the MQTT Client box above is the Paho library's
        own callback goroutine, not spawned directly by `main()`". Use that or an equivalent — the
        requirement is that the annotation no longer implies `main()` spawns the Paho goroutine.
  - [x] **Do not touch the ASCII diagram itself.** The diagram's box labels are accurate; only the
        prose annotation beneath it is wrong. Reflowing the diagram risks breaking its alignment for
        no gain, and AC10's "diagrams display as intended" is easier to keep true if the diagram is
        not edited at all.
  - [x] Verify the reworded annotation against `cmd/mqtt2bdd/main.go` directly rather than against
        gate `3.5`'s account. **`main()` contains three `go func()` statements, not two** — this is
        the number the draft got wrong and the PO corrected, so do not be surprised by it:
        `main.go:256` (`dbWriterLoop`, joined by `wg`), `main.go:268` (`healthCheckLoop`, joined by
        its own `healthWG`), and `main.go:313` — a short-lived shutdown-drain goroutine that does
        nothing but `wg.Wait()` and `close(drained)` so the drain can race
        `defaultShutdownTimeout` in a `select`. Only the first two are long-running application
        goroutines; the third exists for a few milliseconds during shutdown and is an implementation
        detail of the timeout race, not an architectural component. Confirm also that the message
        handler is *registered as a callback* rather than launched with `go` — that is the fact AC11's
        rewording turns on.
  - [x] **Do not widen AC11 to cover the drain goroutine.** The prose below the diagram reads "The
        application runs three goroutines started from `main()` — the main goroutine itself, the
        database writer (`dbWriterLoop`), and the health check (`healthCheckLoop`)". Counting the
        ephemeral drain goroutine would make that "four", and the PO has ruled that out of scope —
        see Dev Notes → Carry-Forward Findings → The Drain Goroutine. AC11 is narrowly about the
        annotation's claim that `main()` starts the *Paho* goroutine. Change that sentence and
        nothing else.

- [x] Task 8 — Verification pass on the changed `README.md` (AC: 2, 3, 9, 11)
  - [x] Render-check the Markdown locally as far as is possible without GitHub: confirm fenced code
        blocks are still balanced (`grep -c '^```' README.md` must stay **even**), that the new
        `## License` heading is at the same level as the other top-level sections, and that the badge
        line is a single line with two badges.
  - [x] Confirm every **relative** link in `README.md` resolves to a file that exists — this is the
        locally checkable half of AC10. Note the useful baseline the PO established: `README.md`
        currently contains **zero** relative links (every existing link is an `http(s)://` URL), so
        the two this story adds — the badge's `](LICENSE)` and the License section's — are the first
        and only ones, and `test -e LICENSE` is the whole of the check. Extract every `](…)` target
        that is not an `http(s)://` URL or a `#anchor`, and `test -e` each one; expect exactly the
        `LICENSE` targets and nothing else.
  - [x] `git status --short` and `git diff --stat`: the only files this story changes are `README.md`
        and the new `LICENSE`, plus this story file. No `.go` file, no `go.mod`, no Docker or Compose
        file, no architecture shard.
  - [x] No toolchain gate is required for this story: it changes no Go source. Run
        `docker compose exec go-dev go build ./...` from `dev/` once anyway, as a cheap confirmation
        that the working tree is still coherent, and record the result.

- [x] Task 9 — Publication: prepare, do not execute (AC: 6, 7, 8, 10 — **deferred**)
  - [x] **Do not create the GitHub repository, do not add the remote, and do not push.** Publishing
        a repository publicly is irreversible with respect to everything it exposes, it is the
        maintainer's decision, and the maintainer has deferred it. The `gh` CLI is installed and
        authenticated as `hagnerk` on this machine, so this restraint is a deliberate choice and not
        a missing capability — say so explicitly in Completion Notes, because "could not" and "chose
        not to" are different facts and a future reader deserves the accurate one.
  - [x] Verify that everything the deferred ACs depend on is genuinely ready, since that *is*
        deliverable: `head -1 go.mod` matches the repository path AC6 requires
        (`github.com/hagnerk/mqtt2bdd`, from Story 4.1); `git remote -v` is still empty, so nothing
        has been published by accident; the history audit (Task 4) has passed; `git status` is clean
        (Task 5).
  - [x] Reproduce the publication runbook from Dev Notes → Deferred: The Publication Runbook into
        Completion Notes as the story's handover, with each command mapped to the AC it satisfies, so
        the maintainer can execute it without re-reading the whole story.
  - [x] AC10 (renders correctly on GitHub) is **not verifiable before publication** and is not
        claimed. What *is* verifiable locally — balanced fences, relative links resolving — is done
        in Task 8; record precisely that split rather than blurring it.

## Dev Notes

### Previous Story Insights

- **Story 4.1** renamed the module path to `github.com/hagnerk/mqtt2bdd`, which is the exact path
  AC6 requires the GitHub repository to carry. Its PO acceptance confirmed `git remote -v` and
  `git tag` are both still empty, so this story inherits an unpublished history — the state the
  epic's whole sequencing argument depends on. [Source: docs/stories/4.1.story.md#po-validation-final]
- **Story 4.1 deliberately left `README.md:165`'s `<repository-url>` placeholder alone**, recording
  it as this story's AC9 and declining it under its own AC7 isolation rule. It is still there.
  [Source: docs/stories/4.1.story.md#completion-notes-list]
- **Story 4.1's PO acceptance asked the SM to home DOC-001** on either this story or 4.4, rather than
  let it carry forward a fourth time. This story adopts it as AC11 — see Carry-Forward Findings.
- **Story 3.5** wrote `README.md` in full, including the Go badge this story's AC2 sits next to, the
  `<repository-url>` placeholder AC9 replaces, and the Architecture annotation AC11 corrects. Its
  AC13 required a licence badge "**if applicable**" — vacuously satisfied at the time because no
  licence existed. AC2 here is what actually closes it.
  [Source: docs/qa/gates/3.5-write-educational-readme-documentation.yml]
- **Story 3.1** established, and gate `3.1` re-verified, that `dev/.env` carries a real value
  (`POSTGRES_PASSWORD=dev_password`) and is gitignored — directly relevant to Task 5's check that the
  ignore patterns still work rather than merely exist.
  [Source: docs/stories/3.1.story.md:151; docs/qa/gates/3.1-create-production-multi-stage-dockerfile.yml:125]

### History Audit (run live for this draft, 2026-07-29 — re-run by the Dev, not inherited)

The epic states the audit "has already been run during epic drafting and came back clean"
(`docs/prd.md:663-666`). It was re-run independently for this draft, against a history that has since
grown by Story 4.1's four commits. Findings, command by command:

**1. Env-shaped and key-shaped files ever added (any commit, any branch).** Exactly two matches, both
templates, both intentionally tracked:

```
.env.prod.example
dev/.env.example
```

No `.env`, no `.env.prod`, no `.pem`, no `.key`, no `id_rsa`, no `.p12`, no `.pfx` — ever, in any
commit. 215 distinct paths have been added across the whole history; these are the only two that
match an environment- or key-shaped pattern.

**2. Private keys anywhere in the object database.** Every blob in `git rev-list --all --objects`
piped through `git cat-file --batch` and grepped for `BEGIN … PRIVATE KEY`: **0 matches**. This is a
content scan of the full object database, not a filename scan — it would find a key pasted into a
Markdown file or a story's Dev Notes, which a filename check would miss.

**3. Assignment-shaped credentials with a non-placeholder value.** This returns roughly a dozen
matches, and **that is the expected result**. Every one is a development fixture:

| Value | Where | What it is |
| --- | --- | --- |
| `POSTGRES_PASSWORD=dev_password` | `dev/.env.example:8`, and quoted in Stories `1.1`, `1.5`, `1.6`, `1.7`, `3.1` and gate `3.1` | The dev stack's throwaway password, in the committed **template**. The real `dev/.env` that uses it is gitignored. |
| `POSTGRES_PASSWORD=wrong_password` | `docs/stories/1.7.story.md:434` | A deliberately wrong value, in a documented negative-path test. |
| `POSTGRES_PASSWORD=test_password` | `test/run-integration-tests.sh:82` | The integration stack's throwaway password, generated into a stack that is torn down unconditionally. |
| `POSTGRES_PASSWORD=prod_test_password` | `docs/stories/3.2.story.md:1191,1384,1385` | The throwaway credential gate `3.2` used to exercise the production stack, recorded in the story's own transcript. Gate `3.2`'s SEC-001 note confirms the `.env.prod` holding it was gitignored before creation and deleted at cleanup. |

None is a credential to a system that exists. None grants access to anything. This matches the
epic's own pre-drafting audit conclusion exactly ("the only credential-shaped strings in the tree are
test fixtures", `docs/prd.md:666`) — re-derived here rather than accepted from it.

**4. Authors.** One: `Kevin Hagner <kevin.hagner@spyzone.fr>`. This address becomes permanently
public and is unretractable from an already-fetched clone. Accepted deliberately in the epic
(`docs/prd.md:666-667`): a professional address, not a personal one.

**Audit verdict: clean.** Nothing in the history requires rewriting before publication, and no
`git filter-repo`/BFG step is needed. The Dev re-runs all four commands regardless — an audit
inherited from a draft is not an audit.

### Deferred: The Publication Runbook (ACs 6, 7, 8, 10)

**Why these are deferred rather than done.** Creating a public repository and pushing history to it
is outward-facing and irreversible: once fetched, a clone cannot be recalled, and a published module
path cannot be changed retroactively for anyone who has already resolved it. That decision belongs to
the maintainer, who has deferred it. This is a *deferral*, not a blocker for the rest of the epic:
Story 4.3 writes the release workflow as a file in `.github/workflows/`, which is authored and
reviewable locally; only its AC12 end-to-end rehearsal shares this dependency.

**The `gh` CLI is present and authenticated** on this machine (`/opt/homebrew/bin/gh`, logged in to
`github.com` as `hagnerk`, git protocol `ssh`). Not executing the runbook is a choice, not an
incapacity. Record it as such.

**Preconditions — all satisfied by this story, verify before running anything below:**

| Precondition | Check | Why it matters |
| --- | --- | --- |
| Module path matches the target repository | `head -1 go.mod` → `module github.com/hagnerk/mqtt2bdd` | AC6. A divergence and `go install github.com/hagnerk/mqtt2bdd/...` cannot resolve. |
| History audited clean | Task 4, all four commands | AC4. A secret published is a secret retrievable forever. |
| Nothing published by accident | `git remote -v` → empty | Confirms this is genuinely the first publication. |
| Working tree clean | `git status --short` → empty | AC5. |
| On `main` | `git rev-parse --abbrev-ref HEAD` → `main` | AC7 pushes `main` specifically. |

**Runbook:**

```bash
# AC6 + AC8 — create the public repository with its description and topics in one call.
gh repo create hagnerk/mqtt2bdd --public \
  --description "Bridge home automation MQTT messages into PostgreSQL. A single wildcard subscription, a buffered channel, and a static Go binary." \
  --source . --remote origin --push

# AC8 — topics are not settable by `gh repo create`; they need a second call.
gh repo edit hagnerk/mqtt2bdd \
  --add-topic go --add-topic mqtt --add-topic postgresql \
  --add-topic home-automation --add-topic iot --add-topic docker

# AC7 — verify the remote and that main is fully pushed.
git remote -v
git rev-parse HEAD && git rev-parse origin/main   # must match

# AC10 — verify rendering by eye at https://github.com/hagnerk/mqtt2bdd :
#   - the Architecture ASCII diagram keeps its alignment inside its ```text fence
#   - the Configuration table renders as a table
#   - both badges render side by side and the MIT one links to LICENSE
#   - GitHub shows an "MIT" licence chip in the sidebar (proves Task 1's text is canonical)
#   - the relative links checked locally in Task 8 resolve on the rendered page too
```

`--source . --remote origin --push` does AC6 and AC7 in one step; the alternative
(`gh repo create` bare, then `git remote add origin …`, then `git push -u origin main`) is equivalent
and may be preferred if the maintainer wants to inspect the empty repository first.

**What to do if AC10 finds a rendering defect:** fix it in a follow-up commit and push. Unlike the
publication itself, this is fully reversible — a rendering fix is an ordinary commit.

### Carry-Forward Findings

Gate `4.1`'s register carries four open, non-blocking items with no owning story. Their disposition
for this story:

| Item | Origin | Homed here? |
| --- | --- | --- |
| **DOC-001** — README Architecture-diagram annotation nit | gate `3.5` | **Yes — adopted as AC11.** |
| REL-001 — `mqtt_status` cannot distinguish a permanently-failing re-subscribe from a transient one | gate `3.3` | No. Requires a behavioural change in `healthCheckLoop`; this story touches no Go source. Still unowned. |
| TEST-001 — AC9's negative path in `test/run-integration-tests.sh` never independently re-exercised | gate `3.3` | No. This story does not run or touch the script. Story 4.3 wires it into CI and is its natural home. |
| Broker authentication (`allow_anonymous true`) | gate `3.2` / `security.md:228-252` | **Re-assessed, not adopted.** See below — this story is where that re-assessment was due. |

**Why DOC-001 is homed here rather than on Story 4.4.** Both stories edit `README.md` substantially,
so "next time `README.md` is touched" no longer discriminates between them. Three reasons favour this
story:

1. **This is the story that makes the README public.** DOC-001's entire cost is that it "could
   momentarily confuse the Go-beginner reader" (gate `3.5`) — a cost that is nearly zero while the
   repository is private and jumps the moment AC6 runs. Fixing it in the story that publishes, rather
   than the one after, is the difference between never shipping the defect publicly and shipping it
   and then correcting it.
2. **Story 4.4's README work is additive and elsewhere.** It adds an Installation section, extends
   the Project Structure tree and the Development section. The Architecture section is not in its
   path, so DOC-001 would sit in 4.4 as an unrelated errand.
3. **It has now been deferred three times** (gates `3.5`, `3.6`, `4.1`). This project's own
   "FIND-017 failure mode" is a finding recorded as handled without a story ever closing it; a fourth
   deferral is how that failure mode starts.

**The Drain Goroutine — observed during PO validation, ruled out of scope, not a finding.** While
verifying AC11's premise against `cmd/mqtt2bdd/main.go`, the PO found that `main()` contains three
`go func()` statements, not the two the draft asserted. The third (`main.go:313`) spawns a
short-lived goroutine that does nothing but `wg.Wait()` then `close(drained)`, so the drain can race
`defaultShutdownTimeout` inside a `select`. Taken literally, this makes `README.md`'s "The
application runs three goroutines started from `main()`" an undercount.

**Ruled out of scope, deliberately, for three reasons.** (1) It is not DOC-001: gate `3.5` reviewed
that exact sentence against `main.go` and passed it, and DOC-001 is narrowly about the annotation's
claim that `main()` starts the *Paho* goroutine — a different sentence and a different error.
(2) The prose's count is defensible as written: it enumerates the application's *architectural*
goroutines, and a goroutine that exists for a few milliseconds to make a `select` expressible is an
implementation detail of the timeout race, not a fourth concurrent component a reader needs in their
model. (3) Adding it would make the sentence longer and less clear in exchange for pedantic
precision, which is the opposite of what a README section aimed at a Go beginner needs.

Recorded here so that QA does not re-raise it as a new defect and so a future reader can see it was
noticed and judged rather than missed. **No change is made, no finding is raised, and AC11 is not
widened.**

**Broker authentication, re-assessed for publication (not adopted, no change made).** Story 4.1's
Dev Notes flagged that this story's AC4 audit is where the accepted-risk judgement gets re-made in
light of going public. Re-made here, and unchanged: `allow_anonymous true` lives in
`dev/mosquitto/mosquitto.conf` and `prod/mosquitto/mosquitto.conf`, is documented as a deliberate
choice for a private-LAN threat model in `docs/architecture/security.md:228-252` and in `README.md`'s
Security subsection, and the escape hatch (`MQTT_USERNAME`/`MQTT_PASSWORD`) is real and already
wired. Publishing the configuration does not expose the maintainer's own broker — it exposes a
*default in a template*, clearly labelled, that a reader deploys onto their own network. That is
disclosure, not exposure. **No change is made and no new finding is raised**; this paragraph exists so
that a reader can see the judgement was re-made at the moment it mattered rather than inherited.

### File Locations

| File | Change | AC |
| --- | --- | --- |
| `LICENSE` | **New.** Canonical MIT text, `Copyright (c) 2026 Kevin Hagner`. | 1 |
| `README.md:3` | Add linked MIT badge after the Go badge, same line. | 2 |
| `README.md:72-74` | Reword the Architecture diagram annotation. | 11 |
| `README.md:165` | `<repository-url>` → `https://github.com/hagnerk/mqtt2bdd.git`. | 9 |
| `README.md` (end) | Append a `## License` section as the final section. | 3 |
| `.gitignore` | **Read only — no edit.** Verified, not changed. | 5 |
| `docs/stories/4.2.story.md` | This file. | — |

No `.go` file. No `go.mod`. No Docker, Compose, or architecture-shard change. **No `.github/`
directory** — that is Story 4.3's, and creating it here would be scope creep.

`README.md`'s Project Structure tree is **not** updated to list the new `LICENSE` file in this story:
that is explicitly Story 4.4 AC8's ("`README.md`'s Project Structure tree includes the new
`.github/workflows/` directory and the `LICENSE` file"), which handles both new entries together
once `.github/workflows/` exists. Adding `LICENSE` alone here would leave 4.4 with half an AC and
would put a tree entry in the README before the file it describes has any sibling. Record this as a
deliberate deferral in Completion Notes so QA does not read it as an omission.

### Technical Constraints

- **English everywhere**, including the `LICENSE` (canonical text is English by definition) and the
  commit message. [Source: CLAUDE.md]
- **Docker-only tooling.** This story runs no Go toolchain command it needs, but the one confirmation
  build in Task 8 goes through `docker compose exec go-dev` from `dev/`, never on the host.
- **No new dependency, no code change.** `go.mod`, `go.sum` and every `.go` file are untouched.
- **Irreversibility is the governing constraint.** Task 4 gates Task 9, and Task 9 stops short of the
  irreversible step deliberately.

### Testing Requirements

There is nothing here for an automated test to assert: the story adds a licence file and edits prose.
Per `docs/architecture/test-strategy-and-standards.md`'s own philosophy — no test that does not
express a real behavioural expectation — no test is added, exactly as Stories 3.5 and 4.1 concluded
for the same reason.

The story's Definition of Done is instead a set of **falsifiable local checks** (Task 8): balanced
code fences, every relative link resolving to a file that exists, a clean `git status`, and a diff
confined to `README.md` + `LICENSE`. AC10's GitHub-rendering half is explicitly **not** claimed and
is carried in the deferred set.

## Testing

- **AC1**: `LICENSE` exists at the repository root; contains the exact string
  `Copyright (c) 2026 Kevin Hagner`; its body matches the canonical MIT text (compare against
  SPDX `MIT`, not against memory).
- **AC2**: `README.md:3` contains two badges on one line; the MIT one is a linked image whose target
  is the relative path `LICENSE`; `test -e LICENSE` passes.
- **AC3**: `## License` is the final top-level section of `README.md`; it names MIT and links to
  `LICENSE`.
- **AC4**: all four audit commands re-run and their output recorded verbatim in the Dev Agent Record;
  command 3's matches individually accounted for as fixtures.
- **AC5**: `git check-ignore -v dev/.env .env.prod bin/ __debug_bin262058197` reports all four
  ignored; `git ls-files | grep -E '(^|/)\.env'` returns only `.env.prod.example` and
  `dev/.env.example`; `git status --short` empty.
- **AC9**: `grep -n "repository-url" README.md` → **no match**;
  `grep -n "git clone" README.md` → the real HTTPS URL.
- **AC11**: the reworded annotation no longer implies `main()` spawns the Paho goroutine; verified
  against `cmd/mqtt2bdd/main.go`'s actual `go ` statements, not against gate `3.5`'s account.
- **Markdown integrity**: `grep -c '^```' README.md` is **even**; every non-`http`, non-`#` link
  target in `README.md` passes `test -e`.
- **Scope**: `git diff --stat` shows only `README.md`, `LICENSE`, and this story file.
- **ACs 6, 7, 8, 10**: **not tested, not claimed.** Deferred to the maintainer per Task 9; the
  runbook and its preconditions are recorded instead.

[Source: docs/architecture/test-strategy-and-standards.md; docs/architecture/coding-standards.md]

## Change Log

| Date | Version | Description | Author |
| ---- | ------- | ------------ | ------ |
| 2026-07-29 | 0.1 | Initial draft. ACs 1–10 copied verbatim from Epic 4. AC11 added: DOC-001 adopted from gate `3.5`'s carry-forward register, per Story 4.1's PO acceptance asking the SM to home it on 4.2 or 4.4 — reasoning for choosing 4.2 recorded in Dev Notes. History audit run live (four commands, all four results recorded); verdict clean, with command 3's dozen fixture matches individually accounted for. ACs 6, 7, 8, 10 scoped as deferred-to-maintainer with a runnable publication runbook and an explicit note that `gh` is authenticated, so the deferral is a choice and not an incapacity. Broker-authentication accepted risk re-assessed for publication and left unchanged. | Bob (Scrum Master) |
| 2026-07-29 | 0.2 | PO draft validation. **One substantive defect found.** Task 7 instructed the Dev to "confirm that exactly two `go ` statements exist in `main()`"; `grep -n "go func" cmd/mqtt2bdd/main.go` returns **three** (`:256` `dbWriterLoop`, `:268` `healthCheckLoop`, `:313` an ephemeral shutdown-drain goroutine that only does `wg.Wait()` then `close(drained)`). A Dev following the draft literally would have hit a discrepancy while verifying AC11's own premise and either halted or widened the fix. Task 7 now states the real count with each site's role, and a new subtask explicitly forbids widening AC11 to cover the drain goroutine; the reasoning is recorded in Dev Notes → Carry-Forward Findings → The Drain Goroutine, which rules it out of scope as **not a finding** so QA does not re-raise it. Three line-reference corrections: PRD AC range `724-749` → `726-749`; the Architecture annotation is at `README.md:72-74`, not `73-75`; `docs/prd.md:666-667` for the author-email acceptance. Task 8 gained a useful baseline the PO established: `README.md` contains **zero** relative links today (grepped — every existing link is `http(s)://`), so the two this story adds are the first, which makes AC10's locally-checkable half a one-file check rather than a sweep. Independently confirmed: all four `.gitignore` patterns present at `:5,:6,:7,:10,:16` and all four working (`git check-ignore -v` reports `dev/.env`, `.env.prod`, `bin/`, `__debug_bin262058197` ignored); `git ls-files` shows exactly the two `.example` templates; `README.md` fence count 34 (even); `docs/architecture/security.md:228` is genuinely the accepted-risk section the story cites and `allow_anonymous true` is in both `dev/` and `prod/` broker configs as described. The four-command history audit was re-run and its results reproduce the draft's table exactly. **The ACs 6/7/8/10 deferral is accepted as scoped**, not treated as an incomplete story — see PO Validation (Draft) → On the Deferral. All 11 ACs trace to at least one task. Status → **Approved**. | Sarah (PO) |
| 2026-07-29 | 0.3 | Dev implementation (James). Tasks 1–8 complete; **Task 9 executed as specified, which means the publication itself was deliberately not performed**. `LICENSE` created with the canonical MIT text and `Copyright (c) 2026 Kevin Hagner` (AC1). MIT badge added as a linked image to the relative path `LICENSE`, on the same line as the Go badge (AC2) — closing Story 3.5's vacuously-satisfied AC13. `## License` appended as the final section (AC3). All four history-audit commands re-run against a history four commits longer than the draft's; results reproduce the draft's table exactly: 2 template files, 0 private keys, 17 fixture matches, 1 author (AC4). `.gitignore` verified working, not merely present — `git check-ignore -v` confirms all four dangerous paths caught, and `git ls-files` shows only the two `.example` templates (AC5). `<repository-url>` replaced with the HTTPS clone URL (AC9). DOC-001 closed: the Architecture annotation now names the DB Writer goroutine specifically and states that the MQTT Client box is Paho's own callback goroutine (AC11) — verified against `main.go`'s three `go func()` sites, and the drain goroutine left out of scope per the PO's ruling. Markdown integrity confirmed: 34 fences (even), and the single relative link target (`LICENSE`) exists. Confirmation build clean. Diff is `README.md` + new `LICENSE` + this story file, nothing else. **ACs 6, 7, 8, 10 remain open and are handed to the maintainer**, with the runbook reproduced in Completion Notes; `gh` was available and authenticated and was deliberately not used. Status → **Ready for Review**. | James (Dev) |

## PO Validation (Draft)

### Validation Date: 2026-07-29

### Validated By: Sarah (Product Owner)

### Verdict: **Approved** (after one substantive and three line-reference corrections)

### Method

Same standard as Story 4.1: every claim of the form "confirmed for this draft" was re-derived with
the command that would falsify it.

1. **AC text.** All ten ACs diffed against `docs/prd.md:726-749` — exact match, none reworded. The
   draft cited `724-749`; corrected. AC11 is clearly marked as SM-added and traceable to Story 4.1's
   PO acceptance, which is the right provenance for it.

2. **The defect that mattered: AC11's own premise, miscounted.** Task 7 told the Dev to "confirm that
   exactly two `go ` statements exist in `main()`". `grep -n "go func" cmd/mqtt2bdd/main.go` returns
   **three**: `:256` (`dbWriterLoop`, joined by `wg`), `:268` (`healthCheckLoop`, joined by its own
   `healthWG`), and `:313` — a goroutine that does nothing but `wg.Wait()` then `close(drained)`, so
   the drain can race `defaultShutdownTimeout` inside a `select`.

   This is not a trivial slip. Task 7's whole purpose is to make the Dev verify AC11's premise
   against live source rather than against gate `3.5`'s account — and the draft handed the Dev a
   wrong expected value for that verification. A Dev doing the check properly would have found three
   where the story promised two, and then faced an unguided choice: halt, or "fix" the README prose
   to say four. The second is scope creep on a story whose AC11 is narrowly about one sentence.

   Corrected in Task 7 with all three sites and their roles, plus a new subtask forbidding the
   widening, plus a Dev Notes ruling (The Drain Goroutine) that records why the README's "three
   goroutines started from `main()`" stays as written: gate `3.5` reviewed that exact sentence
   against `main.go` and passed it; the sentence enumerates the application's *architectural*
   goroutines, and a few-millisecond helper that exists to make a `select` expressible is not one;
   and lengthening it for pedantic precision is the opposite of what a beginner-facing README needs.
   Recorded as explicitly **not a finding** so QA does not re-raise it.

3. **The history audit re-run, not inherited.** All four commands executed against the live
   repository. Results reproduce the draft's table exactly: two `.example` templates and nothing else
   env- or key-shaped ever added across 215 distinct paths; zero `BEGIN … PRIVATE KEY` blobs in the
   full object database; roughly a dozen assignment-shaped matches, every one a documented fixture
   (`dev_password`, `wrong_password`, `test_password`, `prod_test_password`); one author,
   `Kevin Hagner <kevin.hagner@spyzone.fr>`. The draft is right that "clean" here means zero
   *unexplained* matches, not zero matches, and is right to make the Dev read each line — that
   framing is what keeps this audit meaningful rather than ceremonial.

4. **`.gitignore` verified working, not merely present.** All five patterns exist (`:5`, `:6`, `:7`,
   `:10`, `:16`) and `git check-ignore -v` confirms all four dangerous paths are actually caught:
   `dev/.env` (by `*.env`), `.env.prod` (by `.env.prod`), `bin/`, `__debug_bin262058197`.
   `git ls-files | grep -E '(^|/)\.env'` returns exactly `.env.prod.example` and `dev/.env.example`
   — the two templates the ignore file's own comment explains it is careful not to swallow.

5. **README facts.** Line 3 is the single Go badge as described. Line 165 is
   `git clone <repository-url>`. The Architecture annotation begins at line **72**, not 73 —
   corrected. Fence count is 34, even. `README.md` currently has **zero** relative links; the two
   this story adds are the first, which I have recorded in Task 8 because it turns AC10's
   locally-checkable half into a single `test -e`.

6. **Cited sources opened.** `docs/architecture/security.md:228` is genuinely the "MQTT Broker
   Authentication (Accepted Risk, Not a Story)" section. `allow_anonymous true` is present in both
   `dev/mosquitto/mosquitto.conf:2` and `prod/mosquitto/mosquitto.conf:22`, with the latter carrying
   the documented escape-hatch comment the story describes.

### On the Deferral of ACs 6, 7, 8 and 10

I am approving this story with four of its eleven ACs scoped as deferred, and I want the reasoning on
the record rather than implied.

**It is the right call.** Creating a public repository and pushing history is irreversible in a way
nothing else in this epic is: a fetched clone cannot be recalled, and the module path, once resolved
by anyone, cannot be changed retroactively. The maintainer has deferred it. An agent deciding to
publish anyway — because an AC said so — would be substituting its judgement for the owner's on
precisely the decision the owner reserved.

**It is scoped honestly, which is what makes it acceptable.** The draft does not quietly reinterpret
the ACs into something achievable, does not mark them done, and does not bury the gap. It states the
deferral in the AC preamble, in a dedicated Dev Notes section, in Task 9, and in the Testing section,
and it requires the story's status and QA gate to record it. It also delivers everything the deferred
ACs depend on — the module path (Story 4.1), the audit, the clean tree, the runbook — so the
remaining work is a maintainer running four commands, not a story restarted.

**The `gh`-is-authenticated point is the part I want kept.** `gh` is installed and logged in as
`hagnerk` on this machine. The draft insists the Dev record that not publishing was a *choice*, not
an incapacity. That distinction is the difference between an honest deferral and a quiet failure, and
a future reader deserves the accurate one.

**Consequence for the epic, stated once so it is not rediscovered:** Story 4.3 is unaffected in its
substance — a workflow file is authored and reviewed locally — but its AC12 (end-to-end rehearsal on
`v0.9.0-rc1`) shares this dependency and will need the same treatment. Story 4.4's AC10 ("every URL
and command verified against the actual published release") shares it too. The SM should scope both
the same way rather than rediscovering the constraint twice.

### Assessment Against the Story Draft Checklist

- **Goal & context clarity** — ✓. The story is explicit about what it delivers, what it defers, and
  why the deferral is a decision rather than an obstacle.
- **Technical guidance** — ✓ after correction. Every file and line is enumerated; the one place the
  draft handed the Dev a wrong expected value (Task 7's goroutine count) is fixed.
- **Reference effectiveness** — ✓ after correction. Every `[Source:]` citation was opened; three line
  references were stale and are fixed.
- **Self-containment** — ✓. The audit commands, the runbook, and the expected results are all in the
  story; the Dev needs no other document.
- **Testability** — ✓. Every non-deferred AC has a falsifiable check with a stated expected result,
  and the deferred ones are explicitly marked not-tested rather than assumed.

**Readiness: READY.** Clarity 9/10 — one point off for the goroutine miscount, which was in exactly
the subtask meant to enforce verifying against source.

### Note for the Dev Agent

1. **Task 7's corrected count is the one to trust: three `go func()` in `main()`, two of them
   architectural.** Do not widen AC11 when you see the third. The ruling is in Dev Notes.
2. **Task 4 gates Task 9.** Run the audit before anything else, and if any command returns something
   the story does not predict, halt and report rather than judging it. Before the first push this
   costs nothing to fix; after it, it cannot be fixed at all.
3. **Do not publish.** Prepare, verify, hand over. And say plainly in Completion Notes that `gh` was
   available and you chose not to use it.

## Dev Agent Record

### Agent Model Used

James (Dev), running as Claude Opus 5.

### Debug Log References

**Task 4 — the history audit, re-run in full (AC4).** All four commands were executed against the
live repository, not inherited from the draft. The history is four commits longer than when the
draft ran them (Story 4.1's `1fc34ea`, `98cb87e`, `933a4b8`, `c0a51cf`), which is exactly why the
story insists on re-running rather than citing.

```
# 1. Has any env-shaped or key-shaped file EVER been added, in any commit?
git log --all --diff-filter=A --name-only --format="" | sort -u \
  | grep -E '(^|/)\.env|\.pem$|\.key$|id_rsa|\.p12$|\.pfx$'
  -> .env.prod.example
  -> dev/.env.example
     (2 matches, both committed templates. 215 distinct paths have been added
      across the whole history; these are the only two that match at all.)

# 2. Does any blob in the entire object database contain a private key?
git rev-list --all --objects \
  | git cat-file --batch-check='%(objecttype) %(objectname) %(rest)' \
  | awk '$1=="blob"{print $2}' | git cat-file --batch \
  | grep -c "BEGIN .*PRIVATE KEY"
  -> 0

# 3. Any assignment-shaped credential with a real (non-placeholder) value?
git grep -I -n -E "(PASSWORD|SECRET|TOKEN|API_KEY|ACCESS_KEY)=[^\"'[:space:]<\$]{6,}" \
  $(git rev-list --all) | sed 's/^[0-9a-f]*://' | sort -u
  -> 17 matches, enumerated and accounted for below.

# 4. Who authored the history that is about to become public?
git log --all --format="%an <%ae>" | sort -u
  -> Kevin Hagner <kevin.hagner@spyzone.fr>
```

**Command 3's 17 matches, read individually rather than counted.** The story is explicit that a
clean result here is zero *unexplained* matches, not zero matches, so each was traced to its source:

| Value | Occurrences | Verdict |
| --- | --- | --- |
| `POSTGRES_PASSWORD=dev_password` | `dev/.env.example:8` (the committed template) and 12 quotations of it across Stories `1.1`, `1.5`, `1.6`, `1.7`, `3.1` and gate `3.1` | Development fixture. The real `dev/.env` that consumes it is gitignored and has never been tracked (command 1 proves this, not just the ignore file). |
| `POSTGRES_PASSWORD=wrong_password` | `docs/stories/1.7.story.md:434` | A deliberately *invalid* value in a documented negative-path test. Grants access to nothing by construction. |
| `POSTGRES_PASSWORD=test_password` | `test/run-integration-tests.sh:82` | The integration stack's throwaway password, in a stack torn down unconditionally at the end of every run. |
| `POSTGRES_PASSWORD=prod_test_password` | `docs/stories/3.2.story.md:1191,1384,1385` | The throwaway credential gate `3.2` used to exercise the production stack, recorded in that story's own transcript. Gate `3.2`'s SEC-001 note confirms the `.env.prod` holding it was gitignored before creation and deleted at cleanup. |

Seventeen matches, four distinct values, all four development fixtures. **Nothing in the history is a
credential to a system that exists.** No `git filter-repo`/BFG rewrite is needed, and none was run.

**On the author email.** `kevin.hagner@spyzone.fr` is the sole author across the entire history and
becomes permanently public at publication — unretractable from any clone already fetched. The epic
records this as considered and accepted (`docs/prd.md:666-667`, "a professional address, accepted").
Re-confirmed here rather than rediscovered: it is the one irreversible personal-data consequence of
this story, and the audit is not honest if it lists four fixture passwords and stays silent about the
one real identifier.

**Task 5 — `.gitignore` verified working, not merely present (AC5).**

```
grep -n '^\.env$\|^\.env\.prod$\|^\*\.env$\|^bin/$\|^__debug_bin\*$' .gitignore
  -> 5:.env   6:.env.prod   7:*.env   10:bin/   16:__debug_bin*      (all five present)

git check-ignore -v dev/.env .env.prod bin/ __debug_bin262058197
  -> .gitignore:7:*.env          dev/.env
  -> .gitignore:6:.env.prod      .env.prod
  -> .gitignore:10:bin/          bin/
  -> .gitignore:16:__debug_bin*  __debug_bin262058197              (all four caught, by the
                                                                    pattern each was meant to catch)

git ls-files | grep -E '(^|/)\.env'
  -> .env.prod.example
  -> dev/.env.example                                              (only the two templates)
```

The last check is the one that matters for the subtlety `.gitignore:1-4` documents: the patterns are
`.env.prod` and `*.env` rather than `.env.*` precisely so the committed templates stay tracked. Both
are still tracked — an over-broad ignore that silently untracked them would have been a regression
this story would otherwise have shipped into a public repository.

Note that `dev/.env` is caught by `*.env`, not by `.env` — the `.env` pattern on line 5 matches a
root-level `.env`, which does not currently exist. Both patterns are load-bearing; neither is
redundant.

**Task 8 — Markdown integrity and relative links (AC2, AC3, AC9, AC11).**

```
grep -c '^```' README.md                     -> 34   (even: all fences balanced, unchanged
                                                       from before this story's edits)
relative link targets (every ](…) that is not http(s):// and not #anchor):
  LICENSE  -> EXISTS
grep -n '^## ' README.md | tail -1           -> 467:## License   (the final top-level section)
sed -n '3p' README.md                        -> the two badges, one line, MIT one linked to LICENSE
grep -c 'repository-url' README.md           -> 0
grep -n 'git clone' README.md                -> 167:git clone https://github.com/hagnerk/mqtt2bdd.git
```

The relative-link check is the whole of AC10's locally-verifiable half, and the PO's baseline made it
cheap: `README.md` had zero relative links before this story, so the one added by the badge and the
one added by the License section are the only two, both pointing at `LICENSE`, which exists.

**Task 8 — confirmation build.** `docker compose exec go-dev go build ./...` from `dev/` → exit 0.
Not required by any AC (this story changes no Go source) but run as a cheap coherence check, per the
task's own instruction.

**Task 9 — publication preconditions (all satisfied; the publication itself deliberately not run).**

```
head -1 go.mod                    -> module github.com/hagnerk/mqtt2bdd   (matches AC6's target path)
git remote -v                     -> empty                               (nothing published by accident)
git rev-parse --abbrev-ref HEAD   -> main                                (AC7 pushes main specifically)
git status --short                -> only this story's own files          (AC5)
Task 4 audit                      -> clean                                (AC4 gates AC7)
```

### Completion Notes List

- **Task 1 (`LICENSE`, AC1).** Canonical MIT text, 21 lines, byte-for-byte the OSI/SPDX wording with
  only the copyright line filled in: `Copyright (c) 2026 Kevin Hagner`. No extension, at the
  repository root. Not paraphrased and not reflowed — GitHub's licence detector matches against the
  canonical wording, and a reworded copy silently degrades the repository's licence chip to "Other",
  which would be a real loss for a portfolio-facing project.
- **Task 2 (badge, AC2).** `[![License: MIT](https://img.shields.io/badge/License-MIT-yellow)](LICENSE)`
  appended to `README.md:3` after the Go badge, separated by one space so the two render side by
  side. It is a **linked** image, as AC2 requires ("linking to the `LICENSE` file"), and the link is
  **relative** — it resolves on GitHub, in a local Markdown preview, and after any future repository
  move, and it is the first relative link in the file, which keeps AC10's "every relative link
  resolves" trivially true.
- **Task 2 — Story 3.5 AC13 is now genuinely closed.** That AC required a licence badge "if
  applicable" and was satisfied *vacuously* in Story 3.5 because no licence existed. It is satisfied
  substantively for the first time here. Recording it explicitly, because a conditional AC that
  becomes applicable later is exactly this project's own "FIND-017 failure mode" — a requirement
  documented as handled without any story ever actually closing it.
- **Task 3 (License section, AC3).** One heading, one sentence, a relative link, and nothing else.
  Deliberately did **not** add a Contributing section, a CLA paragraph, or a code-of-conduct
  reference: none is asked for by any AC in this epic, and inventing project governance on the way
  past would be scope creep on a story whose whole point is to publish what exists.
- **Task 4 (audit, AC4).** Re-run in full, all 17 of command 3's matches read individually rather
  than counted, all four traced to development fixtures. Nothing surfaced that the story did not
  predict, so no HALT was needed. The author-email consequence is re-stated rather than assumed.
- **Task 5 (`.gitignore`, AC5).** Verified working rather than present. No edit was made or needed.
- **Task 6 (clone URL, AC9).** `<repository-url>` → `https://github.com/hagnerk/mqtt2bdd.git`. HTTPS,
  not SSH: Quick Start is the first thing a reader runs, and an HTTPS clone works for anyone whereas
  SSH requires a key already registered with GitHub. **This URL is a forward reference and is
  unverifiable today** — the repository does not exist yet. It is not claimed as checked; it is
  correct by construction, because it is fixed by `go.mod`'s module declaration (Story 4.1) and by
  AC6, which require exactly this path.
- **Task 7 (DOC-001, AC11).** The annotation now reads "starts the DB Writer goroutine above plus a
  second one (Health Check, below) … The MQTT Client box above is the Paho library's own callback
  goroutine, not one `main()` starts itself." It no longer implies `main()` spawns the Paho
  goroutine, and it now agrees with the prose three lines below it. The ASCII diagram itself was not
  touched, per the task's instruction — its box labels were already accurate, and reflowing it would
  risk AC10's "diagrams display as intended" for no gain.
- **Task 7 — the third `go func()`, handled as ruled.** Verified against `cmd/mqtt2bdd/main.go`
  directly: three `go func()` sites exist (`:256` `dbWriterLoop`, `:268` `healthCheckLoop`, `:313`
  the ephemeral drain goroutine), and the message handler is registered as a Paho callback rather
  than launched with `go` — which is the fact AC11's rewording turns on. Per the PO's explicit
  ruling, AC11 was **not** widened and `README.md`'s "three goroutines started from `main()`"
  sentence was left exactly as it is. Without the PO's correction this would have been a genuine
  fork in the road at implementation time.
- **Task 8 (verification).** 34 fences (even), one relative link target and it exists, `## License`
  confirmed as the final top-level section, zero remaining `repository-url` occurrences, build clean.
- **Deliberate deferral to Story 4.4:** `README.md`'s Project Structure tree was **not** updated to
  list the new `LICENSE` file. That is Story 4.4 AC8's job, which adds `LICENSE` and
  `.github/workflows/` to the tree together once the latter exists. Adding half of it here would
  leave 4.4 with half an AC. Flagging it so QA reads it as a scoped deferral rather than an omission.

### ACs 6, 7, 8 and 10 — Not Done, Handed Over

**These four ACs are not met.** They are not partially met, not met in spirit, and not reinterpreted.
Stating that plainly here because the alternative — burying it in a completion note — is how a
backlog quietly loses a requirement.

**Why not.** They require creating a public GitHub repository under the maintainer's account and
pushing the full history to it. That is outward-facing and irreversible: a fetched clone cannot be
recalled, and a resolved module path cannot be changed retroactively. It is the maintainer's decision
to make, and the maintainer has deferred it.

**`gh` was available and I chose not to use it.** `/opt/homebrew/bin/gh`, authenticated as `hagnerk`,
git protocol `ssh`. Three commands would have completed all four ACs. Recording this because "could
not" and "chose not to" are different facts, and only one of them is true here.

**Everything the four ACs depend on is ready.** Verified above under Task 9: the module path matches
the target repository exactly (AC6's requirement, delivered by Story 4.1), the history audit is
clean (the gate AC4 places in front of AC7), `git remote -v` is empty so this really is a first
publication, the working tree is clean, and the branch is `main`.

**Handover — run from the repository root, in this order:**

```bash
# AC6 + AC7 — create the public repository, wire origin, and push main in one call.
gh repo create hagnerk/mqtt2bdd --public \
  --description "Bridge home automation MQTT messages into PostgreSQL. A single wildcard subscription, a buffered channel, and a static Go binary." \
  --source . --remote origin --push

# AC8 — topics need a second call; gh repo create does not set them.
gh repo edit hagnerk/mqtt2bdd \
  --add-topic go --add-topic mqtt --add-topic postgresql \
  --add-topic home-automation --add-topic iot --add-topic docker

# AC7 — verify origin exists and main is fully pushed.
git remote -v
git rev-parse HEAD && git rev-parse origin/main    # the two hashes must match

# AC10 — verify by eye at https://github.com/hagnerk/mqtt2bdd :
#   - the Architecture ASCII diagram keeps its alignment inside its ```text fence
#   - the Configuration table renders as a table, not as pipes
#   - both badges render side by side, and the MIT one links to LICENSE
#   - GitHub shows an "MIT" chip in the sidebar (this is the proof Task 1's text is canonical)
#   - the LICENSE relative link resolves on the rendered page
```

If the maintainer prefers to inspect an empty repository first, `gh repo create hagnerk/mqtt2bdd
--public --description "…"` followed by `git remote add origin git@github.com:hagnerk/mqtt2bdd.git`
and `git push -u origin main` is equivalent.

**If AC10 finds a rendering defect**, fix it in an ordinary follow-up commit and push. Unlike the
publication itself, that is fully reversible.

**No blocking issue encountered** on the seven ACs this story could deliver. No new dependency, no Go
source change, no test added or edited, no ambiguity requiring a HALT during the audit.

**DoD self-check (`story-dod-checklist.md`):**

1. **Requirements Met** — [x] for ACs 1, 2, 3, 4, 5, 9, 11, each verified by command. **[ ] for ACs
   6, 7, 8, 10** — deferred to the maintainer, handover above, not claimed.
2. **Coding Standards & Project Structure** — [x] No Go source touched. English throughout, including
   the licence (canonical text) and the commit message. `LICENSE` at the repository root with no
   extension, the convention Story 4.4's Project Structure tree will list.
3. **Testing** — [x] No test added, and none should be: the story adds a licence file and edits
   prose, and there is no behavioural expectation to assert. Same conclusion Stories 3.5 and 4.1
   reached for the same reason.
4. **Functionality & Verification** — [x] Every command run for real and its output read; all
   transcripts above. The one thing not verified — that the clone URL resolves — is stated as
   unverifiable rather than asserted.
5. **Story Administration** — [x] All 9 tasks and all 34 subtasks marked `[x]`. Note that Task 9's
   subtasks being ticked means "prepared and handed over as instructed", **not** "published" — the
   task's own instruction is to stop short. The four deferred ACs are called out in their own
   section above so this cannot be misread from the checkboxes alone.
6. **Dependencies, Build & Configuration** — [x] `go build ./...` clean; `go.mod`, `go.sum` and every
   `.go` file untouched; no environment variable or configuration key introduced.
7. **Documentation** — [x] `README.md` updated in four places (badge, annotation, clone URL, License
   section); `LICENSE` added. No architecture shard needed changing. Project Structure tree
   deliberately left to Story 4.4 AC8.

Confirm: story is ready for review, with four ACs explicitly open.

### File List

- `LICENSE` — **new.** Canonical MIT text, 21 lines, `Copyright (c) 2026 Kevin Hagner` (AC1).
- `README.md` — modified in four places:
  - `:3` — MIT badge added as a linked image after the Go badge (AC2).
  - `:72-76` — Architecture diagram annotation reworded (AC11 / DOC-001).
  - `:167` — `<repository-url>` → `https://github.com/hagnerk/mqtt2bdd.git` (AC9).
  - end of file — new `## License` section (AC3).
- `docs/stories/4.2.story.md` — this file.

**Deliberately not modified:** `.gitignore` (AC5 is a verification, not an edit), `README.md`'s
Project Structure tree (Story 4.4 AC8), every `.go` file, `go.mod`, `go.sum`, `Dockerfile`, all
Compose files, and every architecture shard. **No `.github/` directory was created** — that is Story
4.3's.
