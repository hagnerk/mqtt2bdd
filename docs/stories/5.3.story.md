# Story 5.3: Exclude Topics from Persistence

## Status

Ready for Review

## Story

**As an** operator,
**I want** to list MQTT topic filters whose messages are not stored,
**so that** configuration traffic such as `zigbee2mqtt/bridge/#` does not fill `sensor_metrics`.

## Acceptance Criteria

> **ACs 1–10 are copied verbatim from Epic 5 in `docs/prd.md:933-950`.** None is reworded and none is added. Where an AC leaves a detail open, the answer is recorded in Dev Notes → SM Clarifications, with its source or its reasoning. Release target: **v1.1.0** (new configuration variable). It builds on Stories 5.1 and 5.2 (both Done, shipped as v1.0.1) but touches none of their code.

1. A new optional environment variable `MQTT_EXCLUDE_TOPICS` holds a comma-separated list of MQTT topic filters. Surrounding whitespace is trimmed, empty entries are ignored, and an unset or empty value excludes nothing
2. Each filter is validated at startup against MQTT 3.1.1 §4.7: `#` only as a whole, final level; `+` only as a whole level; no NUL character. An invalid filter aborts startup with `event=config_load_failed`, naming the offending filter
3. Matching follows MQTT semantics: `+` matches exactly one level; `#` matches the parent level and any number of child levels (`a/#` matches `a`); empty levels are significant; matching is case-sensitive; a topic starting with `$` is not matched by a filter starting with a wildcard
4. An excluded message is dropped in the reception handler before it reaches the buffer, and is not counted by `processed_last_interval`
5. Each exclusion is logged at DEBUG with `event=message_excluded`, `topic` and the matching `filter`; nothing is logged per message at INFO
6. The `config_loaded` entry reports the parsed list as `exclude_topics`
7. The subscription stays `#`: MQTT has no negative subscription, so excluded payloads still cross the network. The README states this
8. `MQTT_EXCLUDE_TOPICS` is documented in the README configuration table (with `zigbee2mqtt/bridge/#` as the example), in `.env.prod.example`, and passed through in `docker-compose.prod.yml`. `docs/architecture/components.md` and `docs/architecture/logging-standards.md` are updated
9. Unit tests cover filter validation, the matching rules above including the examples of MQTT 3.1.1 §4.7, and configuration parsing. An integration test shows that a message on an excluded topic is absent and a message on another topic is stored
10. Verification passes as in Story 5.1, AC12

## Tasks / Subtasks

- [x] Task 1 — Parse and validate `MQTT_EXCLUDE_TOPICS` in `internal/config` (AC: 1, 2)
  - [x] Add the field `ExcludeTopics []string // Optional — default none; MQTT topic filters whose messages are never persisted` to `Config`, under "Application Configuration", after `BufferSize`, matching `docs/architecture/components.md` → Configuration Structure
  - [x] Create `internal/config/topics.go` (`package config`, stdlib only: `fmt`, `strings`). It holds two unexported functions and nothing exported (SM Clarification C1):
    - `parseTopicFilters(raw string) ([]string, error)`: split `raw` on `,`, apply `strings.TrimSpace` to each entry, skip entries that are empty after trimming, validate each remaining entry with `validateTopicFilter`, and return them **in the configured order, duplicates kept** (C4). Return **`nil`**, not an empty slice, when no entry remains — unset, `""`, `"   "` and `", ,"` all give `nil` (C2)
    - `validateTopicFilter(filter string) error`: the three rules of AC2 and nothing else (C7). No NUL byte anywhere in the filter; split into levels on `/` (`strings.Split` is fine here: this runs once at startup); a level containing `#` must be exactly `#` **and** the last level; a level containing `+` must be exactly `+`. The returned error states which rule failed, in plain words (for example `"'#' must be the whole last level"`, `"'+' must be a whole level"`, `"contains a NUL character"`)
  - [x] The error returned by `parseTopicFilters` names the variable and the offending filter, with `%q` so surrounding or embedded oddities stay visible: `fmt.Errorf("MQTT_EXCLUDE_TOPICS: invalid topic filter %q: %w", filter, err)`. `%w` rather than `%v`, as the rest of `LoadConfig` does (see the comment at `internal/config/config.go:68-71`)
  - [x] In `LoadConfig`, in the "Optional variables with defaults" block after `BUFFER_SIZE`, call `parseTopicFilters(os.Getenv("MQTT_EXCLUDE_TOPICS"))`; on error, return `nil, err` unchanged. `cmd/mqtt2bdd/main.go` already turns any `LoadConfig` error into `event=config_load_failed` and exit 1 (`main.go:212-216`), so **no change is needed in `main` for the abort path** (AC2)
  - [x] Godoc/comments: on `parseTopicFilters`, why `nil` for "no entry" (comparisons and `len` checks stay trivial) and why duplicates are kept; on `validateTopicFilter`, cite MQTT 3.1.1 §4.7.1 and say which rules are deliberately not checked (C7). Explain *why*, not *what* (coding-standards → Comments)
  - [x] Do not log anything from `internal/config`: the package has no logger, and the error return is the contract

- [x] Task 2 — Topic matching in `internal/mqtt` (AC: 3)
  - [x] Create `internal/mqtt/topic.go` (`package mqtt`, stdlib only: `strings`) with one exported function: `func MatchTopic(filter, topic string) bool`. Godoc: MQTT 3.1.1 §4.7 matching semantics, the `$` rule, case sensitivity, and the precondition that `filter` is a valid filter (as `internal/config` guarantees); on an invalid filter the result is unspecified, but the function must never panic (C1)
  - [x] Algorithm (Dev Notes → Matching Algorithm): reject first when `topic` starts with `$` and `filter` starts with `+` or `#`; then walk both strings level by level with `strings.Cut(s, "/")`, which allocates nothing — this runs on every received message once filters are configured. Do not use `strings.Split`, `strings.HasPrefix` on the whole filter, `path.Match` or `regexp`
  - [x] Update `docs/architecture/components.md` → Component: MQTT Client → Key Interfaces with `MatchTopic(filter, topic string) bool` (Task 6)

- [x] Task 3 — Drop excluded messages in the reception handler (AC: 4, 5, 6)
  - [x] In `cmd/mqtt2bdd/main.go`, add an unexported helper near the other pure helpers (`connectionStatus`, `bufferUtilizationPercent`, `writeAgeSeconds`): `func excludingFilter(filters []string, topic string) (string, bool)` — returns the **first** filter, in configured order, for which `mqtt.MatchTopic(filter, topic)` is true (C4). With `filters == nil` the loop body never runs, so the v1.0.0 path costs nothing extra
  - [x] In `mqttMessageHandler` (`main.go:286`), make the exclusion check the **first statement**, before the `mqtt.Message` is built and before the non-blocking send (C6):
    ```go
    if filter, excluded := excludingFilter(cfg.ExcludeTopics, topic); excluded {
        l.Debug("message excluded", "event", "message_excluded", "topic", topic, "filter", filter)
        return
    }
    ```
    `l` is the `main` component logger (logging-standards lists `message_excluded` under `main`). No `operation` field (it reports a state); never the payload body; no level guard needed (both arguments are existing strings, nothing is converted)
  - [x] AC4's "not counted by `processed_last_interval`" holds by construction: that field is the delta of `dbClient.WriteCount()`, and an excluded message never reaches `dbWriterLoop`. Do **not** add a counter or a `health_check` field for exclusions (C13)
  - [x] `config_loaded` (`main.go:225`): append `"exclude_topics", strings.Join(cfg.ExcludeTopics, ",")` after `buffer_size`. It renders as `exclude_topics=zigbee2mqtt/bridge/#,other/+`, or `exclude_topics=""` when nothing is excluded (C3). Add `strings` to the stdlib import block
  - [x] Update the comment above `mqttMessageHandler` or the handler's first lines so a reader learns why the check comes first (an excluded message must never wait on, or be dropped by, a full buffer)
  - [x] Do not touch `internal/mqtt/client.go`: the subscription stays `mqttClient.Subscribe("#", …)` (AC7), and the DEBUG `message_received` entry keeps being logged before the handler runs (C5)

- [x] Task 4 — Unit tests (AC: 1, 2, 3, 9)
  - [x] **Compile trap — `internal/config/config_test.go`.** `Config` now contains a slice, so `*got != *tt.want` (line 235) **no longer compiles** (`struct containing []string cannot be compared`, verified in `go-dev`). Replace it with `!reflect.DeepEqual(got, tt.want)` and import `reflect`. Because `parseTopicFilters` returns `nil` for "no entry" (C2), the existing `want` values, which leave `ExcludeTopics` unset (`nil`), stay correct without edits. Add `"MQTT_EXCLUDE_TOPICS"` to `varToDeleteBeforeTests`
  - [x] In `internal/config/config_test.go` (black-box, `package config_test`), add `TestLoadConfig_ExcludeTopics`, table-driven. `allRequired` and `varToDeleteBeforeTests` are currently local to `TestLoadConfig`: move them to package level (unexported `var`s in `config_test.go`) so both tests share them, and have each row of the new test reset every variable in that list with `t.Setenv(v, "")` before applying its own values, exactly as `TestLoadConfig` does — otherwise the "unset" row would depend on the `go-dev` environment, which `dev/.env` feeds through `env_file`. Each row is `allRequired` plus one `MQTT_EXCLUDE_TOPICS` value, asserting `got.ExcludeTopics` with `reflect.DeepEqual` and, for error rows, that `err.Error()` contains both `MQTT_EXCLUDE_TOPICS` and the quoted offending filter. Rows (Dev Notes → Test Matrix → Parsing): unset, empty, blanks only, separators only, one filter, trimming and empty entries, duplicates, inner spaces kept, invalid only, a valid filter followed by an invalid one
  - [x] Create `internal/config/topics_test.go` as a **white-box** file (`package config`), because `validateTopicFilter` is unexported — the convention `internal/database/queries_test.go` and `sanitize_test.go` follow. `TestValidateTopicFilter`, table-driven, with every row of Dev Notes → Test Matrix → Validation. **The NUL rows can only be tested here:** an environment variable cannot hold a NUL byte (`os.Setenv`, hence `t.Setenv`, returns `invalid argument`, verified in `go-dev`) (C9). Write the NUL with the `\x00` escape in an interpreted string (`"a\x00b"`), never a raw NUL byte in the source file
  - [x] Create `internal/mqtt/topic_test.go` as a **black-box** file (`package mqtt_test`; the exported API is tested as a consumer would use it — test-strategy → Package convention; a directory may hold both `mqtt` and `mqtt_test` test files). `TestMatchTopic`, table-driven, `{filter, topic, want}`, with every row of Dev Notes → Test Matrix → Matching, including the §4.7 examples, each row named after what it proves
  - [x] In `cmd/mqtt2bdd/main_test.go`, add `TestExcludingFilter`: `nil` filters → `("", false)`; no match → `("", false)`; one match → that filter; two matching filters → the **first** one in slice order (for example `[]string{"a/+", "a/#"}` with topic `a/b` → `a/+`)
  - [x] Stdlib `testing` only; `t.Run(tt.name, …)`; `%q` in failure messages so empty levels and spaces are visible. No log assertions in unit tests (test-strategy)

- [x] Task 5 — Integration tests (AC: 2, 4, 5, 6, 9)
  - [x] Create `cmd/mqtt2bdd/topic_exclusion_integration_test.go` (`//go:build integration`, `package main`), modelled on `payload_repair_integration_test.go` and reusing the package helpers: `startTestApp`, `waitForEvent`, `eventLines`, `openTestDBPool`, `waitForRow`, `publishTestMessage`, `waitForEventCount` and `logCaptureTimeout` (both already defined in this package, see Previous Story Insights). **No `t.Parallel()`** (Dev Notes → MQTT ClientID Constraint)
  - [x] `TestTopicExclusion` — compute `suffix := time.Now().UnixNano()` **before** starting the app, because the filter must name run-unique topics:
    - `excludedRoot := fmt.Sprintf("integration/exclusion/excluded-%d", suffix)`, `filter := excludedRoot + "/#"`, `unusedFilter := fmt.Sprintf("integration/exclusion/unused-%d/+", suffix)`
    - env: the usual seven connection variables, `LOG_LEVEL=DEBUG` (`message_excluded` is DEBUG), and `MQTT_EXCLUDE_TOPICS = " " + filter + " ,, " + unusedFilter` (trimming and empty entries exercised end-to-end)
    - `app.waitForEvent(t, "startup_complete", 30*time.Second)`; then assert the `config_loaded` line contains `exclude_topics=` + `regexp.QuoteMeta(filter + "," + unusedFilter)` followed by whitespace or end of line (AC6)
    - publish, QoS 0, never retained, in this order, each with payload `{"value": 1}`: (1) `excludedRoot + "/bridge/state"` — excluded as a child level; (2) `excludedRoot` — excluded, since `a/#` matches `a` (AC3); (3) `excludedRoot + "-sibling"` — **stored**: same string prefix, different level, so a prefix-based matcher would wrongly exclude it; (4) `fmt.Sprintf("integration/exclusion/stored-%d", suffix)` — stored
    - `waitForRow(pool, topic, defaultRetryIntervalOnDatabaseFailure/2)` is true for (3) and (4)
    - `lines := waitForEventCount(t, app, "message_excluded", 2)`; for each of (1) and (2), exactly one line matches `topic=<QuoteMeta(topic)>(\s|$)`; each line has `level=DEBUG`, `filter=<QuoteMeta(filter)>(\s|$)`, no `\soperation=` and no `\spayload=`; no `message_excluded` line names (3) or (4) (AC5)
    - only **after** the two `message_excluded` lines are seen (the handler has run for both), `SELECT COUNT(*) FROM sensor_metrics WHERE sensor = ANY($1)` for (1) and (2) is `0` (AC4, AC9)
    - end with `app.terminate(t)` and `app.waitForExit(t, defaultShutdownTimeout+5*time.Second)`
  - [x] `TestTopicExclusion_InvalidFilter` (AC2, end-to-end): env with the seven connection variables and `MQTT_EXCLUDE_TOPICS=valid/#,sport/tennis/#/ranking`. `startTestApp`, then wait on `app.exited` (bounded, for example 10 s; fail the test on timeout). `app.waitErr` must be an `*exec.ExitError` with `ExitCode() == 1` — do **not** call `app.waitForExit`, which fails on a non-zero exit. Then **poll** for the log line with `app.waitForEvent(t, "config_load_failed", logCaptureTimeout)` rather than reading `allLines()` once: the stdout drain goroutine can still be appending the last line when `exited` closes (Previous Story Insights → "Log assertions must poll"). The captured output contains exactly one `config_load_failed` line (`len(app.eventLines("config_load_failed")) == 1`), at `level=ERROR`, whose text contains `regexp.QuoteMeta("sport/tennis/#/ranking")` (slog escapes the `%q` quotes, so match the filter itself, not the quotes), and contains no `starting` entry (the application aborted before logging it). No broker or database connection is made, so this test cannot collide with the others' client ID. Add `errors` and `os/exec` to the imports
  - [x] Nothing else changes in the integration setup: `test/docker-compose.yml`'s application and the smoke test in `test/run-integration-tests.sh` leave `MQTT_EXCLUDE_TOPICS` unset (v1.0.0 behaviour), and `internal/database`'s `TestMain` calls `config.LoadConfig()` with the variable unset, which yields `nil`

- [x] Task 6 — Configuration templates and documentation (AC: 7, 8)
  - [x] `docker-compose.prod.yml` → `mqtt2bdd.environment`: add `MQTT_EXCLUDE_TOPICS: ${MQTT_EXCLUDE_TOPICS:-}` after `BUFFER_SIZE` (the comment above the block says "Every value the application reads")
  - [x] `.env.prod.example` → "Application (optional)": add a commented block and **an empty value** (`MQTT_EXCLUDE_TOPICS=`), so a copied template keeps v1.0.0 behaviour (Epic 5 compatibility). The comment says: comma-separated MQTT topic filters whose messages are not stored; `+` and `#` wildcards allowed as whole levels, `#` only last; surrounding spaces trimmed; an invalid filter stops the application at startup; the subscription is still `#`, so excluded messages still reach the application; example `zigbee2mqtt/bridge/#`; default empty (nothing excluded). The file's own comment style is hard-wrapped at about 80 columns: follow it, since it is a shell-style comment block, not Markdown
  - [x] `dev/.env.example`: add `MQTT_EXCLUDE_TOPICS=` with a one-line comment in the file's style (C11)
  - [x] `README.md` → Configuration table: add a `MQTT_EXCLUDE_TOPICS` row after `BUFFER_SIZE` — not required, default empty (nothing excluded), description: comma-separated MQTT topic filters whose messages are not stored, for example `zigbee2mqtt/bridge/#`; `+`/`#` wildcards as whole levels (`#` last only), surrounding spaces trimmed, empty entries ignored; an invalid filter aborts startup with `event=config_load_failed`; the parsed list is reported as `exclude_topics` on `config_loaded`
  - [x] `README.md` → Features: add one bullet stating that topic filters in `MQTT_EXCLUDE_TOPICS` (for example `zigbee2mqtt/bridge/#`) keep configuration traffic out of `sensor_metrics`, and that **the subscription stays `#`**: MQTT has no negative subscription, so excluded messages are still delivered by the broker and discarded by the application (AC7). Adjust the first Features bullet and the Overview's "no per-sensor configuration is ever needed" only if they now read wrong; they remain true (nothing is required), so a light touch at most
  - [x] `README.md` → Troubleshooting: add a short entry in the list's existing style — a topic is missing from `sensor_metrics`: check `exclude_topics` on the `config_loaded` line; at `LOG_LEVEL=DEBUG`, each excluded message is logged as `message_excluded` with the `filter` that matched
  - [x] `README.md` → Project Structure (`cmd/mqtt2bdd/` tree) and Testing → Current test shape ("five files under `cmd/mqtt2bdd/`" becomes six) list `topic_exclusion_integration_test.go`
  - [x] `docs/architecture/components.md`: Configuration Manager → Validation Rules already describes the variable (the PO applied the proposal's §7.4 on 2026-09-17). Reconcile, do not rewrite: add the missing NUL rule, trimming and empty-entry behaviour, and that the check lives in `internal/config`; add `MatchTopic` to the MQTT Client's Key Interfaces; in the MQTT Client's Architecture Notes, qualify "Wildcard subscription (`#`) captures all topics without configuration" with one clause saying excluded topics are still received and dropped by `main` before the buffer
  - [x] `docs/architecture/logging-standards.md`: the status sentence under the `event` vocabulary table must say that all Epic 5 events are implemented (`message_excluded` by Story 5.3). Check the Topic exclusion row and `exclude_topics` in the Startup row against the code, and record in the Startup row that `exclude_topics` is the comma-joined list, `""` when empty (C3). The gate 5.2 `recommendations.future` item asks for exactly this status update
  - [x] `docs/architecture/core-workflows.md` already has the "Excluded topic" row: check it against the code, change nothing if it is right
  - [x] Do **not** touch `docs/prd.md`, `docs/architecture/source-tree.md` (stale on unrelated points), the monolithic `docs/architecture.md`, or `docs/architecture/test-strategy-and-standards.md` (its `*got != *tt.want` example is illustrative and predates this field; not in scope)
  - [x] Everything in English (project `CLAUDE.md`). Every Markdown paragraph, list item or table row you add or rewrite goes on a single line (maintainer's global preference). The README is hard-wrapped at about 95 columns in places: do not reflow paragraphs this story does not otherwise touch

- [x] Task 7 — Verification (AC: 10)
  - [x] From `dev/`, all through the container, never on the host: `docker compose exec go-dev gofmt -l .` (empty output), `docker compose exec go-dev go vet ./...`, `docker compose exec go-dev staticcheck ./...`, `docker compose exec go-dev go test ./...`; also `go vet -tags=integration ./...` and `staticcheck -tags=integration ./...`, as QA and the PO ran them for 5.1 and 5.2
  - [x] Coverage of the new code: `docker compose exec go-dev go test -cover ./internal/config/ ./internal/mqtt/` and record the per-function figures for `topics.go` and `topic.go` (`go test -coverprofile` then `go tool cover -func`); every branch of `validateTopicFilter` and `MatchTopic` is expected to be covered
  - [x] From the repository root: `./test/run-integration-tests.sh`, exit code 0, with `TestTopicExclusion`, `TestTopicExclusion_InvalidFilter`, `TestPayloadRepair` and `TestWriteRejection` visible as `PASS` in the `-v` output
  - [x] `git diff --stat -- go.mod go.sum` is empty (no new dependency)
  - [x] No created or modified file contains a NUL byte. From `dev/`: `docker compose exec go-dev sh -c 'for f in $(git ls-files -m -o --exclude-standard internal cmd docs README.md .env.prod.example docker-compose.prod.yml dev/.env.example); do tr -d "\000" < "$f" | cmp -s - "$f" || echo "NUL: $f"; done'` prints nothing (the NUL test rows must use the `\x00` escape, Task 4)
  - [x] Negative control, as Stories 5.1 and 5.2 did: temporarily make `excludingFilter` always return `("", false)`, run `./test/run-integration-tests.sh`, record that `TestTopicExclusion` fails (no `message_excluded` line, and rows stored for the two excluded topics), then restore the code and re-run green
  - [x] Record the command outputs, the captured `config_loaded` and `message_excluded` lines, the `config_load_failed` line, and the negative control in the Debug Log References
  - [x] Do not tag or publish **v1.1.0**: tagging is the maintainer's action once the story is Done (sprint change proposal §8, action 6)

## Dev Notes

### Context: why this story exists

Epic 5 was drafted after a production stall on `zigbee2mqtt/bridge/definitions`, a 283 KB retained configuration message. Stories 5.1 and 5.2 (Done, v1.0.1) made such messages harmless: rejected or repaired, never retried forever. This story addresses the other half of the problem: messages like `zigbee2mqtt/bridge/#` describe the installation, not measurements, and an operator should be able to keep them out of `sensor_metrics` altogether. [Source: docs/prd.md → Epic 5 → Epic Goal; docs/sprint-change-proposals/2026-09-17-write-pipeline-stall.md#1-identified-issue-summary]

**Compatibility (Epic 5):** no schema change, no new Go dependency, and `MQTT_EXCLUDE_TOPICS` is optional: when unset, the application subscribes to and stores everything, as in v1.0.0. With `ExcludeTopics == nil`, `excludingFilter` returns immediately and the handler behaves exactly as before; `config_loaded` gains one field. Rollback is `git revert` of this story alone. [Source: docs/prd.md → Epic 5 → Compatibility requirements]

**Requirements:** FR1 (discard, before buffering, messages whose topic matches an optional operator-supplied list of topic filters) and FR8 (topic exclusion list is configured by environment variable). [Source: docs/prd.md → Functional Requirements]

**Release:** v1.1.0, a backward-compatible feature (new configuration variable). [Source: docs/prd.md → Epic 5 → Enhancement details; docs/sprint-change-proposals/2026-09-17-write-pipeline-stall.md#4-recommended-path--release-plan]

### Previous Story Insights (Stories 5.1 and 5.2, Done)

- **No overlap with the write path.** 5.1 and 5.2 changed `internal/database` and `insertWithRetry`. This story changes `internal/config`, adds a file to `internal/mqtt`, and touches only the handler and `config_loaded` in `cmd/mqtt2bdd/main.go`. `insertWithRetry`, `dbWriterLoop` and `healthCheckLoop` are not changed. [Source: docs/stories/5.2.story.md → File Locations]
- **Subprocess test helpers are ready.** `integration_helpers_test.go` provides `startTestApp(t, env)`, `waitForEvent`, `hasEvent`, `eventLines`, `allLines`, `terminate`, `waitForExit` (fails on a non-zero exit), `publishTestMessage(t, topic, payload)` (QoS 0, not retained), `openTestDBPool`, `waitForRow`. `write_rejection_integration_test.go` defines `logCaptureTimeout` (5 s); `payload_repair_integration_test.go` defines `waitForEventCount(t, app, event, want)`, which fails if the count is exceeded. All are in `package main` behind the same build tag, so they are reusable as-is; do not move or rename them. [Source: cmd/mqtt2bdd/integration_helpers_test.go; cmd/mqtt2bdd/payload_repair_integration_test.go]
- **Log assertions must poll.** Stdout is drained by a separate goroutine, so a line can reach the capture buffer after the row it relates to appears. [Source: docs/stories/5.1.story.md → Task 7]
- **White-box test files are an accepted precedent** beside black-box ones in the same directory (`internal/database/queries_test.go`, `sanitize_test.go`). [Source: docs/stories/5.2.story.md → Task 3]
- **Negative control.** Both previous stories disabled the fix and watched the integration test fail; QA and the PO valued it. Task 7 asks for the same. [Source: docs/stories/5.2.story.md → Debug Log References]
- **Tooling note from 5.2's Dev.** Editor tooling decoded some escape sequences in text it was given (a real NUL landed in a Go comment). This story needs a NUL only in a test row: write it as `\x00` in an interpreted string, and keep the Task 7 byte check. [Source: docs/stories/5.2.story.md → Completion Notes List]

### QA Carry-Forward Review

Per the standing rule, all gate files were read before drafting; gate 5.2's register is the most recent and supersedes the earlier ones.

- **Folded into this story:** gate 5.2 `recommendations.future` item 3 — "message_excluded is the last Epic 5 event not yet implemented; update the status sentence in logging-standards.md when it lands" (Task 6).
- **Not folded, unrelated to topic exclusion:** gate 5.2's two optional items (single `utf8.DecodeRune` in `nextNonASCII`, a committed `FuzzSanitizePayload` target) concern `internal/database/sanitize.go`; the PO left them as a possible small follow-up. TEST-002 (Dev, low, 4.2 audit command), REL-001 (gate 3.3, `mqtt_status` semantics) and the broker-authentication accepted risk are untouched.
- **Monitor items of gate 5.2** (backslash assumption, `payload_sanitized` bursts during an outage) are not affected: this story does not touch the write path.
[Source: docs/qa/gates/5.2-repair-payload-content-postgresql-refuses.yml]

### Existing Code This Story Touches

- `internal/config/config.go` — `Config` struct (lines 16-34), `LoadConfig` (lines 38-107); optional variables are read in the block starting at line 86. The package imports only `fmt`, `os`, `strconv` and has no logger.
- `internal/config/config_test.go` — black-box (`package config_test`), one big table; comparison `*got != *tt.want` at line 235, which stops compiling once `Config` holds a slice (Task 4).
- `internal/mqtt/client.go` — imports `internal/config` (line 14). **This is why validation cannot live in `internal/mqtt`:** `config` importing `mqtt` would create an import cycle (C1). The Paho callback logs DEBUG `message_received` (with the payload body, level-guarded) and then calls `handler(msg.Topic(), msg.Payload())` (lines 125-140).
- `cmd/mqtt2bdd/main.go` — `config_loaded` at line 225; `mqttMessageHandler` at lines 286-300 (non-blocking send, then a blocking send that gives up on `done`); `Subscribe("#", …)` at line 301; `config_load_failed` at lines 212-216, emitted by the bootstrap logger (`component=main`, `operation=load_config`).
- Paho calls the handler sequentially for a subscription by default (order matters), which is why the integration test's messages are handled in publication order; the test still polls rather than relying on it.

### Matching Algorithm

`MatchTopic(filter, topic string) bool`, guidance for an allocation-free implementation (the behaviour, not this exact code, is the contract):

```
if topic starts with "$" and filter starts with "+" or "#":  return false      // §4.7.2
loop:
    fLevel, fRest, fMore := strings.Cut(filter, "/")
    if fLevel == "#":                return true        // this level and everything below, including nothing
    tLevel, tRest, tMore := strings.Cut(topic, "/")
    if fLevel != "+" && fLevel != tLevel:  return false  // byte comparison: case-sensitive, empty level significant
    if !fMore && !tMore:             return true         // both exhausted together
    if !fMore:                       return false        // topic has more levels than the filter
    if !tMore:                       return fRest == "#" // "a/#" matches "a"; "a/+" does not match "a"
    filter, topic = fRest, tRest
```

Walk-throughs, all in the Test Matrix: `sport/+` vs `sport` → false (last line); `sport/+` vs `sport/` → true (the empty level matches `+`); `+` vs `/finance` → false (topic has two levels); `/+` vs `/finance` → true; `a/#` vs `a/` → true. A topic name never contains `+` or `#` (a compliant broker never delivers one), so the topic side needs no wildcard handling. [Source: MQTT 3.1.1 (OASIS Standard, 2014) §4.7.1 Topic wildcards, §4.7.2 Topics beginning with $, §4.7.3 Topic semantic and usage]

### Log Contract

| Entry | Component | Level | Fields | `operation` |
|-------|-----------|-------|--------|-------------|
| `message_excluded` (new) | `main` | DEBUG | `topic`, `filter` (the `MQTT_EXCLUDE_TOPICS` entry that matched, first in configured order). **Never the payload body** | none — it reports a state |
| `config_loaded` (existing) | `main` | INFO | existing fields, plus `exclude_topics` (the parsed list joined with `,`; `""` when nothing is excluded) | none |
| `config_load_failed` (existing, unchanged) | `main` | ERROR | `operation=load_config`, `error` — the error now names `MQTT_EXCLUDE_TOPICS` and the offending filter when a filter is invalid | `load_config` |

`msg` is `"message excluded"`. Values go in fields, never in `msg`. Illustrative output (verified rendering of `slog.TextHandler` for these values in `go-dev`):

```
level=INFO msg="configuration loaded" component=main event=config_loaded log_level=DEBUG effective_log_level=DEBUG buffer_size=1000 exclude_topics=zigbee2mqtt/bridge/#
level=INFO msg="configuration loaded" component=main event=config_loaded log_level=INFO effective_log_level=INFO buffer_size=1000 exclude_topics=""
level=DEBUG msg="MQTT message received" component=mqtt broker=tcp://… event=message_received topic=zigbee2mqtt/bridge/state payload_size=19 payload="{\"state\":\"online\"}"
level=DEBUG msg="message excluded" component=main event=message_excluded topic=zigbee2mqtt/bridge/state filter=zigbee2mqtt/bridge/#
```

At INFO, an excluded message produces no line at all (AC5). [Source: docs/architecture/logging-standards.md#event-vocabulary; docs/architecture/logging-standards.md#operation-vocabulary; docs/architecture/logging-standards.md#domain-fields]

### SM Clarifications

Where the ACs leave a detail open, these answers apply.

- **C1 — Placement: validation in `internal/config`, matching in `internal/mqtt`, the decision in `main`.** AC2 makes an invalid filter a configuration error (`config_load_failed`), and `components.md` gives validation to the Configuration Manager, so `LoadConfig` must validate. The check cannot be delegated to `internal/mqtt`, because `internal/mqtt` already imports `internal/config` and the reverse import would be a cycle. Matching is MQTT protocol semantics used on received messages, so it belongs with the MQTT client; `main` already imports `internal/mqtt`. `logging-standards.md` lists `message_excluded` under `main`, which places the drop in `main`'s handler (AC4: "in the reception handler"). No new package: `source-tree.md` and `components.md` name four internal packages, and a fifth for two functions is not justified. The two functions share the grammar of §4.7 but check different things (well-formedness vs matching), so the split duplicates no rule.
- **C2 — "Excludes nothing" is a `nil` slice.** Unset, empty, blank-only and separator-only values all give `ExcludeTopics == nil`. One representation keeps `reflect.DeepEqual` in tests unambiguous (`nil` and `[]string{}` are not deep-equal) and lets the existing `config_test.go` rows stay as they are.
- **C3 — `exclude_topics` is rendered as the comma-joined list.** Passing the slice to slog renders `exclude_topics="[a/# b c]"`, where a space inside a filter (legal in MQTT) cannot be told apart from the separator (verified in `go-dev`). A filter can never contain a comma (it is the input separator), so `strings.Join(…, ",")` is unambiguous, mirrors the variable's own syntax, and shows the list after trimming and empty-entry removal, which is what "the parsed list" means. An empty list renders as `exclude_topics=""`.
- **C4 — Order and duplicates.** Filters are kept in configured order, duplicates included; `excludingFilter` reports the first match. Deduplicating would add code for no behavioural difference, and "first in the order you wrote them" is the simplest rule to document.
- **C5 — `message_received` still precedes `message_excluded`.** The DEBUG `message_received` entry (with the payload body) is logged inside `internal/mqtt` before the handler runs, so at DEBUG an excluded message still appears there, followed by `message_excluded`. Moving the check into `internal/mqtt` would contradict C1 and `logging-standards.md`. At INFO neither line exists. Operators who exclude a large topic to keep DEBUG readable should know this; the README Troubleshooting entry is enough.
- **C6 — The check is the handler's first statement.** An excluded message must never be timestamped, must never wait on a full buffer, and must never produce `buffer_full` or `message_dropped`. Checking before the send guarantees all three.
- **C7 — Validation covers exactly AC2's three rules.** Not checked: UTF-8 well-formedness and the 65,535-byte length limit of §1.5.3 — the AC lists its rules, and a filter violating them simply never matches a received topic. A filter that matches everything (`#`) is valid and accepted without a warning; `config_loaded` shows it. A `$SYS/#` filter is valid too, but never matches anything, because a compliant broker does not deliver `$` topics to a `#` subscription (§4.7.2).
- **C8 — The `$` rule is unit-tested only.** For the reason in C7, the application never receives a `$` topic, so no integration test can exercise it. `MatchTopic` still implements it, because AC3 requires MQTT semantics and the function must not depend on the subscription it happens to be used with.
- **C9 — The NUL rule is tested white-box.** `os.Setenv` refuses a value containing a NUL byte (`setenv: invalid argument`, verified in `go-dev`), so `LoadConfig` can never see one through the real environment. `validateTopicFilter` still checks it (AC2) and `topics_test.go` covers it directly.
- **C10 — Trimming uses `strings.TrimSpace`.** A filter with meaningful leading or trailing spaces therefore cannot be expressed; inner spaces are kept. This is what AC1 asks for and is documented in the README row.
- **C11 — `dev/.env.example` gets the variable too**, empty. AC8 does not list it, but the README presents `dev/.env.example` and `.env.prod.example` as the two ready-to-copy templates for the application's variables, and the dev stack reads `.env` through `env_file`. (`dev/.env.example` does not list `BUFFER_SIZE` today; adding it is out of scope.) No change to `dev/docker-compose.yml` is needed (`env_file` passes every variable).
- **C12 — `.env.prod.example` ships the variable empty**, with `zigbee2mqtt/bridge/#` as a commented example: a copied template must keep v1.0.0 behaviour (Epic 5 compatibility). The README table carries the example value AC8 requires.
- **C13 — No counter and no health field for exclusions.** No AC asks for one; exclusions are configuration, not loss (`core-workflows.md` → Error Handling Summary: "Data Loss? No (excluded by configuration)").

### File Locations

| File | Change |
|------|--------|
| `internal/config/config.go` | `ExcludeTopics` field; call to `parseTopicFilters` in `LoadConfig` |
| `internal/config/topics.go` | **new** — `parseTopicFilters`, `validateTopicFilter` (unexported) |
| `internal/config/topics_test.go` | **new**, white-box — `TestValidateTopicFilter` |
| `internal/config/config_test.go` | `reflect.DeepEqual`, `MQTT_EXCLUDE_TOPICS` reset, `TestLoadConfig_ExcludeTopics` |
| `internal/mqtt/topic.go` | **new** — `MatchTopic` (exported) |
| `internal/mqtt/topic_test.go` | **new**, black-box — `TestMatchTopic` |
| `cmd/mqtt2bdd/main.go` | `excludingFilter`; first statement of `mqttMessageHandler`; `exclude_topics` on `config_loaded`; `strings` import |
| `cmd/mqtt2bdd/main_test.go` | `TestExcludingFilter` |
| `cmd/mqtt2bdd/topic_exclusion_integration_test.go` | **new** — `TestTopicExclusion`, `TestTopicExclusion_InvalidFilter` |
| `docker-compose.prod.yml`, `.env.prod.example`, `dev/.env.example` | variable passed through / documented |
| `README.md` | Features, Configuration, Troubleshooting, Project Structure, Current test shape |
| `docs/architecture/components.md`, `docs/architecture/logging-standards.md` | reconciliation (Task 6) |

`internal/mqtt/client.go`, `internal/mqtt/types.go`, `internal/database`, `internal/logger`, `test/`, `go.mod` and `go.sum` are **not** changed. Module path `github.com/hagnerk/mqtt2bdd`; imports in three blocks (stdlib, external, internal). File order inside each new file: package comment only on the package's first file (both packages already have one, so the new files have none), imports, constants, then functions. [Source: docs/architecture/coding-standards.md#import-organization; docs/architecture/coding-standards.md#file-organization; go.mod]

### Project Structure Notes

- No new package (C1). `topics.go` sits beside `config.go`, and `topic.go` beside `client.go` and `types.go`, following the existing one-concern-per-file split. [Source: docs/architecture/components.md#package-organization-internal-architecture]
- `docs/architecture/source-tree.md` and the Package Organization tree in `components.md` do not list per-file contents beyond the originals and are not updated for the new files (same decision as Story 5.2). The README's Project Structure lists only `cmd/mqtt2bdd/` files individually, hence only the new integration test file is added there.
- `components.md` → Concurrency Architecture and the synchronization snippet are outdated on unrelated points (`close(msgChan)`); out of scope.

### MQTT ClientID Constraint

`internal/mqtt/client.go` hard-codes the client ID `mqtt2bdd`. Two live instances on the same broker disconnect each other, so the `cmd/mqtt2bdd` integration tests run sequentially, never with `t.Parallel()`, and each test terminates its subprocess before returning. `TestTopicExclusion_InvalidFilter` exits before connecting, so it cannot collide. `TestMain` truncates `sensor_metrics` once before the package's tests; topics are made run-unique with a `UnixNano` suffix. [Source: docs/stories/3.3.story.md → The MQTT ClientID Constraint; cmd/mqtt2bdd/integration_helpers_test.go]

### Technical Constraints

- Go 1.23 (`go.mod`); stdlib only (`strings`, `fmt`, `reflect` in tests). No new dependency. [Source: go.mod; docs/prd.md → Epic 5 → Compatibility requirements]
- `staticcheck` is non-negotiable; godoc on the exported `MatchTopic` and the new `Config` field comment; a *why* comment on the unexported functions; no `panic()`; no magic strings scattered — name the separator and wildcards as constants where they are used more than once (for example `levelSeparator = "/"`, `multiLevelWildcard = "#"`, `singleLevelWildcard = "+"`). [Source: docs/architecture/coding-standards.md]
- Configuration only through environment variables, read only in `internal/config` (the README states `LoadConfig` is the only place `os.Getenv` is called for application settings). [Source: docs/architecture/coding-standards.md#constants-and-configuration; README.md → Project Structure]
- Performance: `MatchTopic` runs once per configured filter on every received message; it must not allocate (Task 2). Validation runs once at startup and may allocate. [Source: docs/prd.md → NFR7]
- Security: never log the payload body on `message_excluded`; the error for an invalid filter contains only the filter, which is not a secret. [Source: docs/architecture/logging-standards.md; docs/architecture/security.md#input-validation]

### Test Matrix

**Validation** (`TestValidateTopicFilter`; ✓ valid, ✗ invalid). Examples marked §4.7 are the specification's own.

| Filter | Result | Why |
|--------|--------|-----|
| `#` | ✓ | §4.7.1.2 |
| `+` | ✓ | §4.7.1.3 |
| `sport/tennis/#` | ✓ | §4.7.1.2 |
| `sport/+/player1` | ✓ | §4.7.1.3 |
| `+/tennis/#` | ✓ | §4.7.1.3 |
| `+/+`, `/+` | ✓ | §4.7.1.3 |
| `+/#` | ✓ | both wildcards as whole levels |
| `/`, `a//b`, `a/` | ✓ | empty levels are legal (§4.7.3) |
| `$SYS/#`, `$SYS/monitor/+` | ✓ | §4.7.2 |
| `zigbee2mqtt/bridge/#` | ✓ | the story's own use case |
| `living room/+` | ✓ | spaces are legal (§4.7.3) |
| `sport/tennis#` | ✗ | §4.7.1.2 — `#` not a whole level |
| `sport/tennis/#/ranking` | ✗ | §4.7.1.2 — `#` not last |
| `#/`, `##`, `a/b#` | ✗ | `#` not a whole, final level |
| `sport+` | ✗ | §4.7.1.3 — `+` not a whole level |
| `+a/b`, `a/+b`, `a/b+`, `++` | ✗ | `+` not a whole level |
| `"a\x00b"`, `"\x00"`, `"a/#\x00"` | ✗ | NUL character (C9) |

**Matching** (`TestMatchTopic`; filter, topic → result).

| Filter | Topic | Want | Rule |
|--------|-------|------|------|
| `sport/tennis/player1/#` | `sport/tennis/player1` | true | §4.7.1.2, parent level |
| `sport/tennis/player1/#` | `sport/tennis/player1/ranking` | true | §4.7.1.2 |
| `sport/tennis/player1/#` | `sport/tennis/player1/score/wimbledon` | true | §4.7.1.2, several child levels |
| `sport/#` | `sport` | true | §4.7.1.2 |
| `#` | `sport/tennis/player1` | true | matches every non-`$` topic |
| `#` | `/` | true | |
| `a/#` | `a/` | true | the empty child level is a level |
| `a/#` | `ab` | false | levels, not string prefixes |
| `a/#` | `b/a` | false | |
| `sport/tennis/+` | `sport/tennis/player1` | true | §4.7.1.3 |
| `sport/tennis/+` | `sport/tennis/player2` | true | §4.7.1.3 |
| `sport/tennis/+` | `sport/tennis/player1/ranking` | false | §4.7.1.3, exactly one level |
| `sport/+` | `sport` | false | §4.7.1.3 |
| `sport/+` | `sport/` | true | §4.7.1.3, the empty level matches `+` |
| `+/+` | `/finance` | true | §4.7.1.3 |
| `/+` | `/finance` | true | §4.7.1.3 |
| `+` | `/finance` | false | §4.7.1.3 |
| `+` | `finance` | true | |
| `sport/+/player1` | `sport/tennis/player1` | true | `+` in the middle |
| `a/b` | `a/b` | true | exact |
| `a/b` | `a/b/` | false | a trailing `/` makes a distinct topic (§4.7.3) |
| `a//b` | `a//b` | true | empty levels are significant |
| `a/b` | `a//b` | false | empty levels are significant |
| `a/+/b` | `a//b` | true | `+` matches the empty level |
| `Sport/#` | `sport/tennis` | false | case-sensitive (§4.7.3) |
| `ACCOUNTS` | `Accounts` | false | §4.7.3 |
| `#` | `$SYS/broker/uptime` | false | §4.7.2 |
| `+/monitor/Clients` | `$SYS/monitor/Clients` | false | §4.7.2 |
| `$SYS/#` | `$SYS/monitor/Clients` | true | §4.7.2 |
| `$SYS/monitor/+` | `$SYS/monitor/Clients` | true | §4.7.2 |
| `a/$SYS` | `a/$SYS` | true | the `$` rule concerns the first character of the topic only |
| `+/b` | `$a/b` | false | any topic starting with `$` |
| `zigbee2mqtt/bridge/#` | `zigbee2mqtt/bridge/definitions` | true | the story's use case |
| `zigbee2mqtt/bridge/#` | `zigbee2mqtt/living_room/temperature` | false | |
| `zigbee2mqtt/bridge/#` | `zigbee2mqtt/bridge` | true | parent level |

**Parsing** (`TestLoadConfig_ExcludeTopics`; value of `MQTT_EXCLUDE_TOPICS` → `ExcludeTopics` or error).

| Value | Result |
|-------|--------|
| unset (only `t.Setenv(…, "")` via the reset list) | `nil` |
| `""` | `nil` |
| `"   "` | `nil` |
| `", ,,"` | `nil` |
| `"zigbee2mqtt/bridge/#"` | `[]string{"zigbee2mqtt/bridge/#"}` |
| `" a/# , b/+ ,,c "` | `[]string{"a/#", "b/+", "c"}` |
| `"a/#,a/#"` | `[]string{"a/#", "a/#"}` (C4) |
| `" living room/+ "` | `[]string{"living room/+"}` (C10) |
| `"sport/tennis/#/ranking"` | error containing `MQTT_EXCLUDE_TOPICS` and `"sport/tennis/#/ranking"` |
| `"ok/#, sport+"` | error containing `"sport+"` — the first filter is valid, the second aborts |

### Testing

- **Frameworks:** stdlib `testing` only; no third-party assertion libraries. Table-driven tests with `t.Run`; `t.Setenv` for environment variables. [Source: docs/architecture/test-strategy-and-standards.md#unit-tests]
- **Locations:** unit tests beside the code — `internal/config/config_test.go` (black-box), `internal/config/topics_test.go` (white-box, C9), `internal/mqtt/topic_test.go` (black-box), `cmd/mqtt2bdd/main_test.go`; integration tests behind `//go:build integration` in `cmd/mqtt2bdd/topic_exclusion_integration_test.go`. [Source: docs/architecture/test-strategy-and-standards.md#test-organization]
- **Package convention:** black-box for exported functions (`MatchTopic`, `LoadConfig`), white-box only where the function under test is unexported (`validateTopicFilter`, `excludingFilter`). [Source: docs/architecture/test-strategy-and-standards.md#test-organization]
- **Mock at system boundaries only:** parsing and matching are pure and tested in memory; the drop is tested end-to-end against the real test broker and database. [Source: docs/architecture/test-strategy-and-standards.md#philosophy]
- **No log-text assertions in unit tests**; the subprocess integration test asserts the log contract on captured stdout by `event=` and field matches.
- **Runner:** unit tests via `docker compose exec go-dev go test ./...` from `dev/`; integration via `./test/run-integration-tests.sh` from the repository root (throwaway `golang:1.23-alpine` container on `mqtt2bdd-test-network`, `go test -tags=integration -p 1 -v ./...`). Nothing runs on the host. [Source: test/run-integration-tests.sh; README.md#testing]
- **Discriminating assertions:** without the exclusion, rows appear for the two excluded topics and no `message_excluded` line is logged (negative control, Task 7). A prefix-based matcher would wrongly exclude `excluded-<suffix>-sibling`. A matcher that forgets the parent-level rule would store `excludedRoot`.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-09-17 | 0.1 | Initial draft from Epic 5, the 2026-09-17 sprint change proposal and gate 5.2 | SM (Bob) |
| 2026-09-17 | 0.2 | PO validation: ACs checked verbatim against `docs/prd.md:941-950`; code line references, integration helpers, architecture docs, configuration templates, gate 5.2 and the MQTT 3.1.1 §4.7 examples verified; `git ls-files` confirmed working inside `go-dev` for the NUL check. Clarified that `TestTopicExclusion_InvalidFilter` polls for `config_load_failed` after exit, that `TestLoadConfig_ExcludeTopics` shares the hoisted `allRequired` and reset list, and corrected C11's description of `dev/.env.example`. Status Approved | PO (Sarah) |
| 2026-09-17 | 0.3 | Implementation: `MQTT_EXCLUDE_TOPICS` parsing and validation, `MatchTopic`, exclusion in the reception handler, `exclude_topics` on `config_loaded`, unit and integration tests, templates and documentation. Status Ready for Review | Dev (James) |

## Dev Agent Record

### Agent Model Used

Claude Opus 5 (`claude-opus-5[1m]`)

### Debug Log References

All commands run from `dev/` through `docker compose exec go-dev`, except `./test/run-integration-tests.sh` (repository root).

- `gofmt -l .` → empty output.
- `go vet ./...`, `go vet -tags=integration ./...` → clean.
- `staticcheck ./...`, `staticcheck -tags=integration ./...` → clean.
- `go test -count=1 ./...` → `ok` for `cmd/mqtt2bdd`, `internal/config`, `internal/database`, `internal/logger`, `internal/mqtt`.
- Coverage (`go test -coverprofile` + `go tool cover -func`): `topics.go:24 parseTopicFilters 100.0%`, `topics.go:43 validateTopicFilter 100.0%`, `topic.go:22 MatchTopic 100.0%`; package totals `internal/config` 98.2 %, `internal/mqtt` 73.1 % (the remainder is pre-existing `client.go` code).
- Allocation check (throwaway test, deleted after the run): `testing.AllocsPerRun(1000, …MatchTopic("zigbee2mqtt/+/#", "zigbee2mqtt/bridge/definitions/x"))` → `allocs=0`.
- NUL byte check over every created or modified file → prints nothing.
- `git diff --stat -- go.mod go.sum` → empty.
- `./test/run-integration-tests.sh` → exit 0; `--- PASS: TestTopicExclusion`, `--- PASS: TestTopicExclusion_InvalidFilter`, `--- PASS: TestPayloadRepair`, `--- PASS: TestWriteRejection`, and every other test of the suite.
- Captured lines (run-unique suffix `1789666834623512382`):
  - `level=INFO msg="configuration loaded" component=main event=config_loaded log_level=DEBUG effective_log_level=DEBUG buffer_size=1000 exclude_topics=integration/exclusion/excluded-1789666834623512382/#,integration/exclusion/unused-1789666834623512382/+`
  - `level=DEBUG msg="message excluded" component=main event=message_excluded topic=integration/exclusion/excluded-1789666834623512382/bridge/state filter=integration/exclusion/excluded-1789666834623512382/#`
  - `level=DEBUG msg="message excluded" component=main event=message_excluded topic=integration/exclusion/excluded-1789666834623512382 filter=integration/exclusion/excluded-1789666834623512382/#`
  - `level=ERROR msg="configuration error" component=main event=config_load_failed operation=load_config error="MQTT_EXCLUDE_TOPICS: invalid topic filter \"sport/tennis/#/ranking\": '#' must be the whole last level"`
  - Tests that leave the variable unset log `exclude_topics=""` (5 occurrences in the run).
- Negative control: `excludingFilter` temporarily forced to return `("", false)`. `./test/run-integration-tests.sh` → exit 1: `TestTopicExclusion` failed with `topic_exclusion_integration_test.go:64: expected 2 message_excluded entries, got 0.`, and the application logged `event=write_success` for both `…/excluded-1789666964716167883/bridge/state` and `…/excluded-1789666964716167883` (rows stored for the two excluded topics). `TestExcludingFilter` failed too (`one_filter_matches`, `first_matching_filter_wins`). Code restored from a backup copy, suite re-run: exit 0, all green.

### Completion Notes List

- Implemented as specified; no deviation from the tasks or SM Clarifications C1–C13.
- `internal/config/topics.go` and `internal/mqtt/topic.go` each declare their own small set of constants (`/`, `#`, `+`); they are deliberately not shared, since `config` cannot import `mqtt` (C1) and a shared package for three constants is not justified.
- `MatchTopic` uses a `switch` for the three end-of-level cases of the Matching Algorithm; behaviour matches the Dev Notes pseudo-code row for row.
- In the integration test, the `config_loaded` line is read with `waitForEvent` after `startup_complete`, and the test-local helper `countTopicLines` counts exact `topic=` matches. The `message_excluded` assertions use `waitForEventCount`, which also fails if more than two exclusion lines appear (the stored topics would then have been excluded).
- `TestLoadConfig_ExcludeTopics` uses a `set` flag so the "unset" row keeps only the reset value; failure messages print slices with `%#v` so `nil` and `[]string{}` are distinguishable.
- README Overview and first Features bullet left unchanged: they remain true (no configuration is required).
- `docs/architecture/core-workflows.md` "Excluded topic" row checked against the code: correct, unchanged.
- v1.1.0 not tagged (maintainer's action once Done).

### File List

- `internal/config/config.go` (modified)
- `internal/config/topics.go` (new)
- `internal/config/topics_test.go` (new)
- `internal/config/config_test.go` (modified)
- `internal/mqtt/topic.go` (new)
- `internal/mqtt/topic_test.go` (new)
- `cmd/mqtt2bdd/main.go` (modified)
- `cmd/mqtt2bdd/main_test.go` (modified)
- `cmd/mqtt2bdd/topic_exclusion_integration_test.go` (new)
- `docker-compose.prod.yml` (modified)
- `.env.prod.example` (modified)
- `dev/.env.example` (modified)
- `README.md` (modified)
- `docs/architecture/components.md` (modified)
- `docs/architecture/logging-standards.md` (modified)
- `docs/stories/5.3.story.md` (Dev Agent Record, task checkboxes, status, change log)

## QA Results

### Review Date: 2026-09-18

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Excellent. This is a small, pure, well-bounded change, and the implementation matches the story row for row — including the thirteen SM Clarifications, which is unusual to see hold end to end.

Three things stand out. First, the placement argument of C1 is respected exactly: `internal/config` validates (it owns configuration errors, and `internal/mqtt` already imports it, so the reverse would be a cycle), `internal/mqtt` matches (it is protocol semantics), and `main` decides (`logging-standards.md` files `message_excluded` under `main`). No new package was invented for two functions. Second, `MatchTopic` is genuinely allocation-free: it walks both strings with `strings.Cut` and never splits, and the `switch` over the three end-of-level cases reads exactly like the §4.7 rules it implements. Third, the exclusion check is the handler's very first statement, which is what makes AC4 true by construction rather than by a counter — an excluded message is never timestamped, never queued, never able to produce `buffer_full` or `message_dropped`.

The one representation decision that pays off repeatedly is C2: "excludes nothing" is always `nil`, never `[]string{}`. It keeps `strings.Join` rendering `""`, keeps `reflect.DeepEqual` unambiguous in the tests, lets the pre-existing `config_test.go` rows stay untouched, and makes `excludingFilter`'s loop a no-op on the v1.0.0 path.

Comments explain *why* throughout (why `nil`, why duplicates are kept, why the check comes first, which §4.7 rules are deliberately not validated and why that is harmless). The godoc on `MatchTopic` states both its precondition and its allocation contract, which is what a caller actually needs.

### Refactoring Performed

- **File**: `README.md`
  - **Change**: In the new Features bullet, "excluded messages are still delivered by the broker and crossing the network" became "…and still cross the network".
  - **Why**: The sentence mixed a passive and a participle and did not parse. The README is the entry point of a public, portfolio-facing repository.
  - **How**: One clause rewritten; the bullet is otherwise untouched and the surrounding paragraph was not reflowed.

No Go code was changed during this review.

### Compliance Check

- Coding Standards: ✓ — import blocks in the prescribed order, godoc on the exported `MatchTopic`, a comment on the new `Config` field, why-comments on the unexported functions, named constants for `/`, `#`, `+` and the separator, no `panic()`, `%w` for error wrapping as the rest of `LoadConfig` does. `gofmt -l .` empty, `go vet` and `staticcheck` clean with and without `-tags=integration`.
- Project Structure: ✓ — `topics.go` beside `config.go`, `topic.go` beside `client.go`, no new package, `os.Getenv` still only in `internal/config`. `internal/mqtt/client.go`, `internal/database`, `internal/logger`, `test/`, `go.mod` and `go.sum` are untouched, as the File Locations table promised.
- Testing Strategy: ✓ — stdlib `testing` only, table-driven with `t.Run`, black-box except where the function under test is unexported (`validateTopicFilter`, `excludingFilter`), no log assertions in unit tests, the log contract asserted only in the subprocess integration test, no `t.Parallel()` in `cmd/mqtt2bdd`.
- All ACs Met: ✓ — see the trace below.

### Requirements Traceability

| AC | Given / When / Then | Covered by |
|----|---------------------|------------|
| 1 | **Given** `MQTT_EXCLUDE_TOPICS` in any of its shapes, **when** `LoadConfig` runs, **then** entries are trimmed, empty ones dropped, and unset/empty/blank/separator-only all give `nil` | `TestLoadConfig_ExcludeTopics` (10 rows) |
| 2 | **Given** a filter breaking a §4.7.1 rule or holding a NUL, **when** `LoadConfig` runs, **then** it returns an error naming `MQTT_EXCLUDE_TOPICS` and the quoted filter, and `main` aborts with `config_load_failed` exit 1 | `TestValidateTopicFilter` (28 rows, the three NUL rows reachable only white-box), `TestLoadConfig_ExcludeTopics` error rows, `TestTopicExclusion_InvalidFilter` end-to-end |
| 3 | **Given** a valid filter and a topic, **when** `MatchTopic` runs, **then** `+` matches one level, `#` matches the parent and any children, empty levels count, comparison is case-sensitive, and a `$` topic resists a leading wildcard | `TestMatchTopic` (35 rows — every row of the Matching matrix, including the specification's own §4.7 examples) |
| 4 | **Given** a message on an excluded topic, **when** the handler runs, **then** it returns before the `Message` is built and no row reaches `sensor_metrics`; `processed_last_interval` is unaffected | `TestExcludingFilter`, `TestTopicExclusion` (`COUNT(*) = 0`, checked only after both exclusion lines are observed) |
| 5 | **Given** `LOG_LEVEL=DEBUG`, **when** a message is excluded, **then** exactly one `message_excluded` line carries `topic` and the matching `filter`, at DEBUG, with no `operation` and no payload body; at INFO nothing is logged | `TestTopicExclusion` (per-line assertions + `waitForEventCount`, which also fails on a surplus line) |
| 6 | **Given** a parsed list, **when** `config_loaded` is emitted, **then** `exclude_topics` is the comma-joined list, `""` when empty | `TestTopicExclusion` (anchored regex on the captured line); the `""` rendering observed in the five other integration runs |
| 7 | **Given** exclusion is configured, **when** the client subscribes, **then** it is still `#` and the README says why | `internal/mqtt/client.go` unchanged (empty diff); README Features bullet |
| 8 | Documentation and templates | Read in `README.md` (table, Features, Troubleshooting, Project Structure, test shape), `.env.prod.example`, `dev/.env.example`, `docker-compose.prod.yml`, `components.md`, `logging-standards.md` |
| 9 | Unit + integration coverage | The seven test functions above |
| 10 | Verification as Story 5.1 AC12 | QA rerun below |

No gaps, no partials.

### Test Architecture Assessment

The test levels are chosen correctly: parsing and matching are pure, so they are exercised in memory with the specification's own examples; the drop is only meaningful end to end, so it is proved against the real broker and database. The discriminating rows are the ones that matter — `excluded-<suffix>-sibling` shares the excluded root as a *string* prefix but not as a level, so a prefix-based matcher fails the test; `excludedRoot` itself catches a matcher that forgets the §4.7.1.2 parent-level rule. `waitForEventCount` failing on a *surplus* line is what turns "the stored topics were not excluded" into an assertion rather than an assumption. Ordering the SQL check after both `message_excluded` lines removes the last "maybe the row is just late" reading of a zero count.

The Dev's negative control (forcing `excludingFilter` to return `("", false)`, watching `TestTopicExclusion` fail with `got 0` exclusion lines and `write_success` for both excluded topics, then restoring) is the right way to show the suite has teeth; it is recorded in the Debug Log References.

Testability is good on all three axes: inputs are controllable (pure functions, environment variables, a run-unique topic suffix), outputs observable (structured log lines keyed by `event=`, plus the table), and failures debuggable (`%q` and `%#v` in the failure messages keep empty levels, inner spaces and `nil`-vs-empty-slice visible).

### Non-Functional Requirements

- **Security — PASS.** The payload body never appears on `message_excluded`: asserted line by line, and structurally guaranteed since the handler returns before the `mqtt.Message` exists. The `config_load_failed` error carries only the operator's own filter. No new dependency, no new surface.
- **Performance — PASS.** Independently verified, not merely accepted: a throwaway `AllocsPerRun(1000)` probe inside `go-dev` over three filter shapes gives **0 allocations**. The v1.0.0 path costs one range over a `nil` slice per message.
- **Reliability — PASS.** Fail-fast on a bad filter with a named cause; the check's position in the handler makes the "never blocks, never counted as loss" property structural. No schema change; `git revert` of this story alone restores v1.0.0.
- **Maintainability — PASS.** Small pure functions, why-comments citing the specification, and the architecture docs reconciled rather than rewritten. `core-workflows.md`'s "Excluded topic" row was checked against the code and is correct.

### Improvements Checklist

- [x] Corrected the README Features bullet (DOC-010, `README.md`)
- [x] Independently verified the zero-allocation claim for `MatchTopic` (throwaway probe, not committed)
- [x] Verified `MatchTopic` never panics on an invalid filter — 11 invalid filters × 8 topics, including empty topics, `$` topics and a NUL
- [x] Re-ran the full verification chain: gofmt, vet, staticcheck (both tag sets), unit tests, coverage, integration suite
- [ ] Optional: `validateTopicFilter`'s two constant-string errors could be `errors.New` rather than `fmt.Errorf` — no verb is used. Cosmetic only; `staticcheck` is clean either way
- [ ] Optional, out of this story's scope: `dev/.env.example` still omits `BUFFER_SIZE`, so it does not quite match the README's "complete list"
- [ ] Still open from gate 5.2, unrelated: the single `utf8.DecodeRune` in `nextNonASCII`, and a committed `FuzzSanitizePayload` target

### Security Review

No concerns. The one place operator-supplied text reaches a log — the invalid-filter error — carries a topic filter, which is not secret, and is `%q`-quoted so control characters stay visible rather than corrupting the line. The `$`-topic rule of §4.7.2 is implemented even though a compliant broker never delivers such a topic to a `#` subscription (C8), which is the right call: `MatchTopic` does not depend on the subscription it happens to be used with.

### Performance Considerations

None outstanding. `MatchTopic` allocates nothing (QA-verified) and is O(levels); `excludingFilter` is O(filters) per message with an empty body on the default path. Validation runs once at startup, where `strings.Split` is explicitly acceptable and the code says so.

### Files Modified During Review

- `README.md` — DOC-010 correction (one clause). The File List already lists it, so no update is needed from Dev.

### Gate Status

Gate: PASS → `docs/qa/gates/5.3-exclude-topics-from-persistence.yml`
Quality score: 100

Three items are recorded as *monitor*, none blocking: a filter containing a comma cannot be expressed through a comma-separated variable (legal in MQTT §4.7.3, never seen in this project's scope); at DEBUG an excluded message still produces the `message_received` line with its body, from `internal/mqtt`, so excluding a chatty topic does not make DEBUG quieter (C5, documented in Troubleshooting); and the §4.7 grammar now lives in two files, since `internal/config` cannot import `internal/mqtt` — change one, check the other.

Gate 5.2's carry-forward on `logging-standards.md` is closed by this story. TEST-002 (gate 4.2), REL-001 (gate 3.3) and the broker-authentication accepted risk stay open and untouched.

### Recommended Status

✓ **Ready for Done** — the two unchecked items above are cosmetic and out of scope. The story owner decides; tagging **v1.1.0** remains the maintainer's action once the status is Done.
