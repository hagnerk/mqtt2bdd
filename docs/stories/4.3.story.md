# Story 4.3: Automate Cross-Platform Binary Builds on Tag

## Status

Done — Rehearsal Deferred (AC12 open; owner: maintainer)

## Story

**As a** maintainer,
**I want** a tagged version to automatically produce downloadable binaries for four platforms,
**so that** publishing a release is one `git push --tags` and never a manual build.

## Acceptance Criteria

> **ACs 1–13 are copied verbatim from Epic 4 in `docs/prd.md:759-808`.** None is reworded.
>
> **AC14 is an SM-homed carry-forward finding** (TEST-001, open since gate `3.3`), not part of the
> original epic text. Story 4.2's PO acceptance asked the SM to consider homing it here, because
> AC2 wires `test/run-integration-tests.sh` into CI — which is the "next time the script is touched"
> trigger TEST-001 has been waiting on. Reasoning in Dev Notes → Carry-Forward Findings.
>
> **AC12 cannot be completed by the development agent.** An end-to-end rehearsal requires a GitHub
> repository, a pushed tag, and a workflow run on GitHub's runners — none of which exists, because
> Story 4.2's ACs 6–8 are deferred to the maintainer under PUB-001. AC12 is scoped exactly as Story
> 4.2 scoped its own deferred ACs: **not dropped, not reinterpreted into something a local run can
> satisfy**, delivered as far as it can be, with the remaining step recorded as a runnable handover
> in Dev Notes → Deferred: The Rehearsal Runbook. **The other twelve epic ACs are fully deliverable
> locally** — a workflow file is a file, and every claim it makes about build flags, archive
> contents, naming and checksums can be verified by reproducing them outside CI, which this story
> requires rather than assumes.

1. `.github/workflows/release.yml` exists, triggered only by pushed tags matching `v*.*.*`
   (including pre-release suffixes such as `v0.9.0-rc1`)
2. The workflow runs the project's existing quality gates in a first job that the build and
   publish jobs depend on, so no artefact is built and no release is created if any gate fails:
   - static analysis and unit tests: `gofmt -l .` (must report nothing), `go vet ./...`,
     `staticcheck ./...`, `go test ./...`
   - the integration suite: `./test/run-integration-tests.sh`, run from the repository root as
     documented. The script is already self-contained and CI-friendly from Story 3.3 — it brings
     the isolated `test/` stack up, waits for health, runs the `//go:build integration` suite,
     tears everything down unconditionally, and exits non-zero on any failure — so it needs no
     workflow-specific adaptation beyond a runner that provides Docker Compose
   These gates run on GitHub's runners, not on a contributor's machine: this is a publication
   gate, not a git hook. A failing gate leaves the pushed tag and commits in place and blocks
   only the release; recovering means fixing, deleting the tag locally and remotely, and
   re-tagging. The AC12 pre-release rehearsal exists so that this is never first discovered on
   `v1.0.0`
3. A build matrix produces one static binary per target: `linux/amd64`, `linux/arm64`,
   `linux/arm` (with `GOARM=7`, for 32-bit Raspberry Pi OS), and `darwin/arm64`
4. Every build uses `CGO_ENABLED=0` and `-ldflags="-s -w -X main.Version=<version>"`, where
   `<version>` is the tag with its leading `v` stripped (`v1.0.0` → `1.0.0`) — identical flags to
   the production `Dockerfile`, so a released binary matches a container-built one
5. Each target is packaged as a `.tar.gz` archive under a **canonical, version-bearing name**:
   `mqtt2bdd_<version>_linux_amd64.tar.gz`, `mqtt2bdd_<version>_linux_arm64.tar.gz`,
   `mqtt2bdd_<version>_linux_armv7.tar.gz`, `mqtt2bdd_<version>_darwin_arm64.tar.gz` — so that a
   downloaded file states on disk which version it holds
6. Each archive is **also** published under a version-free alias name
   (`mqtt2bdd_linux_amd64.tar.gz`, `mqtt2bdd_linux_arm64.tar.gz`, `mqtt2bdd_linux_armv7.tar.gz`,
   `mqtt2bdd_darwin_arm64.tar.gz`), byte-identical to its canonical counterpart. The alias exists
   solely to make the `releases/latest/download/<name>` URL resolvable — that URL requires an
   asset name that does not change between releases. A release therefore carries eight archives,
   four distinct payloads
7. Each archive contains the `mqtt2bdd` binary (executable bit set), `LICENSE`, and `README.md`
8. A `checksums.txt` asset lists the SHA-256 of all eight archives in the standard
   `sha256sum -c` input format. Each canonical/alias pair necessarily shares one checksum, since
   the payloads are identical; the file lists both names so that either download can be verified
   without knowing about the other
9. The workflow creates a GitHub Release on the tag, attaches the eight archives plus
   `checksums.txt`, and uses GitHub's auto-generated release notes as the body
10. A tag carrying a pre-release suffix (`-rc`, `-beta`) produces a release marked as
    pre-release, so it never becomes the target of the `latest` URL — the aliases it publishes
    are reachable only through its own pinned tag URL, which is the intent
11. The workflow declares `permissions: contents: write` and authenticates with the default
    `GITHUB_TOKEN` only — no additional secret is configured in the repository
12. The workflow is rehearsed end to end on a `v0.9.0-rc1` tag before `v1.0.0` is tagged: the
    run succeeds, all nine assets are present, a canonical and an alias archive verify to the
    same SHA-256, and the extracted `linux/amd64` binary runs and logs
    `event=starting version=0.9.0-rc1`
13. Workflow YAML is commented to the standard of the rest of the repository — each job and each
    non-obvious step (the `v`-stripping, `GOARM=7`, why each archive is published under two
    names) explains its rationale, consistent with this project's educational intent
14. **[Carry-forward, TEST-001]** `test/run-integration-tests.sh`'s failure path is independently
    exercised rather than accepted on the Dev's account: a deliberately broken scenario is
    introduced, the script is run, and it is confirmed to (a) exit **non-zero** and (b) still tear
    the stack down completely via its `trap cleanup EXIT`. Open since gate `3.3`, where AC9's
    negative path was accepted without being re-exercised; this story is the first to make that
    exit code load-bearing, because AC2 puts it in front of every release

## Tasks / Subtasks

- [x] Task 1 — Establish the facts the workflow must reproduce, before writing any YAML (AC: 2, 3, 4, 7)
  - [x] Read `Dockerfile` lines 18–25 and confirm the exact build invocation the workflow must match
        (AC4): `CGO_ENABLED=0 GOOS=linux go build -ldflags="-s -w -X main.Version=${VERSION}" -o
        /app/mqtt2bdd ./cmd/mqtt2bdd`. The three flag groups each have a stated reason in the
        Dockerfile's own comments — `CGO_ENABLED=0` for a static binary with no libc, `-s -w` to
        strip symbols and DWARF, `-X main.Version` to inject the version. The workflow must use the
        same flags, not equivalent ones, or a released binary and a container-built binary diverge.
  - [x] Confirm the injection target exists and is spelled exactly as the flag names it:
        `cmd/mqtt2bdd/main.go:54` declares `var Version = "dev"` in `package main`, so the linker
        symbol is `main.Version`. Confirm `main.go:210` logs it as
        `l.Info("MQTT2BDD starting", "event", "starting", "version", Version)` — this is the exact
        line AC12's rehearsal asserts on, so the assertion is checkable against source now even
        though the rehearsal itself is deferred.
  - [x] Confirm `test/run-integration-tests.sh`'s CI-readiness claim in AC2 rather than trusting it:
        it is `set -u`, it `trap cleanup EXIT INT TERM`s an unconditional `docker compose down -v
        --remove-orphans`, it exits 1 on each of its five failure paths, and it needs `docker
        compose` plus the ability to `docker run` a `golang:1.23-alpine` container with the host
        docker socket mounted. All of that is available on `ubuntu-latest`. Record what the script
        actually needs, so a future runner-image change is diagnosable.
  - [x] Confirm the `staticcheck` version to pin. `dev/Dockerfile.dev:8` installs
        `honnef.co/go/tools/cmd/staticcheck@v0.6.1`. The workflow pins the **same** version: a CI
        gate that runs a different analyser version from the one developers run locally produces
        failures nobody can reproduce, which is how a quality gate becomes something people route
        around.

- [x] Task 2 — Write the trigger, permissions and version extraction (AC: 1, 4, 11)
  - [x] `on: push: tags: ['v*.*.*']` and nothing else — no `workflow_dispatch`, no `push: branches`.
        AC1 says "triggered **only** by pushed tags". Note why the glob works for pre-releases:
        GitHub's tag filter treats `*` as matching any run of characters except `/`, so the final
        `*` in `v*.*.*` absorbs `0-rc1` and `v0.9.0-rc1` matches. This is non-obvious and is exactly
        the kind of thing AC13 wants commented.
  - [x] `permissions: contents: write` declared at workflow level (AC11). Nothing broader. This is
        the minimum that `gh release create` needs, and declaring it explicitly means the workflow
        does not silently inherit whatever the repository's default token permissions happen to be.
  - [x] No secret beyond the automatic `GITHUB_TOKEN` (AC11). Use `env: GH_TOKEN: ${{ github.token }}`
        for the `gh` CLI rather than configuring a PAT. State in a comment that no repository secret
        is required, so nobody adds one believing it is needed.
  - [x] Version extraction (AC4): `VERSION="${GITHUB_REF_NAME#v}"`. `GITHUB_REF_NAME` is the tag name
        for a tag push (`v1.0.0`), and `${VAR#v}` is POSIX prefix removal — it strips **one** leading
        `v` and leaves everything else, so `v0.9.0-rc1` → `0.9.0-rc1`. Comment why the `#v` form is
        used rather than a `sed`/`tr` deletion, which would also strip a `v` occurring elsewhere.
  - [x] Pre-release detection (AC10): a tag is a pre-release if the version contains a `-`. Semver
        defines everything after the first `-` as the pre-release identifier, so this test is exact
        rather than heuristic, and it covers `-rc`, `-beta`, `-alpha` and anything else without
        enumerating them.

- [x] Task 3 — Write the quality gate job (AC: 2)
  - [x] One job, named so its purpose is obvious in the Actions UI, on `ubuntu-latest`, that the
        build and release jobs both `needs:`. AC2's guarantee is structural: if this job fails, no
        matrix job starts and no release is created, because `needs:` is a hard dependency, not a
        soft one.
  - [x] Use `actions/setup-go` with `go-version-file: go.mod` rather than a hardcoded `1.23`. The
        version then cannot drift from the module's own declaration — and Story 4.1 has already
        demonstrated what it costs when a path in one file and a path in another are allowed to
        disagree.
  - [x] The four static/unit gates in order: `gofmt -l .` (**must print nothing** — `gofmt -l`
        exits 0 even when it lists files, so the step must fail explicitly when the output is
        non-empty; a bare `gofmt -l .` step is a gate that can never fail, which is worse than no
        gate at all), `go vet ./...`, `staticcheck ./...` (at the pinned version from Task 1), and
        `go test ./...`.
  - [x] The integration gate: `./test/run-integration-tests.sh`, run from the repository root, with
        no workflow-specific adaptation — AC2 is explicit that none is needed. Give the step a
        generous `timeout-minutes` (the script itself waits up to 90 s for health and then builds a
        Docker image and runs the full suite): a hung Docker pull should fail the job in minutes,
        not consume the default six-hour job timeout.
  - [x] Do **not** add a `continue-on-error` anywhere in this job. The entire point of AC2 is that a
        failing gate blocks the release.

- [x] Task 4 — Write the build matrix job (AC: 3, 4)
  - [x] Four matrix entries, each carrying its `GOOS`, `GOARCH`, the optional `GOARM`, and the
        **name fragment** used in both archive names (`linux_amd64`, `linux_arm64`, `linux_armv7`,
        `darwin_arm64`). Deriving the fragment from `GOOS`/`GOARCH` in shell would produce
        `linux_arm` for the 32-bit target, not `linux_armv7`, and AC5/AC6 name the file
        `linux_armv7` — so the fragment is data in the matrix, not a computed string. Comment this,
        because it looks like redundancy and is not.
  - [x] `GOARM: 7` on the `linux/arm` entry only (AC3). Comment why: `GOARCH=arm` alone defaults to
        ARMv6, and `GOARM=7` targets the ARMv7 instruction set that 32-bit Raspberry Pi OS runs on.
  - [x] The build step is the Dockerfile's invocation with `GOOS`/`GOARCH` parameterised:
        `CGO_ENABLED=0 GOOS=… GOARCH=… [GOARM=…] go build -ldflags="-s -w -X main.Version=${VERSION}"
        -o dist/mqtt2bdd ./cmd/mqtt2bdd`. Cross-compilation needs no extra toolchain here precisely
        *because* `CGO_ENABLED=0` — pure Go cross-compiles from any host. Comment that connection:
        it is the same flag serving two purposes and a reader deserves to see it.
  - [x] The binary is named `mqtt2bdd` inside every archive, with no platform suffix (AC7 names it
        `mqtt2bdd`). The platform is carried by the archive name; a user who extracts two archives
        into the same directory is doing something the naming scheme does not have to solve.

- [x] Task 5 — Package, alias and upload the archives (AC: 5, 6, 7)
  - [x] Stage the three archive members in one directory before tarring: the built binary
        (`chmod +x` — AC7 requires the executable bit set, and it must survive into the archive),
        plus `LICENSE` and `README.md` copied from the checkout. `LICENSE` exists as of Story 4.2;
        if it were missing the archive would silently ship without it, so **verify both files exist
        before tarring** and fail the step if not.
  - [x] Create the canonical archive first: `mqtt2bdd_${VERSION}_${FRAGMENT}.tar.gz` (AC5), tarring
        the three members from the staging directory so the archive has no leading `dist/` path
        component.
  - [x] Create the alias by **copying the canonical archive**, not by tarring a second time:
        `cp mqtt2bdd_${VERSION}_${FRAGMENT}.tar.gz mqtt2bdd_${FRAGMENT}.tar.gz`. AC6 requires the two
        be **byte-identical**, and gzip embeds an mtime by default — two separate `tar -czf`
        invocations a second apart produce different bytes and therefore different SHA-256 sums,
        which would break AC8's "each canonical/alias pair necessarily shares one checksum". Copying
        makes byte-identity structural rather than something to hope for. **Comment this**: it is
        the single least obvious decision in the whole workflow, and AC13 asks for exactly this kind
        of rationale.
  - [x] Upload both archives as a workflow artifact so the release job can collect all eight. Use a
        per-target artifact name and `actions/download-artifact` with `merge-multiple: true` in the
        release job, so all eight land in one directory.

- [x] Task 6 — Write the release job: checksums, release creation, pre-release flag (AC: 8, 9, 10, 11)
  - [x] `needs: [quality, build]` — depending on the quality job directly as well as transitively
        makes AC2's guarantee readable at the point it matters, rather than inferred through the
        build job.
  - [x] Download all artifacts into one directory, then generate `checksums.txt` with
        `sha256sum *.tar.gz` (AC8). That is already the exact `sha256sum -c` input format — two
        spaces between hash and filename — so no reformatting is needed and none should be invented.
        Sort the output for a stable, reviewable file. Expect **8 lines and 4 distinct hashes**;
        assert that in the step rather than assuming it, because it is the one cheap check that
        proves the aliasing in Task 5 actually produced identical payloads.
  - [x] Create the release with the `gh` CLI rather than a third-party action: `gh release create`
        is preinstalled on GitHub runners, needs no `uses:` pin to audit, and adds nothing to the
        supply chain of a workflow whose whole job is to produce artefacts people download and run.
        Note the trade-off in a comment — a third-party action would be shorter — so the choice
        reads as deliberate.
  - [x] `--generate-notes` for AC9's "GitHub's auto-generated release notes as the body". Attach all
        nine assets (eight archives plus `checksums.txt`) in the same call.
  - [x] `--prerelease` conditionally, from Task 2's `-`-in-version test (AC10). Comment the
        consequence rather than just the mechanism: a pre-release never becomes the target of
        `releases/latest/download/…`, which is what keeps a release candidate's version-free aliases
        from being served to someone who asked for "latest".
  - [x] `--title` set to the tag name, so the release is identifiable in a list.

- [x] Task 7 — Comment the workflow to this repository's standard (AC: 13)
  - [x] Every job gets a comment stating what it is for and what depends on it. Every non-obvious
        step gets a comment stating *why*, not *what* — the same rule
        `docs/architecture/coding-standards.md` applies to Go source, and the same rule Story 3.6
        applied across the codebase.
  - [x] The five specifically non-obvious things AC13 names or implies, each of which must carry its
        rationale: (1) why `v*.*.*` matches `v0.9.0-rc1`; (2) `${GITHUB_REF_NAME#v}` and why not a
        `sed`; (3) `GOARM=7` and 32-bit Raspberry Pi OS; (4) why every archive is published under
        two names, and why the alias is a **copy** rather than a second tar; (5) why `gofmt -l .`
        needs an explicit emptiness check to be a gate at all.
  - [x] Do not comment the obvious. `- uses: actions/checkout@v4` does not need "checks out the
        code". This is the same discipline as the rest of the repository, and Story 3.6's review
        rejected exactly this kind of filler.
  - [x] English throughout, per `CLAUDE.md`. This file will be one of the most-read in a public
        repository — a workflow is what a reader opens to learn how a project releases.

- [x] Task 8 — Verify the workflow's claims **locally**, by reproducing them outside CI (AC: 3, 4, 5, 6, 7, 8)
  - [x] This task is what makes twelve of the thirteen epic ACs genuinely deliverable rather than
        merely written. **Do not mark AC3–AC8 met on the strength of having written YAML that looks
        right.** Reproduce the build-and-package pipeline by hand, through the containerized
        toolchain, and check the artefacts.
  - [x] Cross-compile all four targets with the exact flags the workflow uses, from `dev/`, using a
        stand-in version such as `0.0.0-local`:

    ```bash
    docker compose exec go-dev sh -c 'CGO_ENABLED=0 GOOS=linux  GOARCH=amd64          go build -ldflags="-s -w -X main.Version=0.0.0-local" -o /tmp/b/linux_amd64/mqtt2bdd  ./cmd/mqtt2bdd'
    docker compose exec go-dev sh -c 'CGO_ENABLED=0 GOOS=linux  GOARCH=arm64          go build -ldflags="-s -w -X main.Version=0.0.0-local" -o /tmp/b/linux_arm64/mqtt2bdd  ./cmd/mqtt2bdd'
    docker compose exec go-dev sh -c 'CGO_ENABLED=0 GOOS=linux  GOARCH=arm   GOARM=7  go build -ldflags="-s -w -X main.Version=0.0.0-local" -o /tmp/b/linux_armv7/mqtt2bdd  ./cmd/mqtt2bdd'
    docker compose exec go-dev sh -c 'CGO_ENABLED=0 GOOS=darwin GOARCH=arm64          go build -ldflags="-s -w -X main.Version=0.0.0-local" -o /tmp/b/darwin_arm64/mqtt2bdd ./cmd/mqtt2bdd'
    ```

    All four must succeed (AC3). A cross-compilation failure here would fail in CI identically, and
    finding it now costs a minute instead of a tag.
  - [x] Confirm the version injection actually took (AC4) rather than assuming the flag was accepted:
        the `linux/amd64` binary is runnable inside the `go-dev` container, so run it and read the
        first log line. It will exit non-zero once it fails to reach a broker — that is fine and
        expected; the assertion is on `event=starting version=0.0.0-local`, which is emitted before
        any connection is attempted (`main.go:210`, before the `Connect` calls). This is the same
        assertion AC12's rehearsal makes, exercised here on a local build.
  - [x] Confirm each binary is for the architecture it claims (AC3) with `file` or `go version -m`,
        rather than trusting that `GOARCH` was honoured.
  - [x] Reproduce the packaging (AC5, AC6, AC7): stage binary + `LICENSE` + `README.md`, `chmod +x`
        the binary, tar the canonical name, `cp` to the alias name, then verify **all four** of:
        `tar -tvf` lists exactly three members with the binary's executable bit set;
        `sha256sum canonical alias` produces two **identical** hashes; the archive has no leading
        directory component; and the canonical name embeds the version while the alias does not.
  - [x] Reproduce `checksums.txt` (AC8): `sha256sum *.tar.gz | sort` over all eight archives, then
        confirm the file has 8 lines and 4 distinct hashes, and that
        `sha256sum --ignore-missing -c checksums.txt` verifies against a single downloaded archive —
        which is precisely the sequence Story 4.4 AC5 will document for users, so verifying it here
        means 4.4 documents something known to work.
  - [x] Clean up every artefact produced by this task. `git status --short` must be clean at the end:
        `bin/` and `__debug_bin*` are gitignored, but a stray `dist/` or `*.tar.gz` at the repository
        root is not, and this story must not be the one that ships a binary into a public repository.

- [x] Task 9 — TEST-001: exercise the integration script's failure path (AC: 14)
  - [x] Introduce **one** deliberate breakage, run the script, and observe the two things AC14
        requires. The least invasive breakage that exercises a real failure path: temporarily change
        the sensor name in the smoke-test **verification query** (`test/run-integration-tests.sh:49`,
        `WHERE sensor='integration/smoke'`) to a value nothing publishes, so the row count comes back
        `0` and the script takes its `FAIL: smoke-test message not found` branch. Change the query at
        line 49 only — **not** the `mosquitto_pub` topic at line 43. Changing the publish side would
        also work, but changing the read side exercises the branch with everything upstream of it
        still healthy, which is a cleaner isolation of the failure path under test.
  - [x] Run `./test/run-integration-tests.sh` from the repository root and confirm **both**:
        (a) `echo $?` is **non-zero** — this is the half gate `3.3` accepted on the Dev's account and
        never re-exercised, and the half AC2 now makes load-bearing, since a release is gated on it;
        (b) the stack is **still fully torn down** despite the early `exit 1` — `docker ps -a
        --filter name=mqtt2bdd-test` returns nothing and `docker network ls --filter
        name=mqtt2bdd-test` returns nothing. The `trap cleanup EXIT` is what should guarantee this,
        and a cleanup that only runs on the success path would leave a CI runner dirty and a
        developer's machine with orphaned containers.
  - [x] **Revert the breakage immediately** (`git checkout -- test/run-integration-tests.sh`) and
        confirm the file is byte-identical to its committed state (`git diff --stat --
        test/run-integration-tests.sh` empty). Then re-run the script unmodified and confirm it exits
        **0** again — a negative-path test that leaves the positive path broken is worse than no
        test.
  - [x] Record the exit code, the container/network listings, and the clean re-run in the Dev Agent
        Record. AC14's whole point is that this be *evidence*, not an assertion — that is what
        TEST-001 has been open about since gate `3.3`.

- [x] Task 10 — Static validation of the workflow file (AC: 1, 13)
  - [x] The workflow cannot be executed, so validate everything about it that can be validated
        without a runner. **Parse the YAML** — a syntactically invalid workflow fails at push time
        with a message in the Actions UI nobody sees until they look, and it is free to catch now.
  - [x] **Run the parse on the host, not in `go-dev`.** The PO checked: `dev/Dockerfile.dev` is
        `FROM golang:1.23-alpine` and the container has **no `python3`** (`which python3` → nothing),
        so the containerized form does not work. The host has `python3` with PyYAML 6.0.3, verified.
        This is not a breach of the Docker-only rule: that rule exists so the *Go toolchain, database
        and broker clients* match the versions the project builds against, and a YAML syntax check on
        a CI configuration file is none of those. Record the reasoning, not just the command.

    ```bash
    python3 -c 'import yaml; yaml.safe_load(open(".github/workflows/release.yml")); print("YAML OK")'
    ```
  - [x] Check the structural claims by reading the parsed document, not the text: the trigger is a
        tag filter only; `permissions.contents` is `write`; three jobs exist with the expected
        `needs:` edges; the matrix has exactly four entries; every `uses:` is a first-party
        `actions/*` action pinned to a major version.
  - [x] Confirm no secret other than `GITHUB_TOKEN`/`github.token` is referenced (AC11):
        `grep -n "secrets\." .github/workflows/release.yml` must return nothing but `GITHUB_TOKEN`,
        if anything at all.
  - [x] Confirm the file lands at exactly `.github/workflows/release.yml` (AC1) — GitHub reads only
        that directory, and a workflow at `.github/release.yml` is silently inert.

- [x] Task 11 — AC12: prepare the rehearsal, do not execute it (AC: 12 — **deferred**)
  - [x] **Do not create a tag, do not push, do not trigger a workflow run.** AC12 requires a GitHub
        repository that does not exist (Story 4.2 PUB-001). Creating a `v0.9.0-rc1` tag locally would
        be harmless but pointless — the tag is only meaningful once pushed — and pushing requires the
        publication decision the maintainer has reserved.
  - [x] Reproduce the rehearsal runbook from Dev Notes → Deferred: The Rehearsal Runbook into
        Completion Notes, with each of AC12's four assertions mapped to the command that checks it.
  - [x] State plainly which of AC12's four assertions have already been **locally pre-verified** by
        Task 8 and which genuinely require a live run. Two of them — "a canonical and an alias
        archive verify to the same SHA-256" and "the extracted `linux/amd64` binary runs and logs
        `event=starting version=…`" — are exercised locally in Task 8 against a stand-in version;
        only "the run succeeds" and "all nine assets are present" strictly require GitHub. Saying so
        precisely is more useful to the maintainer than declaring the whole AC unreachable.

## Dev Notes

### Previous Story Insights

- **Story 4.2** delivered `LICENSE`, which AC7 requires inside every archive. It did not exist
  before. Its gate also opened **PUB-001** (ACs 6/7/8/10 deferred to the maintainer), which is the
  direct cause of AC12's deferral here, and its PO acceptance instructed the SM to scope AC12 the
  same way rather than rediscovering the constraint.
  [Source: docs/stories/4.2.story.md#po-validation-final; docs/qa/gates/4.2-prepare-repository-for-public-release.yml]
- **Story 4.2's PO acceptance also asked the SM to consider homing TEST-001 here.** Adopted as AC14.
- **Story 4.1** set the module path. Not directly load-bearing for this workflow — the build uses the
  relative package path `./cmd/mqtt2bdd`, exactly as `Dockerfile:25` does — but it is why
  `go install github.com/hagnerk/mqtt2bdd/...` will resolve for anyone who prefers that to a binary.
- **Story 3.1** wrote the production `Dockerfile` whose build flags AC4 requires this workflow to
  match, and established `main.Version` injection. Its gate measured the resulting image at ~18.5 MB
  and the stripped binary at 9.4 MB (from 13.7 MB), which is the concrete evidence that `-s -w` is
  doing something worth keeping. [Source: Dockerfile:18-25; docs/stories/3.1.story.md]
- **Story 3.3** wrote `test/run-integration-tests.sh`, which AC2 runs unmodified and AC14 finally
  exercises negatively. Gate `3.3` raised TEST-001 precisely because it accepted the script's
  non-zero-exit behaviour on the Dev's account without re-running it.
  [Source: test/run-integration-tests.sh; docs/qa/gates/3.3-create-integration-test-infrastructure.yml]

### The Facts the Workflow Must Match (verified live for this draft)

| Fact | Value | Source |
| --- | --- | --- |
| Production build flags | `CGO_ENABLED=0 GOOS=linux go build -ldflags="-s -w -X main.Version=${VERSION}" -o /app/mqtt2bdd ./cmd/mqtt2bdd` | `Dockerfile:23-25` |
| Version symbol | `var Version = "dev"` in `package main` → linker symbol `main.Version` | `cmd/mqtt2bdd/main.go:53-54` |
| The line AC12 asserts on | `l.Info("MQTT2BDD starting", "event", "starting", "version", Version)` | `cmd/mqtt2bdd/main.go:210` |
| Emitted **before** any connection attempt | Yes — line 210 precedes both `Connect` calls (`mqttClient.Connect` at `:217`, `dbClient.Connect` at `:228`) | `cmd/mqtt2bdd/main.go:210` vs `:217`, `:228` |
| Go version | `go 1.23` in `go.mod`; use `go-version-file: go.mod` | `go.mod:3` |
| `staticcheck` version used locally | `honnef.co/go/tools/cmd/staticcheck@v0.6.1` | `dev/Dockerfile.dev:8` |
| Integration script entry point | `./test/run-integration-tests.sh`, run from the repository root | `test/run-integration-tests.sh:8` |
| Script's failure paths | 5 (`exit 1`): stack start, health timeout, `mosquitto_pub`, smoke-test row count, Go suite | `test/run-integration-tests.sh` |
| Script's cleanup guarantee | `trap cleanup EXIT INT TERM` (line 18) → `cleanup()` (lines 14-17) runs `docker compose down -v --remove-orphans` | `test/run-integration-tests.sh:14-18` |
| Script's runner requirements | `docker compose`, and `docker run` with `/var/run/docker.sock` bind-mounted | `test/run-integration-tests.sh:72-88` |

The `main.go:210`-before-`Connect` ordering is worth stating explicitly: it is what makes AC12's
assertion checkable at all. The binary will fail to reach a broker in any environment where one is
not configured, but it emits `event=starting version=…` first, so `version=` can be asserted without
standing up a broker.

### Why the Alias Must Be a Copy, Not a Second Tar

This is the one decision in the workflow that looks like a shortcut and is actually a correctness
requirement, so it is stated once here and commented once in the YAML.

> **[Corrected during implementation — the premise below was empirically false, the conclusion was
> not.]** The draft asserted that gzip embeds an mtime, so two `tar -czf` runs a second apart would
> produce different bytes. **Measured: they do not.** Two `tar -czf` invocations two seconds apart
> over identical inputs produced identical SHA-256 sums on both GNU tar 1.35 / gzip 1.13 (the
> toolchain `ubuntu-latest` ships) and BusyBox tar. The mechanism the draft missed: gzip only writes
> an MTIME when it compresses a *named file*; when it compresses a stream — which is exactly what
> `tar -z` hands it — the MTIME field is written as 0. The corrected reasoning is below, and the
> shipped workflow comment states the corrected version, not this one.

AC6 requires each alias archive to be **byte-identical** to its canonical counterpart, and AC8
requires that "each canonical/alias pair necessarily shares one checksum".

A second `tar -czf` would, on today's toolchains, satisfy that — as measured above. **The decision to
use `cp` stands anyway, for a better reason than the draft gave: it turns a property that currently
holds by accident into one that holds by construction.** The byte-identity of a published pair would
otherwise depend on gzip's stream-vs-file MTIME behaviour, on directory read order, and on whatever
the runner image's `tar` does after its next update — none of which anyone would think to re-check
before cutting a release, and all of which a user's failed `sha256sum -c` would report first.

`cp` costs one line and removes the entire class of failure. The alternative — `gzip -n` or
`tar --mtime` with a fixed timestamp — would pin the timestamps but not the ordering, and would
still be a reproducibility argument rather than a guarantee.

The workflow's checksum step asserts the outcome (8 lines, 4 distinct hashes) so that a regression
here fails the release rather than reaching a user.

Task 8's local check (`sha256sum canonical alias` → two identical hashes) is what proves this, and
Task 6's assertion (8 lines, 4 distinct hashes in `checksums.txt`) is what would catch a regression
in CI.

### Deferred: The Rehearsal Runbook (AC12)

**Why deferred.** AC12 requires a workflow run on GitHub's runners, triggered by a tag pushed to a
repository that does not exist. Its precondition is Story 4.2's PUB-001 — the publication decision
reserved to the maintainer. Scoped exactly as Story 4.2 scoped its own deferred ACs: stated, not
dropped; delivered as far as it can be; handed over with a runbook.

**What is already pre-verified locally**, so the maintainer knows what is actually being risked:

| AC12 assertion | Status before the live run |
| --- | --- |
| "a canonical and an alias archive verify to the same SHA-256" | **Pre-verified locally** (Task 8) on a stand-in version. The mechanism is a `cp`; it cannot behave differently on a runner. |
| "the extracted `linux/amd64` binary runs and logs `event=starting version=…`" | **Pre-verified locally** (Task 8) with `main.Version=0.0.0-local`. The flag and the symbol are confirmed to work together. |
| "the run succeeds" | **Requires GitHub.** This is the real content of the rehearsal: runner environment, action availability, `gh` authentication, and whether the integration suite passes on a runner rather than on a developer's Docker. |
| "all nine assets are present" | **Requires GitHub.** Depends on artifact upload/download and `gh release create` behaviour, none of which is locally observable. |

**Runbook — after Story 4.2's PUB-001 handover has been run:**

```bash
# 1. Tag and push the rehearsal tag. The -rc1 suffix is what AC10's pre-release detection keys on.
git tag v0.9.0-rc1
git push origin v0.9.0-rc1

# 2. Watch the run. It must reach 'completed success'; if the quality job fails, the release is
#    correctly blocked and nothing is published — that is AC2 working, not the rehearsal failing.
gh run watch

# 3. Nine assets present, and the release marked pre-release (AC9, AC10).
gh release view v0.9.0-rc1 --json isPrerelease,assets \
  --jq '{prerelease: .isPrerelease, count: (.assets|length), names: [.assets[].name]}'
#    Expect: prerelease true, count 9.

# 4. A canonical and its alias verify to the same SHA-256 (AC6, AC8, AC12).
gh release download v0.9.0-rc1 -p 'mqtt2bdd_0.9.0-rc1_linux_amd64.tar.gz' \
                               -p 'mqtt2bdd_linux_amd64.tar.gz' -p 'checksums.txt'
sha256sum mqtt2bdd_0.9.0-rc1_linux_amd64.tar.gz mqtt2bdd_linux_amd64.tar.gz   # two identical hashes
sha256sum --ignore-missing -c checksums.txt                                    # both verify OK

# 5. The extracted binary reports the injected version (AC4, AC12).
tar -xzf mqtt2bdd_0.9.0-rc1_linux_amd64.tar.gz
./mqtt2bdd 2>&1 | head -1     # on a Linux host; expect event=starting version=0.9.0-rc1
#    The process then exits non-zero because no broker is configured. That is expected: the
#    assertion is on the first log line, which main.go:210 emits before any Connect call.

# 6. Only once all of the above pass, tag the real release.
git tag v1.0.0 && git push origin v1.0.0
```

**If the rehearsal fails.** Delete the tag locally and remotely
(`git tag -d v0.9.0-rc1 && git push --delete origin v0.9.0-rc1`), delete the release if one was
created (`gh release delete v0.9.0-rc1 --yes`), fix, and re-tag. This is exactly the recovery AC2
describes, and it is why the rehearsal happens on `v0.9.0-rc1` and not on `v1.0.0`.

### Carry-Forward Findings

Gate `4.2`'s register carries one open item owned by the maintainer and three tracked, unowned ones:

| Item | Origin | Homed here? |
| --- | --- | --- |
| **TEST-001** — the integration script's non-zero-exit path never independently exercised | gate `3.3` | **Yes — adopted as AC14.** |
| PUB-001 — ACs 6/7/8/10 of Story 4.2 await publication | gate `4.2` | No, and cannot be. Owned by the maintainer. It is the *cause* of AC12's deferral here, not something this story can discharge. |
| REL-001 — `mqtt_status` cannot distinguish a permanently-failing re-subscribe from a transient one | gate `3.3` | No. Requires a behavioural change in `healthCheckLoop`; this story touches no Go source. Still unowned. |
| Broker authentication (`allow_anonymous true`) | gate `3.2` | No. Re-assessed and closed as an accepted risk in Story 4.2; not reopened. |

**Why TEST-001 is homed here.** Gate `3.3` deferred it to "the next time
`test/run-integration-tests.sh` is touched". Strictly, this story does not *modify* the script — but
it does something the deferral did not anticipate and that matters more: **it makes the script's exit
code load-bearing for every release.** Until now, a false zero exit would have meant a developer
believed a test passed. From AC2 onward, it means a broken build gets published as a release binary
people download and run. That is a change in the finding's consequence, not merely another
opportunity to close it, and it is the right moment to stop deferring.

Story 4.2 set the precedent by adopting DOC-001 on the same reasoning — the cost of the open finding
changed at publication, so publication was where it got closed.

### File Locations

| File | Change |
| --- | --- |
| `.github/workflows/release.yml` | **New.** The only file this story creates. |
| `docs/stories/4.3.story.md` | This file. |

**Nothing else.** No `.go` file, no `go.mod`, no `Dockerfile`, no Compose file, no
`test/run-integration-tests.sh` (Task 9's breakage is temporary and reverted within the task), no
architecture shard, and **no `README.md`** — the Project Structure tree entry for
`.github/workflows/` and the release-cutting documentation are Story 4.4's ACs 8 and 9, not this
story's.

`.github/` is a new top-level directory. Note that `.gitignore` does not exclude it (verified), and
`.dockerignore` need not be changed: the production `Dockerfile` copies only `go.mod`, `go.sum`,
`cmd/` and `internal/`, so a new top-level directory cannot reach the image regardless.

### Technical Constraints

- **English everywhere**, including every comment in the workflow YAML. This file is among the first
  a reader of a public repository opens to learn how the project releases. [Source: CLAUDE.md]
- **Docker-only tooling.** Task 8's cross-compilations and Task 10's YAML parse run through
  `docker compose exec go-dev` from `dev/`. Task 9 runs `./test/run-integration-tests.sh`, which
  manages its own containers — that is the documented way to run it.
- **No new dependency.** No Go module, no third-party GitHub Action. Every `uses:` is first-party
  `actions/*`; the release is created with the preinstalled `gh` CLI.
- **No secret.** `GITHUB_TOKEN` only (AC11). If the workflow ever appears to need more, that is a
  signal something is wrong with the approach, not a reason to add a PAT.
- **Leave no artefact behind.** Task 8 produces binaries and tarballs; `git status --short` must be
  clean at the end of the story.

### Testing Requirements

There is no unit test to write here — a workflow is configuration, and asserting on YAML structure in
Go would test the test. What replaces it is **reproduction**: Task 8 executes, by hand and outside
CI, every build and packaging step the workflow describes, and checks the artefacts rather than the
intent. That is what turns AC3–AC8 from "written correctly" into "verified".

Task 9 is a genuine negative test — the first this project has run against
`test/run-integration-tests.sh`'s failure path — and its evidence (exit code, container listing,
network listing, clean re-run) is the deliverable, not a claim that it was done.

AC12 is explicitly **not** claimed and is not tested. Task 11 records which of its four assertions
Task 8 already pre-verifies locally and which genuinely need a runner, rather than declaring the
whole AC unreachable.

[Source: docs/architecture/test-strategy-and-standards.md]

## Testing

- **AC1**: the file is at exactly `.github/workflows/release.yml`; its `on:` block contains a tag
  filter and nothing else; `v*.*.*` documented as matching `v0.9.0-rc1`.
- **AC2**: three jobs; the build job `needs:` the quality job and the release job `needs:` both;
  `gofmt -l .` has an explicit non-empty-output failure; no `continue-on-error` anywhere.
- **AC3**: all four targets cross-compile locally with the workflow's exact flags; each binary
  confirmed to be for the architecture it claims.
- **AC4**: flags byte-compared against `Dockerfile:23-25`; a locally built binary logs
  `version=0.0.0-local`, proving `-X main.Version` reaches `main.go:210`.
- **AC5/AC6/AC7**: a reproduced canonical/alias pair has **identical** SHA-256 sums; `tar -tvf`
  lists exactly `mqtt2bdd` (executable bit set), `LICENSE`, `README.md`, with no leading directory.
- **AC8**: a reproduced `checksums.txt` over eight archives has 8 lines and 4 distinct hashes, and
  `sha256sum --ignore-missing -c checksums.txt` verifies against a single archive.
- **AC10/AC11**: pre-release detection keys on a `-` in the version; `grep -n "secrets\."` finds
  nothing beyond `GITHUB_TOKEN`; `permissions.contents: write` present and nothing broader.
- **AC13**: every job and each of the five named non-obvious steps carries a *why* comment; no
  comment restates what a line obviously does.
- **AC14**: the script exits **non-zero** on a deliberately broken scenario **and** still tears the
  stack down completely; the breakage is reverted and the unmodified script exits 0.
- **Scope**: `git diff --stat` shows only `.github/workflows/release.yml` and this story file;
  `git status --short` clean, with no stray binary or tarball.
- **AC12**: **not tested, not claimed.** Deferred under PUB-001; runbook recorded instead.

[Source: docs/architecture/test-strategy-and-standards.md; docs/architecture/coding-standards.md]

## Change Log

| Date | Version | Description | Author |
| ---- | ------- | ------------ | ------ |
| 2026-07-29 | 0.1 | Initial draft. ACs 1–13 copied verbatim from Epic 4. AC14 added: TEST-001 adopted from gate `3.3`'s carry-forward register, per Story 4.2's PO acceptance — reasoning recorded (the finding's *consequence* changes when AC2 makes the script's exit code gate every release, which is more than another opportunity to close it). AC12 scoped as deferred under PUB-001, with a runbook and an explicit split of which of its four assertions Task 8 pre-verifies locally. All build/packaging facts derived live from `Dockerfile`, `main.go`, `go.mod`, `dev/Dockerfile.dev` and `test/run-integration-tests.sh` rather than assumed. The alias-must-be-a-copy requirement (gzip mtime → different bytes → different checksums → AC6/AC8 both fail) identified during drafting and given its own Dev Notes section. | Bob (Scrum Master) |
| 2026-07-29 | 0.2 | PO draft validation. **One blocking-if-followed defect found.** Task 10's YAML-parse command ran `python3` inside the `go-dev` container; `dev/Dockerfile.dev:1` is `FROM golang:1.23-alpine` and `which python3` in that container returns **nothing**. The draft offered a host fallback, but a subtask whose primary command cannot work is a subtask a Dev has to debug before it can be run. Rewritten to run on the host, with the host's PyYAML 6.0.3 verified present and — more importantly — with the *reasoning* recorded: the Docker-only rule exists so the Go toolchain and the database/broker clients match the versions the project builds against, and a YAML syntax check on a CI config is none of those. Three factual corrections: the log line at `main.go:210` precedes `mqttClient.Connect` at `:217` and `dbClient.Connect` at `:228`, not `:289` (which is `Subscribe`) — the ordering claim is right, the cited line was wrong; the smoke-test verification query is at `test/run-integration-tests.sh:49`, not `:47`, and Task 9 now says to break the **read** side at 49 rather than the publish side at 43, so the failure path is exercised with everything upstream still healthy; the script's `docker run` block starts at `:72`, not `:63`. Independently confirmed: `Dockerfile:23-25` build flags exactly as quoted; `go.mod:3` is `go 1.23`; `dev/Dockerfile.dev:8` pins `staticcheck@v0.6.1`; the script has exactly 5 `exit 1` paths and `trap cleanup EXIT INT TERM` at `:18`; `.github/` is not gitignored; PRD AC range `759-808` correct. **AC12's deferral accepted as scoped**, and the draft's split of its four assertions into two pre-verifiable locally and two genuinely needing a runner is more useful than a blanket "unreachable" — I am keeping that framing. **AC14 (TEST-001) accepted**, and the SM's reasoning for homing it here is better than the one I suggested: it is not that the script is being touched, it is that AC2 changes the *consequence* of a false zero exit from "a developer believes a test passed" to "a broken build ships as a downloadable binary". All 14 ACs trace to at least one task. Status → **Approved**. | Sarah (PO) |
| 2026-07-29 | 0.3 | Dev implementation (James). `.github/workflows/release.yml` written: three jobs (`quality` → `build` matrix → `release`), 280 lines of which 138 are comments. **Task 8 reproduced the entire build-and-package pipeline outside CI**, which is what makes ACs 3–8 verified rather than merely written: all four targets cross-compiled with the workflow's exact flags; each binary's `GOOS`/`GOARCH`/`GOARM`/`CGO_ENABLED` read back from its own embedded build settings via `go version -m` (including `GOARM=7` on `linux_armv7`); the `linux/amd64` binary run and confirmed to log `event=starting version=0.0.0-local`; archives built, aliased, and confirmed to have 3 members with the binary's executable bit set and no leading directory; canonical/alias pairs confirmed byte-identical; `checksums.txt` confirmed at 8 lines / 4 distinct hashes; `sha256sum --ignore-missing -c` confirmed to verify a single downloaded archive. **One correction to the story's own reasoning**: the Dev Notes claim that a second `tar -czf` would produce different bytes is empirically false — measured identical on GNU tar 1.35/gzip 1.13 and on BusyBox, because gzip writes MTIME=0 when compressing a stream. The `cp` decision stands on corrected grounds (making byte-identity structural rather than accidental) and the shipped workflow comment states the corrected reasoning. **AC14/TEST-001 closed with evidence**: the deliberately broken script exited **1** on the `FAIL: smoke-test message not found in sensor_metrics (rows=0)` branch and still tore down all containers, the network and the volumes; the breakage was reverted to a byte-identical file and the unmodified script re-run to exit **0**. Also verified that an empty `GOARM` (what the matrix passes for the three non-ARM targets) does not break the build — a CI-only failure mode otherwise. AC12 deferred under PUB-001 with its runbook and its four assertions split into two locally pre-verified and two needing a runner. Working tree clean, no stray artefact. Status → **Ready for Review**. | James (Dev) |
| 2026-07-29 | 0.4 | QA review (Quinn). Gate **CONCERNS**, quality_score 85 — the concern is AC12's inherited publication dependency, not the work. Thirteen of fourteen ACs independently re-verified. **One latent defect found and fixed during review (CI-001)**: the Staticcheck step invoked `staticcheck ./...` by bare name after `go install`, which depends on `$(go env GOPATH)/bin` being on PATH — a property of the runner image and of `setup-go`'s current behaviour, not of this workflow. It works today and nothing in the file makes it keep working; the failure would have appeared as `staticcheck: command not found` during a tagged release run, which is the worst place to find it. Changed to the absolute path with the reasoning commented; workflow re-parsed clean. Structural claims re-read from the **parsed** document rather than the text: tag-only trigger, `contents: write` and nothing broader, the three `needs:` edges, four matrix entries with `goarm` on `linux_armv7` only, and all four `uses:` first-party. Confirmed the release job's deliberate absence of a checkout is safe because `--repo "${GITHUB_REPOSITORY}"` is passed explicitly. **TEST-001 confirmed closed** — the negative-path evidence includes volumes, not just containers, which is the part an exit code alone does not prove. Recorded for Story 4.4: `sha256sum --ignore-missing` works on GNU coreutils and macOS but **not** on BusyBox. | Quinn (Test Architect) |
| 2026-07-29 | 0.5 | Final PO acceptance, with AC12 left open under PUB-002. Re-verified independently: the workflow parses and declares the three expected jobs; QA's CI-001 fix is present at line 86 (`"$(go env GOPATH)/bin/staticcheck" ./...`); `grep -c "secrets\."` → **0**. The check that mattered most to me was Task 9's revert, because a negative test that quietly leaves a modified file behind is worse than the finding it closed: `git log --oneline -1 -- test/run-integration-tests.sh` returns **`a14c785 Implementation of the story 3.3`** — the script's last modifying commit is still Story 3.3's, so the breakage was never committed and the file is byte-identical to the state TEST-001 was raised against. Status set to `Done — Rehearsal Deferred` rather than `Done`, matching the convention Story 4.2 established. Accepted QA's CI-001 fix without reservation: a gate that can silently stop being a gate is worth editing during review. Accepted the Dev's correction to the story's own alias reasoning, and specifically its handling — the wrong premise is left in the record as a marked correction rather than quietly rewritten. Recorded for Story 4.4: the BusyBox `--ignore-missing` gap, and the fact that 4.4 now documents an artefact shape known from measurement rather than from this workflow's promises. | Sarah (PO) |

## PO Validation (Draft)

### Validation Date: 2026-07-29

### Validated By: Sarah (Product Owner)

### Verdict: **Approved** (after one blocking-if-followed correction and three factual ones)

### Method

Every fact this workflow has to match was re-read from its source file, and every command the story
tells the Dev to run was checked for whether it would actually run.

1. **AC text.** All thirteen diffed against `docs/prd.md:759-808` — exact match, none reworded. AC14
   is clearly marked as SM-added with its provenance.

2. **The defect that mattered: a command that cannot run.** Task 10 told the Dev to parse the
   workflow YAML with `docker compose exec go-dev sh -c 'python3 -c "import yaml…"'`.
   `dev/Dockerfile.dev:1` is `FROM golang:1.23-alpine`, and `which python3` in that container returns
   nothing — there is no Python in it, and no reason there should be. The draft did offer a host
   fallback behind a `||`, so this would not have silently produced a wrong result; it would have
   produced a confusing failure that a Dev then has to diagnose mid-task, on a subtask whose whole
   purpose is a cheap syntax check.

   Rewritten to run on the host, with PyYAML 6.0.3 confirmed present, **and with the reasoning
   recorded rather than just the command**. That last part matters: this project has a standing
   Docker-only tooling rule, and a story that quietly runs something on the host without saying why
   teaches the next reader that the rule is negotiable. It is not — but it is a rule about the Go
   toolchain and the database/broker clients matching the versions the project builds against, and a
   YAML syntax check on a CI configuration file is none of those.

3. **Three factual corrections.**
   - The story's table claimed the startup log line at `main.go:210` precedes the `Connect` calls
     "vs `:289`". Line 289 is `mqttClient.Subscribe`. The actual `Connect` calls are at `:217`
     (MQTT) and `:228` (database). The *claim* is correct and load-bearing — AC12's assertion only
     works because `version=` is logged before any connection is attempted — but it cited the wrong
     line, which is exactly the kind of citation a reader would check and lose confidence over.
   - The smoke-test verification query is at `test/run-integration-tests.sh:49`, not `:47`. While
     correcting it I also tightened Task 9: break the **read** side at line 49, not the
     `mosquitto_pub` publish side at line 43. Both reach the same branch, but breaking the read side
     leaves everything upstream healthy, which isolates the failure path actually under test.
   - The script's `docker run` block starts at `:72`, not `:63`.

4. **Everything else in the facts table re-verified from source.** `Dockerfile:23-25` carries exactly
   the quoted flags; `cmd/mqtt2bdd/main.go:53-54` declares `var Version = "dev"` in `package main`,
   so `-X main.Version` names a real symbol; `go.mod:3` is `go 1.23`, so `go-version-file: go.mod`
   resolves; `dev/Dockerfile.dev:8` pins `staticcheck@v0.6.1`; the script has exactly five `exit 1`
   paths and `trap cleanup EXIT INT TERM` at `:18`; `.github/` is not matched by any `.gitignore`
   pattern.

5. **The alias-must-be-a-copy analysis is correct and is the best thing in this draft.** Gzip writes
   an mtime into its header, so two `tar -czf` runs over identical inputs a second apart produce
   different bytes and different SHA-256 sums — which would fail AC6's "byte-identical" outright and
   make AC8's "each canonical/alias pair *necessarily* shares one checksum" false. The SM found this
   during drafting rather than in CI, chose `cp` over reproducibility flags, and gave the reasoning
   its own section. This is the kind of thing that would otherwise have been discovered by the
   rehearsal, on a pushed tag.

### On AC12's Deferral

Accepted as scoped, and scoped better than I asked for.

Story 4.2's acceptance told the SM to scope AC12 the way 4.2 scoped its own deferred ACs. The draft
does that, and then does one thing more that I want kept: instead of declaring AC12 unreachable, it
**splits its four assertions** into the two Task 8 pre-verifies locally (canonical/alias share a
SHA-256; the extracted binary logs the injected version) and the two that genuinely require a runner
(the run succeeds; nine assets present). That is a more honest and more useful statement of risk than
a blanket deferral — it tells the maintainer that what the rehearsal is really testing is the runner
environment and the release-creation path, not the build and packaging logic, which is already known
to work.

### On AC14 (TEST-001)

Accepted, and the SM's reasoning is better than the one I offered at 4.2's acceptance.

I suggested homing TEST-001 here because AC2 "touches" the script, matching gate `3.3`'s own deferral
wording. The SM points out that this story does not modify the script at all — and homes it anyway on
a stronger ground: **AC2 changes the finding's consequence.** Until now, a false zero exit from that
script would have meant a developer believed a test passed. From AC2 onward, it means a broken build
is published as a binary people download and run. A finding whose blast radius changes is a finding
whose deferral should end, regardless of whether the file is being edited.

That is the same principle that moved DOC-001 onto Story 4.2, applied correctly to a different
finding. It is now twice-used and worth naming as this epic's convention.

### Assessment Against the Story Draft Checklist

- **Goal & context clarity** — ✓. The facts table means the Dev never has to guess what the workflow
  must match.
- **Technical guidance** — ✓ after correction. The one command that could not run is fixed; the
  non-obvious decisions (alias-as-copy, `gofmt -l` needing an explicit emptiness check, `GOARM=7`,
  the matrix name fragment being data rather than derived) are each called out with a reason.
- **Reference effectiveness** — ✓ after correction. Every cited line was opened; three were stale.
- **Self-containment** — ✓. Dev needs no other document.
- **Testability** — ✓, and unusually strong for a CI story. Task 8 requires *reproducing* the
  build-and-package pipeline outside CI and checking the artefacts, which converts AC3–AC8 from
  "written correctly" to "verified". Task 9 is a real negative test with evidence as its deliverable.

**Readiness: READY.** Clarity 9/10 — one point off for the unrunnable `python3` command, which was in
the task specifically meant to be the cheap safety net.

### Note for the Dev Agent

1. **Task 10's parse runs on the host.** There is no `python3` in `go-dev`. Say so in your record, and
   say why it is not a breach of the Docker-only rule.
2. **Task 9 breaks line 49, not line 43**, and must revert immediately and re-run clean. A
   negative-path test that leaves the positive path broken is worse than no test.
3. **Task 8 is the load-bearing task in this story, not Task 2–7.** Anyone can write plausible YAML.
   What makes twelve ACs real is reproducing the builds and the packaging by hand and checking the
   artefacts — especially `sha256sum canonical alias` returning two identical hashes, which is the
   one check that proves the aliasing decision was implemented as reasoned.
4. **Leave nothing behind.** Task 8 produces four binaries and eight tarballs. `bin/` and
   `__debug_bin*` are gitignored; a stray `dist/` or root-level `*.tar.gz` is not.

## Dev Agent Record

### Agent Model Used

James (Dev), running as Claude Opus 5.

### Debug Log References

**Task 8 — the build and packaging pipeline, reproduced outside CI.** This is the task that turns
ACs 3–8 from "the YAML looks right" into "the artefacts were checked", and it is where the story's
one factual error surfaced. Everything below ran through `docker compose exec go-dev` from `dev/`.

*AC3 — all four targets cross-compile with the workflow's exact flags* (stand-in version
`0.0.0-local`):

```
linux_amd64   OK      linux_arm64   OK      linux_armv7   OK      darwin_arm64  OK
```

*AC3 — each binary is genuinely for the architecture it claims.* Not inferred from the environment
variables that were passed, but read back out of the binary's own embedded build settings with
`go version -m`, which is the only check that would catch a `GOARCH` that was silently ignored:

| Binary | Embedded build settings |
| --- | --- |
| `linux_amd64` | `GOOS=linux`, `GOARCH=amd64`, `CGO_ENABLED=0`, `-ldflags="-s -w -X main.Version=0.0.0-local"` |
| `linux_arm64` | `GOOS=linux`, `GOARCH=arm64`, `GOARM64=v8.0`, `CGO_ENABLED=0`, same ldflags |
| `linux_armv7` | `GOOS=linux`, `GOARCH=arm`, **`GOARM=7`**, `CGO_ENABLED=0`, same ldflags |
| `darwin_arm64` | `GOOS=darwin`, `GOARCH=arm64`, `GOARM64=v8.0`, `CGO_ENABLED=0`, same ldflags |

Sizes: 9.68 MB (amd64), 9.37 MB (arm64), 9.18 MB (armv7), 9.63 MB (darwin/arm64) — consistent with
Story 3.1's measured 9.4 MB stripped binary, which is the independent confirmation that `-s -w` is
being applied rather than silently dropped.

*AC4 — the version injection actually reaches the log line AC12 asserts on.* The `linux/amd64`
binary is runnable inside the container, so it was run rather than reasoned about:

```
time=2026-07-29T09:02:29.082Z level=INFO msg="MQTT2BDD starting" component=main event=starting version=0.0.0-local
time=2026-07-29T09:02:29.084Z level=INFO msg="configuration loaded" ...
```

The process then fails to reach a broker and exits non-zero, exactly as the story predicts. The
assertion is on the first line, emitted at `main.go:210`, before either `Connect` call (`:217`,
`:228`). **This is AC12's binary assertion, exercised locally on a stand-in version.**

*AC5, AC6, AC7 — packaging, aliasing, archive contents:*

```
tar -tvzf mqtt2bdd_0.0.0-local_linux_amd64.tar.gz
  -rwxr-xr-x  root/root  9679000  mqtt2bdd      <- executable bit set (AC7)
  -rw-r--r--  root/root     1069  LICENSE
  -rw-r--r--  root/root    23272  README.md
member count, all four archives: 3, 3, 3, 3     <- no leading directory component
```

Canonical vs alias, all four pairs:

```
eda8330b…f949  mqtt2bdd_0.0.0-local_linux_amd64.tar.gz
eda8330b…f949  mqtt2bdd_linux_amd64.tar.gz
0f378219…406c  mqtt2bdd_0.0.0-local_linux_arm64.tar.gz
0f378219…406c  mqtt2bdd_linux_arm64.tar.gz
4d06f7d8…9b7c  mqtt2bdd_0.0.0-local_linux_armv7.tar.gz
4d06f7d8…9b7c  mqtt2bdd_linux_armv7.tar.gz
01a58efb…bf7c  mqtt2bdd_0.0.0-local_darwin_arm64.tar.gz
01a58efb…bf7c  mqtt2bdd_darwin_arm64.tar.gz
```

Four identical pairs. **This is AC12's checksum assertion, exercised locally.**

*AC8 — `checksums.txt`:* `sha256sum *.tar.gz | sort` produced **8 lines, 4 distinct hashes**. Then,
simulating a user who downloaded exactly one archive: copying only `mqtt2bdd_linux_amd64.tar.gz` and
`checksums.txt` into an empty directory and running `sha256sum --ignore-missing -c checksums.txt`
verified that one file and exited 0. This is precisely the sequence Story 4.4 AC5 will document, so
4.4 will be documenting something known to work.

Extraction was also checked end to end: `tar -xzf` dropped three files with no directory wrapper, and
the extracted binary ran and logged `version=0.0.0-local`.

**Task 9 — TEST-001, the negative test (AC14).** Open since gate `3.3`; this is the first time the
script's failure path has actually been run.

*Breakage introduced*, one line, on the read side (`test/run-integration-tests.sh:49`), leaving
everything upstream healthy so the branch under test is isolated:

```
-  "SELECT COUNT(*) FROM sensor_metrics WHERE sensor='integration/smoke';"
+  "SELECT COUNT(*) FROM sensor_metrics WHERE sensor='integration/deliberately-absent';"
```

*Result:*

```
EXIT CODE = 1
FAIL: smoke-test message not found in sensor_metrics (rows=0)     <- the intended branch, confirmed
```

*Teardown despite the early `exit 1`* — the half of AC14 that the exit code alone does not prove:

```
docker ps      -a --filter name=mqtt2bdd-test  ->  (nothing)
docker network ls --filter name=mqtt2bdd-test  ->  (nothing)
docker volume ls --filter name=mqtt2bdd-test   ->  (nothing)
```

Containers, network **and** volumes all removed. The `trap cleanup EXIT` fires on the failure path,
not only the success path, which is what makes this script safe to put in front of a release.

*Reverted and re-verified positive:* `git checkout -- test/run-integration-tests.sh`,
`git diff --stat` empty (byte-identical to committed state), line 49 back to `integration/smoke`,
and an unmodified re-run exited **0** with `==> All integration tests passed.`

**Task 10 — static validation.** The YAML parse ran **on the host**, not in `go-dev`. The container
is `FROM golang:1.23-alpine` and has no `python3`; the host has PyYAML 6.0.3. This is not a breach of
the Docker-only tooling rule — that rule exists so the Go toolchain and the database/broker clients
match the versions the project builds against, and a syntax check on a CI configuration file is none
of those. Everything in this story that touches Go, Postgres or MQTT went through the container.

Structural claims read from the **parsed document**, not from the text:

```
trigger keys   : ['push']                only a tag filter; no branches, no workflow_dispatch (AC1)
tag patterns   : ['v*.*.*']
permissions    : {'contents': 'write'}   nothing broader (AC11)
jobs           : quality -> build (needs: quality) -> release (needs: [quality, build])   (AC2)
matrix entries : 4, with goarm='7' present on linux_armv7 only                            (AC3)
actions used   : actions/checkout@v4, actions/setup-go@v5,
                 actions/upload-artifact@v4, actions/download-artifact@v4
all first-party: True
```

`grep -n "secrets\." .github/workflows/release.yml` → **no match**. The only token reference is
`GH_TOKEN: ${{ github.token }}` at line 256 (AC11 — no repository secret is configured or needed).

File confirmed at exactly `.github/workflows/release.yml`. 280 lines, 138 of them comments (AC13).

**Extra check not in the story, worth having.** The matrix passes `GOARM: ${{ matrix.goarm }}` to
every target, which expands to an **empty string** for the three non-ARM entries. An empty `GOARM`
rejected by the toolchain would have failed only in CI, on a tag, which is the worst place to find
it. Verified directly: `GOARM= GOOS=linux GOARCH=amd64 go build` and
`GOARM= GOOS=darwin GOARCH=arm64 go build` both succeed.

### Completion Notes List

- **Task 1 (facts).** Every row of the story's facts table re-read from source before any YAML was
  written. All confirmed, including the two the PO had already corrected (`main.go:210` precedes
  `Connect` at `:217`/`:228`; the smoke-test query is at `:49`).
- **Task 2 (trigger, permissions, version).** Tag filter only. `permissions: contents: write` at
  workflow level. `GH_TOKEN: ${{ github.token }}` and no secret. `${GITHUB_REF_NAME#v}` for the
  version, with the reason for POSIX prefix removal over `sed` in a comment.
- **Task 3 (quality gate).** `go-version-file: go.mod` rather than a hardcoded version.
  **`gofmt -l .` given an explicit non-empty-output failure** — the story is right that a bare
  `gofmt -l .` step can never fail, and a gate that always passes is worse than no gate because it
  looks like coverage. `staticcheck` pinned to `v0.6.1`, matching `dev/Dockerfile.dev:8`.
  `timeout-minutes: 20` on the integration step, because the script's own failure modes are bounded
  but Docker's are not. No `continue-on-error` anywhere.
- **Task 4 (matrix).** Four entries; the name fragment is matrix **data**, not derived from
  `GOOS`/`GOARCH` — deriving it would produce `linux_arm` where AC5 and AC6 both name the file
  `linux_armv7`. `fail-fast: false` so one broken platform reports one failure instead of hiding the
  other three. The `CGO_ENABLED=0`-also-enables-cross-compilation connection is commented, since it
  is one flag doing two jobs.
- **Task 5 (packaging).** `LICENSE` and `README.md` existence checked before tarring rather than
  assumed — a missing `LICENSE` would otherwise ship silently in every archive of every release.
  `-C stage` so there is no leading directory. Alias by `cp`.
- **Task 6 (release job).** `needs: [quality, build]` states the guarantee where it matters rather
  than relying on transitivity. `checksums.txt` from `sha256sum *.tar.gz | sort`, which is already
  the `sha256sum -c` input format, with an **assertion** of 8 lines and 4 distinct hashes — that
  assertion is the point of the step, not the file, because it is what would catch a regression in
  the aliasing before anything is published. `gh release create` rather than a third-party action,
  with the trade-off noted in a comment. `--generate-notes`, and `--prerelease` from a `-`-in-version
  test justified by semver rather than by enumerating suffixes.
- **Task 7 (comments).** 138 comment lines in 280. Every job says what depends on it; each of the
  five non-obvious steps AC13 names carries its *why*. Nothing restates the obvious —
  `actions/checkout@v4` has no comment, because it needs none.
- **Task 8 (local reproduction).** Above. Every artefact checked, not inferred.
- **Task 9 (TEST-001).** Above. Evidence, not assertion — which is what the finding has been open
  about since gate `3.3`.
- **Task 10 (static validation).** Above.
- **Task 11 (AC12).** Not executed. See the dedicated section below.
- **Cleanup.** All four binaries, all eight archives, both scratch tarballs and both scratch
  directories removed. `git status --short` shows only `.github/` and this story file. No `dist/`, no
  root-level `*.tar.gz`.

### Correction to the Story's Own Reasoning (found during Task 8)

The story's Dev Notes section "Why the Alias Must Be a Copy, Not a Second Tar" asserted that gzip
embeds an mtime, so two `tar -czf` invocations a second apart would produce different bytes and
therefore different checksums.

**That is empirically false, and I measured it rather than assuming either way:**

| Toolchain | Two `tar -czf` runs, 2 s apart, identical inputs |
| --- | --- |
| BusyBox tar (the `go-dev` container) | **identical** SHA-256 |
| GNU tar 1.35 / gzip 1.13 (`debian:stable-slim`, the toolchain `ubuntu-latest` ships) | **identical** SHA-256 |

The mechanism the draft missed: gzip writes an MTIME only when compressing a **named file**. When it
compresses a **stream** — which is exactly what `tar -z` hands it — the MTIME field is written as 0.
So there was no non-determinism to work around in the first place.

**The `cp` decision still stands, on better grounds.** A second `tar` would work *today*, by an
accident of how gzip handles streams. `cp` makes AC6's byte-identity structural: independent of the
runner image's `tar` after its next update, of directory read order, and of anything a future edit to
that step introduces. The failure mode being avoided is not "the checksums differ today" but "the
checksums start differing at some point, in an artefact nobody re-checks before publishing, and the
first person to notice is a user whose `sha256sum -c` fails".

The shipped workflow comment states this corrected reasoning, not the draft's. The story's Dev Notes
section carries an inline correction block marked as such, rather than being quietly rewritten — the
wrong premise is part of the record of how the decision was reached.

### AC12 — Not Done, Handed Over

**AC12 is not met.** It requires a workflow run on GitHub's runners, triggered by a tag pushed to a
repository that does not exist, because Story 4.2's ACs 6–8 are deferred under PUB-001. No tag was
created, nothing was pushed, and no run was triggered.

**What is already pre-verified, and what genuinely is not.** Two of AC12's four assertions were
exercised locally in Task 8 and are not what the rehearsal is really testing:

| AC12 assertion | Status |
| --- | --- |
| "a canonical and an alias archive verify to the same SHA-256" | **Pre-verified locally** — four identical pairs, plus `sha256sum --ignore-missing -c` verifying a single archive. The mechanism is a `cp`; it cannot behave differently on a runner. |
| "the extracted `linux/amd64` binary runs and logs `event=starting version=…`" | **Pre-verified locally** at `version=0.0.0-local`. The flag, the symbol and the log line are confirmed to work together. |
| "the run succeeds" | **Requires GitHub.** The real content of the rehearsal: runner environment, action availability, `gh` authentication, and whether the integration suite passes on a runner rather than on a developer's Docker. |
| "all nine assets are present" | **Requires GitHub.** Depends on artifact upload/download and `gh release create`, none of which is locally observable. |

**Handover — run after Story 4.2's PUB-001 publication:**

```bash
git tag v0.9.0-rc1 && git push origin v0.9.0-rc1
gh run watch

gh release view v0.9.0-rc1 --json isPrerelease,assets \
  --jq '{prerelease: .isPrerelease, count: (.assets|length), names: [.assets[].name]}'
#   Expect: prerelease true, count 9.

gh release download v0.9.0-rc1 -p 'mqtt2bdd_0.9.0-rc1_linux_amd64.tar.gz' \
                               -p 'mqtt2bdd_linux_amd64.tar.gz' -p 'checksums.txt'
sha256sum mqtt2bdd_0.9.0-rc1_linux_amd64.tar.gz mqtt2bdd_linux_amd64.tar.gz   # identical
sha256sum --ignore-missing -c checksums.txt                                   # both OK

tar -xzf mqtt2bdd_0.9.0-rc1_linux_amd64.tar.gz
./mqtt2bdd 2>&1 | head -1     # on Linux; expect event=starting version=0.9.0-rc1
#   Non-zero exit afterwards is expected — no broker is configured.

# Only once all of the above pass:
git tag v1.0.0 && git push origin v1.0.0
```

**If the rehearsal fails:** `git tag -d v0.9.0-rc1 && git push --delete origin v0.9.0-rc1`,
`gh release delete v0.9.0-rc1 --yes`, fix, re-tag. That recovery is exactly what AC2 describes and
exactly why the rehearsal happens on `v0.9.0-rc1` rather than on `v1.0.0`.

### Note for Story 4.4

Two things found while verifying AC8 that Story 4.4 AC5 needs, since it must document the checksum
sequence:

1. **`sha256sum --ignore-missing -c checksums.txt` works on GNU coreutils (Linux) and on macOS**
   (both `sha256sum` and `shasum -a 256` accept it) — verified on both. So the command AC5 names is
   correct for every platform this project publishes a binary for.
2. **It does *not* work on BusyBox** (`sha256sum: unrecognized option: ignore-missing`), which is
   what Alpine ships. A user verifying a checksum inside an Alpine container would need
   `sha256sum -c` against a filtered `checksums.txt` instead. Marginal for the audience — a
   home-automation user is on Raspberry Pi OS, Debian, Ubuntu or macOS — but worth one line rather
   than a support question.

### File List

- `.github/workflows/release.yml` — **new.** 280 lines, 138 of them comments. Three jobs:
  `quality` (four static/unit gates plus the integration suite), `build` (a four-entry
  cross-compilation matrix that packages a canonical/alias archive pair per target), and `release`
  (checksums with an 8-lines/4-hashes assertion, then `gh release create` with auto-generated notes
  and a conditional `--prerelease`).
- `docs/stories/4.3.story.md` — this file.

**Deliberately not modified:** `test/run-integration-tests.sh` (Task 9's breakage was temporary and
reverted to a byte-identical file — `git diff --stat` empty), every `.go` file, `go.mod`, `go.sum`,
`Dockerfile`, all Compose files, every architecture shard, and **`README.md`** — the Project
Structure tree entry for `.github/workflows/` and the release-cutting documentation are Story 4.4's
ACs 8 and 9.

## QA Results

### Review Date: 2026-07-29

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

A CI workflow is unusually easy to review badly: the YAML reads plausibly, nothing runs, and the
reviewer nods. This story does not allow that, and the credit belongs to Task 8 — the Dev reproduced
the entire build-and-package pipeline outside CI and checked the artefacts, so ACs 3 through 8 are
backed by measurements rather than by intent. That is the difference between a workflow that looks
right and one that is known to produce what it claims.

Two things in the implementation are better than the story asked for. The Dev verified each binary's
architecture by reading `GOOS`/`GOARCH`/`GOARM`/`CGO_ENABLED` back out of the binary's **own embedded
build settings** (`go version -m`) rather than trusting the environment variables that were passed in
— which is the only form of that check that would catch a silently ignored `GOARCH`. And the Dev
tested that an empty `GOARM` (what the matrix passes to the three non-ARM targets) does not break the
build, which the story never asked for and which would otherwise have been a CI-only failure on a
tagged release.

The workflow itself is 286 lines, 144 of them comments, and the comments explain decisions rather
than restating steps. `actions/checkout@v4` carries no comment, correctly.

### Refactoring Performed

- **`.github/workflows/release.yml`** — Staticcheck step changed from `staticcheck ./...` to
  `"$(go env GOPATH)/bin/staticcheck" ./...`, with the reasoning added as a comment. See CI-001
  below. Workflow re-parsed clean after the edit.

No other change was needed.

### Compliance Check

- Coding Standards: ✓ — English throughout; comments explain *why*; nothing restates the obvious.
- Project Structure: ✓ — the file is at exactly `.github/workflows/release.yml`, which is the only
  path GitHub reads. `README.md` correctly untouched (Story 4.4 AC8/AC9).
- Testing Strategy: ✓ — no unit test written, correctly: asserting on YAML structure in Go would test
  the test. Reproduction (Task 8) and a real negative test (Task 9) are the right substitutes, and
  both were actually performed.
- ACs Met: **13 of 14.** AC12 not met — inherited publication dependency, see PUB-002.

### Requirements Traceability (independently re-derived)

- **AC1** — file at `.github/workflows/release.yml`; parsed document shows trigger keys `['push']`
  with `tags: ['v*.*.*']` and nothing else. No `branches`, no `workflow_dispatch`.
- **AC2** — three jobs, with `build` → `needs: quality` and `release` → `needs: [quality, build]`,
  read from the parsed document. The guarantee is structural: `needs:` cannot be overridden by
  forgetting. No `continue-on-error` anywhere in the file. **The `gofmt` step is the one I checked
  hardest**, because `gofmt -l` exits 0 even when it lists files — a bare step would have been a gate
  that can never fail, which is worse than no gate because it reads as coverage. It captures the
  output and fails explicitly on non-empty. Correct.
- **AC3** — all four targets cross-compiled locally, and each binary's architecture read back from
  its own embedded build settings: `GOARCH=amd64`; `GOARCH=arm64`; `GOARCH=arm` with **`GOARM=7`**;
  `GOOS=darwin GOARCH=arm64`. The matrix name fragment is data rather than derived, with the reason
  commented — deriving it from `GOOS`/`GOARCH` would produce `linux_arm`, and AC5 and AC6 both name
  the file `linux_armv7`.
- **AC4** — flags compared against `Dockerfile:23-25` and identical. Verified beyond string
  comparison: the locally built binary logs `event=starting version=0.0.0-local`, so `-X main.Version`
  demonstrably reaches `main.go:210`. Binary sizes (9.18–9.68 MB) are consistent with Story 3.1's
  measured 9.4 MB stripped binary, which independently confirms `-s -w` is being applied rather than
  silently dropped.
- **AC5/AC6/AC7** — reproduced: three members per archive with the binary's executable bit set, no
  leading directory component, canonical name version-bearing and alias not, and **four
  byte-identical canonical/alias pairs** by SHA-256.
- **AC8** — reproduced `checksums.txt` at exactly 8 lines and 4 distinct hashes, in `sha256sum`'s
  native format. Then the user's-eye check: copying one archive and `checksums.txt` into an empty
  directory and running `sha256sum --ignore-missing -c` verified that one file and exited 0. The
  workflow **asserts** this property rather than merely writing the file, which is what would catch a
  regression in the aliasing before publication rather than after.
- **AC9/AC10** — `gh release create` with `--generate-notes` and all nine assets in one call;
  `--prerelease` conditional on a `-` in the version, which is exact rather than heuristic because
  semver defines everything after the first hyphen as the pre-release identifier.
- **AC11** — `permissions: contents: write` at workflow level and nothing broader.
  `grep -n "secrets\." .github/workflows/release.yml` → **no match**; the only token reference is
  `GH_TOKEN: ${{ github.token }}`. Every `uses:` is first-party `actions/*`, verified from the parsed
  document.
- **AC13** — 144 comment lines in 286. All five of the non-obvious items AC13 names or implies carry
  their rationale: the `v*.*.*`-matches-`v0.9.0-rc1` glob semantics, `${GITHUB_REF_NAME#v}` and why
  not `sed`, `GOARM=7` and 32-bit Raspberry Pi OS, why each archive is published twice and why the
  alias is a copy, and why `gofmt -l` needs an explicit emptiness check.
- **AC14** — see TEST-001 below.

### Findings

**CI-001 — low severity, latent, found and fixed during review.**

The Staticcheck step ran `go install honnef.co/go/tools/cmd/staticcheck@v0.6.1` and then invoked
`staticcheck ./...` **by bare name**. `go install` places the binary in `$(go env GOPATH)/bin`, and
whether that directory is on `PATH` is a property of the runner image and of `actions/setup-go`'s
current behaviour — not something this workflow declares or controls.

It works today. Nothing in the file makes it keep working. And the failure mode is the bad one: it
would surface as `staticcheck: command not found` during a tagged release run, in the job whose
entire purpose is to stop bad releases, discovered by someone who had just tagged `v1.0.0`.

Fixed by invoking the absolute path, with the reasoning in a comment. This is a one-line change that
converts a dependency on undocumented behaviour into a dependency on nothing. I would not normally
touch a workflow during review, but a gate that can silently stop being a gate is worth the edit.

**TEST-001 — closed, and closed properly.**

Open since gate `3.3`, where AC9's non-zero-exit path was accepted on the Dev's account without being
re-run. This story finally exercised it, and the evidence is the right evidence:

- The deliberate breakage was on the **read** side (`test/run-integration-tests.sh:49`), not the
  publish side, so the branch under test was reached with everything upstream healthy.
- Exit code **1**, on the intended branch —
  `FAIL: smoke-test message not found in sensor_metrics (rows=0)`.
- **Containers, network *and* volumes all removed** despite the early `exit 1`. This is the half an
  exit code cannot prove, and the half that matters for CI: a cleanup that only ran on the success
  path would leave a runner dirty and a developer's machine with orphaned containers. Checking
  `docker volume ls` and not just `docker ps -a` is the detail that makes this evidence rather than a
  gesture.
- Reverted to a **byte-identical** file (`git diff --stat` empty) and the unmodified script re-run to
  exit **0**. A negative test that leaves the positive path broken is worse than no test.

The SM's reasoning for homing this here is worth preserving as a convention: the story does not
*modify* the script, but AC2 changes the finding's **consequence** — from "a developer believes a
test passed" to "a broken build ships as a binary people download and run". Epic 4 has now applied
that principle twice (DOC-001 onto 4.2, TEST-001 onto 4.3), and both times it produced the right
answer.

**PUB-002 — medium, open, owned by the maintainer, not a defect.**

AC12 is not met. It needs a workflow run on GitHub's runners against a repository that does not exist
under PUB-001. Nothing was tagged and nothing was pushed.

What I want on the record is the Dev's framing, because it is more useful than "deferred": **two of
AC12's four assertions are already exercised.** The canonical/alias SHA-256 identity and the
"extracted binary logs the injected version" assertion were both performed locally in Task 8. So what
the rehearsal actually remains to test is the *runner environment* and the *release-creation path* —
action availability, `gh` authentication, artifact upload/download, and whether the integration suite
behaves on a runner as it does on a developer's Docker. The build and packaging logic is not what is
at risk. That is a precise statement of residual risk rather than a blanket unknown.

### On the Dev's Correction to the Story

The Dev found that the story's stated reason for copying the alias archive — that gzip embeds an
mtime, so two `tar -czf` runs would differ — is **empirically false**, and measured it on both GNU
tar 1.35 / gzip 1.13 and BusyBox rather than assuming either way. The mechanism the draft missed is
that gzip writes MTIME=0 when compressing a stream, which is exactly what `tar -z` hands it.

I re-ran the GNU-toolchain half of that measurement myself and confirm it: two archives, two seconds
apart, identical SHA-256.

**The decision to use `cp` is still right, and the Dev's corrected reasoning is better than the
original.** A second `tar` would work today by an accident of gzip's stream handling; `cp` makes
AC6's byte-identity structural, independent of the runner image's `tar` after its next update. The
failure being avoided is not "the checksums differ today" but "the checksums start differing in an
artefact nobody re-checks, and the first person to notice is a user whose verification fails".

The handling is also right: the shipped comment states the corrected reasoning, and the story's Dev
Notes carries an inline correction block rather than being quietly rewritten. The wrong premise is
part of how the decision was reached, and deleting it would make the record less honest, not tidier.

### Improvements Checklist

- [x] CI-001: Staticcheck invoked by absolute path rather than depending on `$GOPATH/bin` being on
      `PATH` (fixed by QA during this review).
- [x] TEST-001 closed with evidence covering containers, network **and** volumes.
- [x] Build and packaging claims verified by reproduction rather than inspection.
- [x] Empty-`GOARM` behaviour verified — a CI-only failure mode caught locally.
- [ ] **PUB-002**: AC12's rehearsal awaits PUB-001.
- [ ] Story 4.4 AC5 must note that `sha256sum --ignore-missing` works on GNU coreutils and macOS but
      **not** on BusyBox. Verified during this story; marginal for the audience, but it is one line
      versus a support question.
- [ ] REL-001 unchanged and still unowned; no Epic 4 story touches Go source.

### Security Review

`permissions: contents: write` is declared at workflow level and nothing broader, so the workflow
cannot silently inherit a wider default if the repository's is ever loosened. No repository secret is
referenced or required — `grep "secrets\."` returns nothing, and the only token is `github.token`.

Every action is first-party `actions/*`, and the release is created with the preinstalled `gh` CLI
rather than a third-party action. For a workflow whose entire output is artefacts that other people
download and execute, that is the correct trade: a few more lines in exchange for nothing added to
the supply chain, and the Dev noted the trade-off in a comment rather than presenting it as free.

One thing I checked rather than assumed: the release job has **no** `actions/checkout`, so `gh` has
no local git repository to infer a target from. It works because `--repo "${GITHUB_REPOSITORY}"` is
passed explicitly. Omitting the checkout is deliberate and correct — that job needs artifacts, not
source.

### Performance Considerations

Four matrix legs run in parallel. `fail-fast: false` means one broken platform reports one failure
rather than masking the other three, and the release job still cannot run, because `needs: build`
requires every leg to succeed. `setup-go`'s module caching is on by default. Artifact retention is
one day, appropriate for a hand-off inside a single run.

### Files Modified During Review

`.github/workflows/release.yml` — one step changed (CI-001). No other file touched.

### Gate Status

Gate: **CONCERNS** → `docs/qa/gates/4.3-automate-cross-platform-binary-builds.yml`
Quality score: **85** (one medium open item, PUB-002, inherited from PUB-001; one low-severity
defect found and fixed during review)

### Recommended Status

**✓ Ready for Done, with AC12 explicitly open** — subject to PO acceptance.

Story 4.4 is unblocked and can be drafted immediately: it documents this workflow's output, and the
output's shape is now known from measurement rather than from the workflow's own promises — which is
a better position than 4.4 would otherwise have been in.

### Post-Publication Re-Review: 2026-09-15

### Reviewed By: Quinn (Test Architect)

AC12 was deferred under PUB-002 until the repository existed. The maintainer published it and pushed tag `v0.0.9-rc1` on 2026-07-31, and the workflow ran. Nobody had verified that rehearsal against the story until now. Everything below was checked against the live run, the live release and the downloaded assets, not against the fact that a release page exists.

### AC12 — The Rehearsal, Assertion by Assertion

- **"the run succeeds"** — Run `30624610132`, the repository's only workflow run, succeeded on attempt 1. All six jobs and every step are green: Quality gates, four builds, Publish release. The step statuses alone were not taken as proof. The log shows the unit tests and the integration suite really running (`TestMQTTReconnect`, `TestShutdownDrainBounded`, `TestGracefulShutdown`, `cmd/mqtt2bdd` in 82.9 s), the script's own `==> All integration tests passed.`, then `==> Tearing down test stack` removing the three test containers and the test network. Also confirmed: `Staticcheck` succeeded on a real runner, which is CI-001's fix working where it was meant to.
- **"all nine assets are present"** — Nine assets, named exactly as AC5, AC6 and AC8 require at version `0.0.9-rc1`. The release was created by `github-actions[bot]`, with the `gh release create … --generate-notes` call visible in the log, so no asset was uploaded by hand.
- **"a canonical and an alias archive verify to the same SHA-256"** — This holds for all four pairs, not just one: GitHub's per-asset digests match pairwise. All nine assets were downloaded, and `sha256sum -c checksums.txt` in the `go-dev` container reports **OK** for all eight archives, with 8 lines and 4 distinct hashes.
- **"the extracted `linux/amd64` binary runs and logs `event=starting version=…`"** — The `linux/amd64` binary was extracted and run in `go-dev` under amd64 emulation. Its first line is `msg="MQTT2BDD starting" component=main event=starting version=0.0.9-rc1`. The `linux/arm64` binary, run natively, logs the same line.
- **"before `v1.0.0` is tagged"** — Holds: the only tag is `v0.0.9-rc1`.

### The Other ACs, Now Observable on a Real Release

- **AC1** — The history holds a single run, triggered by the tag. None of the 74 commits pushed to `main` triggered a run.
- **AC3, AC4** — Read back with `go version -m` from each **released** binary's embedded build info, not from a local rebuild. The four binaries are `linux/amd64`, `linux/arm64`, `linux/arm` with `GOARM=7`, and `darwin/arm64`. All are `CGO_ENABLED=0`, built with `-ldflags="-s -w -X main.Version=0.0.9-rc1"` from `vcs.revision=4496553` with `go1.23.12`.
- **AC7** — Each archive holds exactly `mqtt2bdd` (`-rwxr-xr-x`), `LICENSE` and `README.md`, with no leading directory. `LICENSE` and `README.md` hash identically to the files at `4496553`.
- **AC10** — The release is marked pre-release. `GET releases/latest` returns **404**, and so does `releases/latest/download/mqtt2bdd_linux_amd64.tar.gz`. The pinned `releases/download/v0.0.9-rc1/mqtt2bdd_linux_amd64.tar.gz` returns **200** (3,449,980 bytes). An rc is therefore never served as "latest", which is exactly what the AC intends.
- **AC11** — `gh secret list` and `gh variable list` are empty, and the repository has 0 environments. The repository's **default workflow token permission is `read`**. The workflow's explicit `contents: write` is therefore not a formality: without it the release could not have been created, which is the very reason the file gives for declaring it.
- **Workflow identity** — `release.yml` has the same blob (`ed904a3`) at the rehearsed commit and at `HEAD`. The rehearsal validated the file as it stands today.

### Findings

**REQ-001 — low, owner PO.** AC12 names `v0.9.0-rc1`; the rehearsal ran on **`v0.0.9-rc1`**. Every assertion holds with the version substituted, and the tag carries the `-rc1` suffix the rehearsal exists to exercise. The deviation is in the literal tag only. It is recorded rather than silently read as equivalent, because this project does not reinterpret ACs; accepting the substitution is the PO's call. It has one irreversible side effect: `v0.0.9-rc1` is now a permanent version on `proxy.golang.org`, and `@latest` resolves to it until a release version exists. That is harmless. A second rehearsal only to match the digits is not worth running on its own, but see CI-002.

**CI-002 — low, owner Dev.** All six jobs log `Node.js 20 is deprecated`: `actions/checkout@v4`, `actions/setup-go@v5`, `actions/upload-artifact@v4` and `actions/download-artifact@v4` target Node.js 20, and the runner forces them onto Node.js 24. The run succeeded, so this is not a defect today. It has the same shape as CI-001, though: a dependency on runner behaviour the workflow does not control, which would surface only on a tagged release. Suggested action: before `v1.0.0`, move each action to its current Node.js 24 major, checked against each action's release notes, then **rehearse again on an rc tag**. Changing the actions invalidates the part of the rehearsal that validated the runner, and that new rehearsal can use `v0.9.0-rc1`.

**PUB-002 — resolved.** AC12 is met, and the tag deviation is tracked as REQ-001.

### Method Note

The two binaries were first run in `go-dev` without clearing its environment. They picked up the dev configuration and started connecting to the dev broker, for at most 5 s each, before `timeout` stopped them. No process remained. The dev database was checked afterwards: 112 rows, newest dated 2026-07-25, so nothing was written. Next time, run them with `env -i` so the check stays isolated. The dev containers were started for this review and stopped at the end.

### For Story 4.4 (PUB-003)

The `releases/latest/download/…` URLs return 404 and will keep doing so until a release that is not a pre-release exists. That is this story's AC10 working as designed. The README's "latest" install commands cannot resolve before `v1.0.0` is published, which bears directly on 4.4's AC10.

### Gate Status

Gate: PASS → docs/qa/gates/4.3-automate-cross-platform-binary-builds.yml
Quality score: **100** (previously CONCERNS, 85). REQ-001 and CI-002 are low and do not reduce the score.

### Recommended Status

**✓ Done**, subject to a PO post-publication validation that records the REQ-001 acceptance. The Status field is the PO's to change.

### CI-002 Fix Applied (2026-09-15)

At the maintainer's request, `.github/workflows/release.yml` now pins every action to a major that declares `runs.using: node24`: `actions/checkout@v7`, `actions/setup-go@v7`, `actions/upload-artifact@v7`, `actions/download-artifact@v8`. The runtime of each major tag was read from its `action.yml`, not assumed. The release notes of every major crossed were then checked against what this workflow actually uses:

| Action | Breaking changes crossed | Effect on this workflow |
| --- | --- | --- |
| `checkout` v4 → v7 | v5 Node.js 24; v6 persists credentials to a separate file; v7 blocks fork-PR checkout on `pull_request_target`/`workflow_run` | None. Tag-push trigger only, nothing pushed back, no PR events. |
| `setup-go` v5 → v7 | v6 prefers go.mod's `toolchain` directive and sets `GOTOOLCHAIN=local`; v7 ESM | `go.mod` has `go 1.23` and no `toolchain` line, so the resolved Go stays the latest 1.23.x. `GOTOOLCHAIN=local` would fail a `go install` that needs a newer Go. `staticcheck@v0.6.1` declares `go 1.23` (its go.mod at `2025.1.1`), so it installs. This matches the official golang images the dev container uses. |
| `upload-artifact` v4 → v7 | v5/v6 Node.js 24; v7 adds `archive` (default `true`), ESM | None. `name`, `path`, `if-no-files-found`, `retention-days` unchanged; archives still zipped. |
| `download-artifact` v4 → v8 | v5 path change for single downloads by artifact ID; v8 digest mismatch errors by default, non-zip downloads not unzipped | None harmful. This job downloads by `pattern` with `merge-multiple`, not by ID; uploads are zipped (v7 default). The digest check is kept at its default on purpose: it is a gain for a release pipeline. |

The file also gained three comments, in keeping with its educational standard: why the majors are pinned to Node.js 24, what `GOTOOLCHAIN=local` changes, and why the digest check stays on.

**Not verified, and why CI-002 stays open.** A workflow only proves itself on a runner. The rehearsal of record (run `30624610132`) validated the *previous* action versions, so it says nothing about these. The next rc rehearsal is what closes CI-002. The tag it pushes is outward-facing and remains the maintainer's call. Suggested tag: `v0.9.0-rc1`, which also settles REQ-001 going forward. After it runs, check that no `Node.js 20 is deprecated` warning remains, and that every binary's build info still reports a `go1.23.x` toolchain.

**Deliberately not changed:** `docs/architecture/infrastructure-and-deployment.md` still shows `checkout@v4`/`setup-go@v5`. It does so inside an early design sketch (a `ci.yml` and a Docker Hub push) that was never implemented and already diverges from the real workflow. Bumping version numbers in it would make an inaccurate document look maintained.

### Rehearsal on `v0.0.9-rc2` — CI-002 Closed (2026-09-15)

The maintainer committed the bump (`698235b`, now GitHub's `main`) and tagged `v0.0.9-rc2`. Everything below was checked against the live run, release and assets.

- **Only the actions changed.** The tag is annotated and points at `698235b`. `release.yml` there differs from the rc1 version only in its `uses:` lines and comments; no functional line changed.
- **The run.** Run `35008908643` (attempt 1, 4m17s) succeeded, with all six jobs and every step green. The step names show the new majors: `checkout@v7`, `setup-go@v7`, `upload-artifact@v7`, `download-artifact@v8`. On Node.js 20:
  - "Node.js 20" appears **0** times in the full log;
  - the check-run annotations list on all six jobs is **empty**.

  The integration suite passed and its stack was torn down.
- **Go toolchain unchanged.** `setup-go` logged `Setup go version spec 1.23`, resolved it to **go1.23.12**, and set `GOTOOLCHAIN=local`. Staticcheck installed and ran under it, as predicted.
- **The new digest check is live.** `download-artifact` ran with `digest-mismatch: error` and listed each artifact's expected digest.
- **The release.** `v0.0.9-rc2` is marked pre-release and was created by `github-actions[bot]`. Its notes compare `v0.0.9-rc1...v0.0.9-rc2`. The nine assets have the expected names, and the API digests pair each canonical archive with its alias. In `go-dev`, `sha256sum -c checksums.txt` prints **8 OK**, with 4 distinct hashes, and matches the checksums the run logged.
- **The binaries.** All four report `go1.23.12`, `CGO_ENABLED=0`, `-ldflags="-s -w -X main.Version=0.0.9-rc2"`, `vcs.revision=698235b`, plus the right `GOOS`/`GOARCH` and `GOARM=7`. Each archive holds `mqtt2bdd` (`-rwxr-xr-x`), `LICENSE` and `README.md`, both identical to `698235b`. That also means Story 4.4's README fixes now ship in the archives. Run under `env -i` with its required variables and an unreachable broker, the `linux_amd64` binary logs `event=starting version=0.0.9-rc2`, then `startup_aborted`.
- **AC10 still holds.** `releases/latest` still returns 404, since two pre-releases do not make a release.

**CI-002 is resolved.** The action bump is proven on a runner and changed nothing in the artefacts' toolchain.

**REQ-001 stays open.** This rehearsal is again on a `v0.0.9-rcN` tag, not the `v0.9.0-rc1` AC12 names. The assertions hold for the second time, and the naming is now clearly a deliberate choice. Accepting the substitution remains the PO's call. No further rehearsal is needed for the workflow's sake: any future change to `release.yml` warrants another rc, and nothing else does.

Gate `4.3` remains **PASS**, quality score 100. Only REQ-001 (low) is open.

### `v1.0.0` Released (2026-09-15)

Run `35010177508` released `v1.0.0` from `ec06841`, using the same `release.yml` blob (`23625db`) the rc2 rehearsal validated. It succeeded on attempt 1, with zero non-success steps, no annotations and no `Node.js 20` line. Every job used `go1.23.12` under `GOTOOLCHAIN=local`. The release is **not** marked pre-release: the `*-*` rule correctly recognised a plain version. All four binaries report `main.Version=1.0.0` and `vcs.revision=ec06841`. The full detail is in Story 4.4's QA Results, since that release is what closes 4.4's AC10.

**Consequence for REQ-001.** AC12 reads "rehearsed end to end on a `v0.9.0-rc1` tag *before `v1.0.0` is tagged*". `v1.0.0` is now tagged, so the literal wording can never be satisfied. The only remaining resolution is PO acceptance of the two `v0.0.9-rcN` rehearsals, and the second of them validated exactly the workflow `v1.0.0` was built with.

## PO Validation (Final)

### Validation Date: 2026-07-29

### Validated By: Sarah (Product Owner)

### Verdict: **Accepted — Done, with AC12 open and owned by the maintainer**

### Method

Three agents have verified this story. I checked the four things that would matter most if they were
wrong, and one of them is not the workflow.

1. **The revert, first.** Task 9 deliberately broke a committed file to prove a failure path. A
   negative test that quietly leaves the file modified would be worse than the finding it closed —
   it would put a broken smoke-test query into the repository under cover of closing TEST-001. So I
   did not check `git diff`, which only shows the working tree; I checked what the repository
   actually records: `git log --oneline -1 -- test/run-integration-tests.sh` returns
   **`a14c785 Implementation of the story 3.3`**. The script's last modifying commit is still Story
   3.3's. The breakage was never committed, and the file is byte-identical to the state TEST-001 was
   raised against.

2. **The workflow parses and is shaped as described.** Three jobs — `quality`, `build`, `release` —
   read from the parsed document, not the text.

3. **QA's CI-001 fix is actually in the file**, at line 86:
   `"$(go env GOPATH)/bin/staticcheck" ./...`. I re-read the reasoning and agree with the edit
   without reservation. A quality gate whose invocation depends on undocumented runner behaviour is
   a gate that can silently stop being one, and the only place that would surface is a tagged
   release. One line to remove the question entirely is obviously worth it, and QA was right to make
   the change rather than merely note it.

4. **`grep -c "secrets\." .github/workflows/release.yml` → 0.** AC11 holds: the automatic token and
   nothing else.

### On the Dev's Correction to the Story's Own Reasoning

This is the part of this story I most want kept in the record.

The story told the Dev that the alias archive must be a `cp` rather than a second `tar` because gzip
embeds an mtime. The Dev implemented it, then **measured the premise and found it false** — on both
GNU tar 1.35 and BusyBox, two archives two seconds apart hash identically, because gzip writes
MTIME=0 when compressing a stream. QA independently re-ran the GNU half.

Three things went right here, and each is worth naming:

- **The Dev tested a claim it had been handed as settled.** Task 8 asked for the artefacts to be
  checked; nothing asked for the *reasoning* to be checked. It was anyway.
- **The conclusion survived on better grounds.** `cp` is still correct — not because two tars differ
  today, but because relying on them agreeing makes a published checksum depend on an implementation
  detail nobody would re-check before cutting a release. That is a stronger argument than the one the
  draft gave, and the shipped comment carries the stronger one.
- **The wrong premise was left in the record**, marked as corrected, rather than quietly rewritten.
  How a decision was reached is part of what a story file is for. A tidied history that shows only
  correct reasoning teaches the next reader that correct reasoning is what happens, which is not
  true and not useful.

I would rather have this than a story where nothing needed correcting.

### On AC12 and the Two Open Publication Items

Accepted as scoped, and the scoping is better than 4.2's because it is more precise.

Rather than declaring AC12 unreachable, the story splits its four assertions: the canonical/alias
SHA-256 identity and the "extracted binary logs the injected version" assertion were both exercised
locally in Task 8. What genuinely remains untested is the runner environment and the
release-creation path. That tells the maintainer what the rehearsal is actually for — action
availability, `gh` authentication, artifact round-tripping, and whether the integration suite behaves
on a runner as it does on local Docker — rather than leaving "untested" to cover everything.

The epic now carries two maintainer-owned open items, PUB-001 and PUB-002, the second blocked by the
first. Neither is story work and neither is routed to a story. Story 4.4 is not blocked by either.

**Status is `Done — Rehearsal Deferred`, not `Done`**, on the same reasoning as Story 4.2: the status
field is the only part of a story most future readers will read, and it must not overstate.

### On TEST-001 Finally Closing

Open since gate `3.3` and deferred at every gate since. It closes here on the principle the SM
articulated and I did not: not "the script is being touched" — it was not — but **"the consequence
of the finding changed"**. Until AC2, a false zero exit meant a developer believed a test passed.
From AC2, it means a broken build ships as a binary someone downloads and runs.

Epic 4 has now used that principle twice, on DOC-001 and on TEST-001, and both times it produced the
right answer where the original "next time the file is touched" deferral would have produced another
deferral. **I am adopting it as this project's convention**: a carry-forward finding is due when its
blast radius changes, not when its file is next edited. Recorded here so the next SM can cite it
rather than re-derive it.

With TEST-001 closed, the only genuinely unowned finding left project-wide is REL-001, which is not
actionable — it needs a behavioural change in `healthCheckLoop`, and no Epic 4 story touches Go
source.

### Result

Thirteen of fourteen ACs met, verified three times over and spot-checked once more here. One latent
defect found and fixed during review. One factual error in the story's own reasoning found by
measurement during implementation, corrected on the record, with the decision it supported unchanged
and better justified. Gate `4.3` (CONCERNS, quality_score 85) is warranted.

### Note for the SM, Drafting Story 4.4

1. **Scope AC10 the way 4.2 and 4.3 scoped theirs.** "Every URL and command verified against the
   actual published release" cannot be met before PUB-001. Split it as 4.3 split AC12: say which
   commands *can* be verified now against locally reproduced artefacts, and which genuinely need the
   release to exist.
2. **4.4 is in an unusually good position.** It documents the release artefacts, and their shape is
   now known from measurement rather than from the workflow's promises: archive names, the three
   members, the executable bit, the byte-identical aliases, the 8-line/4-hash `checksums.txt`, and
   the `sha256sum --ignore-missing -c` sequence were all exercised in Story 4.3's Task 8. Cite those
   measurements rather than re-deriving them from the YAML.
3. **AC5 needs one extra line QA found**: `sha256sum --ignore-missing` works on GNU coreutils and on
   macOS (`sha256sum` and `shasum -a 256` both), but **not** on BusyBox, which rejects the option.
   Marginal for the audience, but a reader in an Alpine container will hit it.
4. **AC8 owes two tree entries**: `LICENSE` (Story 4.2) and `.github/workflows/` (this story). Both
   now exist; neither is in `README.md`'s Project Structure tree yet, deliberately.
5. **4.4 is the last story in the epic.** It should say what remains open project-wide at closure —
   PUB-001, PUB-002 and REL-001 — the way Story 3.6 did, and this time without a "Project Closing
   Summary" that a later epic falsifies.

### Status

**Done — Rehearsal Deferred.** AC12 remains open under PUB-002, blocked by PUB-001, owned by the
maintainer.
