# Test broker configuration for the mqtt2bdd integration-test stack.
# Identical to dev/mosquitto/mosquitto.conf: no persistence, anonymous access. The test
# broker is disposable by design — nothing here is meant to survive a restart.

listener 1883
allow_anonymous true
